---
title: 740.delete-and-earn
date: 2026-04-09 11:50:00
categories: medium
tags: [数组, 哈希表, 动态规划]
---

## 题目描述

给你一个整数数组 `nums`，你可以对它进行一些操作。

每次操作中，选择任意一个 `nums[i]`，删除它并获得 `nums[i]` 的点数。之后，你必须删除 **所有** 等于 `nums[i] - 1` 和 `nums[i] + 1` 的元素。

开始你拥有 `0` 个点数。返回你能通过这些操作获得的最大点数。

**示例 1：**

```
输入：nums = [3,4,2]
输出：6
解释：
删除 4 获得 4 个点数，因此 3 也被删除。
之后，删除 2 获得 2 个点数。总共获得 6 个点数。
```

**示例 2：**

```
输入：nums = [2,2,3,3,3,4]
输出：9
解释：
删除 3 获得 3 个点数，接着要删除两个 2 和 4 。
之后，再次删除 3 获得 3 个点数，再次删除 3 获得 3 个点数。
总共获得 9 个点数。
```

## 解题思路

**核心观察：**
类似于打家劫舍问题！

如果选择了数字 `x`，就不能选择 `x-1` 和 `x+1`。

**转化：**
1. 统计每个数字的总点数
2. 问题变成：从连续数字中选择，不能选相邻
3. 这就是打家劫舍问题

## Python 代码实现

### 方法一：动态规划（推荐）⭐

```python
from typing import List
from collections import Counter

class Solution:
    def deleteAndEarn(self, nums: List[int]) -> int:
        # 统计每个数字的总点数
        points = Counter(nums)
        
        if not points:
            return 0
        
        # 获取所有不同的数字并排序
        all_nums = sorted(points.keys())
        
        # DP：dp[i] 表示考虑前 i 个数字的最大点数
        prev = 0  # 不选当前数字
        curr = points[all_nums[0]]  # 选当前数字
        
        for i in range(1, len(all_nums)):
            num = all_nums[i]
            
            if num == all_nums[i - 1] + 1:
                # 连续，不能同时选
                prev, curr = curr, max(curr, prev + points[num])
            else:
                # 不连续，可以都选
                prev, curr = curr, curr + points[num]
        
        return curr
```

### 方法二：数组DP

```python
class Solution:
    def deleteAndEarn(self, nums: List[int]) -> int:
        max_val = max(nums)
        # points[i] 表示选择数字 i 能获得的总点数
        points = [0] * (max_val + 1)
        
        for num in nums:
            points[num] += num
        
        # 打家劫舍问题
        prev, curr = 0, 0
        
        for point in points:
            prev, curr = curr, max(curr, prev + point)
        
        return curr
```

## 复杂度分析

- **时间复杂度：** O(n + M)，M 是数字范围
- **空间复杂度：** O(M)

## 关键点总结

1. **问题转化**：转化为打家劫舍
2. **点数统计**：统计每个数字的总贡献
3. **DP状态**：选或不选当前数字

## 相关题目

- [198. House Robber](https://leetcode.com/problems/house-robber/)
- [213. House Robber II](https://leetcode.com/problems/house-robber-ii/)
- [337. House Robber III](https://leetcode.com/problems/house-robber-iii/)

## 学习建议

1. 先掌握打家劫舍系列
2. 理解问题的转化
3. 掌握DP的空间优化
