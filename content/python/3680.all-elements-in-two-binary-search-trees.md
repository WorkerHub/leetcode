---
title: 3680.all-elements-in-two-binary-search-trees
date: 2026-04-13 10:00:00
categories: medium
tags: [树, DFS, 排序, 二叉搜索树]
---

## 题目描述

给你两个二叉搜索树的根节点 `root1` 和 `root2`，返回包含两棵树中所有元素的、有序的列表。

**示例 1：**

```
输入：root1 = [2,1,4], root2 = [1,0,3]
输出：[0,1,1,2,3,4]
```

**示例 2：**

```
输入：root1 = [1,null,8], root2 = [8,1]
输出：[1,1,8,8]
```

**提示：**

- 每棵树的节点数在 `[0, 5000]`
- `-10^5 <= Node.val <= 10^5`

## 解题思路

中序遍历两棵 BST 得到两个有序列表，然后合并（归并排序的合并步骤）。

## Python 代码实现

### 方法一：中序遍历 + 归并（推荐）

```python
from typing import Optional, List

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def getAllElements(self, root1: Optional[TreeNode],
                       root2: Optional[TreeNode]) -> List[int]:
        def inorder(node):
            if not node:
                return []
            return inorder(node.left) + [node.val] + inorder(node.right)

        list1 = inorder(root1)
        list2 = inorder(root2)

        # 归并两个有序列表
        result = []
        i = j = 0
        while i < len(list1) and j < len(list2):
            if list1[i] <= list2[j]:
                result.append(list1[i])
                i += 1
            else:
                result.append(list2[j])
                j += 1
        result.extend(list1[i:])
        result.extend(list2[j:])
        return result
```

### 方法二：Python 简洁版

```python
from typing import Optional, List

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def getAllElements(self, root1: Optional[TreeNode],
                       root2: Optional[TreeNode]) -> List[int]:
        result = []

        def inorder(node):
            if node:
                inorder(node.left)
                result.append(node.val)
                inorder(node.right)

        inorder(root1)
        inorder(root2)
        return sorted(result)
```

## 复杂度分析

- 时间复杂度：O(m + n)（方法一），O((m+n) log(m+n))（方法二）
- 空间复杂度：O(m + n)

## 关键点总结

1. BST 中序遍历给出升序序列
2. 两个有序序列归并比排序更高效
3. 归并步骤：双指针，每次取较小值

## 易错点

1. 方法二直接 `sorted()` 失去了 BST 的有序性优势
2. 其中一棵树可能为空
3. 归并后需处理剩余元素

## 相关题目

- [88. 合并两个有序数组](https://leetcode.com/problems/merge-sorted-array/)
- [1305. 两棵二叉搜索树中的所有元素](https://leetcode.com/problems/all-elements-in-two-binary-search-trees/)
