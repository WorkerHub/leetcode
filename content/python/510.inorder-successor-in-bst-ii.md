---
title: 510.inorder-successor-in-bst-ii
date: 2026-04-09 10:09:00
categories: medium
tags: [树, 二叉搜索树, 深度优先搜索]
---

## 题目描述

给定一棵二叉搜索树和其中的一个节点 `node`，找到该节点在树中的中序后继。如果节点没有中序后继，请返回 `null`。

一个节点 `node` 的中序后继是键值大于 `node.val` 且键值最小的节点。

你可以直接访问节点，但无法直接访问树的根节点。每个节点都有其父节点的引用。节点定义如下：

```python
class Node:
    def __init__(self, val):
        self.val = val
        self.left = None
        self.right = None
        self.parent = None
```

**示例 1：**
```
输入：tree = [2,1,3], node = 1
输出：2
解释：1 的中序后继是 2。注意节点和返回值都是 Node 类型的。
```

**示例 2：**
```
输入：tree = [5,3,6,2,4,null,null,1], node = 6
输出：null
解释：该节点没有中序后继，因此返回 null。
```

**示例 3：**
```
输入：tree = [15,6,18,3,7,17,20,2,4,null,13,null,null,null,null,null,null,null,null,9], node = 15
输出：17
```

**提示：**
- 树中节点的数目在范围 `[1, 10^4]` 内
- `-10^5 <= Node.val <= 10^5`
- 树中所有节点的值都是唯一的

**进阶：** 你能否在不访问任何节点的值的情况下解决问题？

## 解题思路

这道题与 [285. Inorder Successor in BST](https://leetcode.com/problems/inorder-successor-in-bst/) 不同之处在于：
1. **没有给出根节点**，但每个节点有父节点引用
2. 需要直接从给定节点找中序后继

### 核心思想：
BST 的中序后继有两种情况：
1. **如果节点有右子树**：后继是右子树的最左节点
2. **如果节点无右子树**：向上找父节点，直到找到一个节点是其父节点的左孩子

### 算法流程：

**情况 1：有右子树**
```
node → 右子树的最左节点
```

**情况 2：无右子树**
```
向上找父节点，直到：
- 当前节点是父节点的左孩子（父节点就是后继）
- 或到达根节点（无后继）
```

## Python 代码实现

### 方法一：分类讨论（推荐）⭐

```python
class Node:
    def __init__(self, val):
        self.val = val
        self.left = None
        self.right = None
        self.parent = None

class Solution:
    def inorderSuccessor(self, node: 'Node') -> 'Node':
        """
        BST 中序后继
        时间复杂度：O(h)，h 是树的高度
        空间复杂度：O(1)
        """
        # 情况 1：如果有右子树
        if node.right:
            # 后继是右子树的最左节点
            successor = node.right
            while successor.left:
                successor = successor.left
            return successor
        
        # 情况 2：没有右子树
        # 向上找，直到找到一个节点是其父节点的左孩子
        while node.parent:
            # 如果当前节点是父节点的左孩子
            if node.parent.left == node:
                return node.parent
            # 否则继续向上
            node = node.parent
        
        # 到达根节点还没找到，说明没有后继
        return None
```

### 方法二：统一处理（更简洁）

```python
class Solution:
    def inorderSuccessor(self, node: 'Node') -> 'Node':
        """
        更简洁的实现
        时间复杂度：O(h)，空间复杂度：O(1)
        """
        # 有右子树：找右子树最小值
        if node.right:
            node = node.right
            while node.left:
                node = node.left
            return node
        
        # 无右子树：向上找第一个左转的祖先
        while node.parent and node == node.parent.right:
            node = node.parent
        
        return node.parent
```

### 方法三：递归实现

```python
class Solution:
    def inorderSuccessor(self, node: 'Node') -> 'Node':
        """
        递归实现
        时间复杂度：O(h)，空间复杂度：O(h)
        """
        # 情况 1：有右子树
        if node.right:
            return self.findMin(node.right)
        
        # 情况 2：无右子树，递归向上找
        return self.findSuccessorUp(node)
    
    def findMin(self, node):
        """找子树的最小节点"""
        while node.left:
            node = node.left
        return node
    
    def findSuccessorUp(self, node):
        """向上找后继"""
        if not node.parent:
            return None
        
        # 如果是父节点的左孩子，父节点就是后继
        if node.parent.left == node:
            return node.parent
        
        # 否则继续向上找
        return self.findSuccessorUp(node.parent)
```

### 方法四：进阶解法（不访问节点值）

```python
class Solution:
    def inorderSuccessor(self, node: 'Node') -> 'Node':
        """
        进阶：不访问节点值
        仅通过指针关系判断
        时间复杂度：O(h)，空间复杂度：O(1)
        """
        # 有右子树
        if node.right:
            # 找右子树最左节点
            result = node.right
            while result.left:
                result = result.left
            return result
        
        # 无右子树
        # 向上找到第一个"从左边上来"的祖先
        child = node
        parent = node.parent
        
        while parent:
            # 如果从左边上来，parent 就是后继
            if parent.left is child:
                return parent
            # 从右边上来，继续向上
            child = parent
            parent = parent.parent
        
        return None
```

## 复杂度分析

所有方法的复杂度相同：
- **时间复杂度**：O(h)，h 是树的高度
  - 最坏情况：从叶子节点到根节点
  - 平衡树：O(log n)
  - 链状树：O(n)
- **空间复杂度**：
  - 迭代：O(1)
  - 递归：O(h)

## 关键点总结

1. **两种情况**：有右子树 vs 无右子树
2. **有右子树**：后继是右子树的最小值（最左节点）
3. **无右子树**：向上找第一个"左转"的祖先
4. **终止条件**：到达根节点仍未找到 → 无后继
5. **不需要根节点**：利用 parent 指针即可

## 易错点

1. **忘记右子树情况**：有右子树时不需要向上找
2. **左右孩子判断**：要用 `==` 或 `is` 判断，不是比较值
3. **空指针检查**：访问 `.parent` 前要检查是否为 None
4. **返回值**：找不到后继时返回 None，不是 -1
5. **循环条件**：while 条件要正确，避免死循环

## 图解示例

```
示例：tree = [5,3,6,2,4,null,null,1], node = 3

        5
       / \
      3   6
     / \
    2   4
   /
  1

情况分析：
node = 3，有右子树 (4)
→ 后继 = 右子树的最左节点 = 4

---

node = 6，无右子树
→ 向上找：
  - 6 是 5 的右孩子，继续
  - 5 没有父节点
→ 后继 = None

---

node = 1，无右子树
→ 向上找：
  - 1 是 2 的左孩子
→ 后继 = 2

---

node = 4，无右子树
→ 向上找：
  - 4 是 3 的右孩子，继续
  - 3 是 5 的左孩子
→ 后继 = 5
```

## 与前一题的对比

| 特性 | 285 题 | 510 题 |
|------|--------|--------|
| 给定 | 根节点 + 目标节点 | 目标节点（有 parent） |
| 方法 | 从根向下搜索 | 从节点向上/向下 |
| 复杂度 | O(h) | O(h) |
| 难度 | Medium | Medium |

## 相关题目

- [285. Inorder Successor in BST](https://leetcode.com/problems/inorder-successor-in-bst/) - 有根节点版本
- [173. Binary Search Tree Iterator](https://leetcode.com/problems/binary-search-tree-iterator/) - BST 迭代器
- [94. Binary Tree Inorder Traversal](https://leetcode.com/problems/binary-tree-inorder-traversal/) - 中序遍历
- [230. Kth Smallest Element in a BST](https://leetcode.com/problems/kth-smallest-element-in-a-bst/) - 第 k 小元素
- [426. Convert Binary Search Tree to Sorted Doubly Linked List](https://leetcode.com/problems/convert-binary-search-tree-to-sorted-doubly-linked-list/) - BST 转双向链表

## 学习建议

**对于新手：**
1. 先理解中序遍历和 BST 的性质
2. 手动画图模拟几个例子
3. 理解为什么要分两种情况

**进阶学习：**
1. 对比有根节点和无根节点的两种解法
2. 理解为什么无右子树时要向上找"左转"
3. 尝试实现中序前驱（类似思路）

**实战技巧：**
- BST + 中序后继 → 考虑右子树和向上查找
- 有 parent 指针 → 可以向上遍历
- 注意空指针检查，避免空引用
