---
title: 649.dota2-senate
date: 2026-04-09 12:15:00
categories: medium
tags: [贪心, 队列, 字符串]
---

## 题目描述

Dota2 的世界里有两个阵营：`Radiant`（天辉）和 `Dire`（夜魇）。

Dota2 参议院由来自两派的参议员组成。现在参议院希望对一个 Dota2 游戏里的改变作出决定。对于这个改变，他们以一个基于轮次的投票过程进行。在每一轮中，每个参议员都可以行使以下两项权利中的一项：

1. **禁止一名参议员的权利**：参议员可以让另一位参议员在本轮和后续几轮中失去所有权利。
2. **宣布胜利**：如果参议员发现有权利投票的参议员都是同一阵营的，他可以宣布胜利并决定在游戏中的有关变化。

给你一个字符串 `senate` 代表每个参议员的阵营。字母 `'R'` 和 `'D'` 分别代表了 `Radiant`（天辉）和 `Dire`（夜魇）。然后，如果有 `n` 个参议员，给定字符串的大小将是 `n`。

以轮为基础的过程从给定顺序的第一个参议员开始到最后一个参议员结束。这一过程将持续到投票结束。所有失去权利的参议员将在过程中被跳过。

假设每个参议员都足够聪明，会为自己的政党做出最好的策略，你需要预测哪一方最终会宣布胜利并在 Dota2 游戏中决定改变。

**示例 1：**

```
输入：senate = "RD"
输出："Radiant"
解释：
第 1 轮时，第一个参议员来自 Radiant 阵营，他可以使用第一项权利让第二个参议员失去所有权利。
这一轮中只有一名拥有投票权的参议员，来自 Radiant 阵营，于是他可以宣布胜利。
```

**示例 2：**

```
输入：senate = "RDD"
输出："Dire"
```

## Python 代码实现

```python
from collections import deque

class Solution:
    def predictPartyVictory(self, senate: str) -> str:
        """
        贪心 + 队列
        
        时间复杂度：O(n)
        空间复杂度：O(n)
        """
        n = len(senate)
        radiant = deque()
        dire = deque()
        
        # 记录每个参议员的索引
        for i, s in enumerate(senate):
            if s == 'R':
                radiant.append(i)
            else:
                dire.append(i)
        
        # 模拟投票过程
        while radiant and dire:
            r_index = radiant.popleft()
            d_index = dire.popleft()
            
            # 谁的索引小谁先投票，禁掉对方
            if r_index < d_index:
                radiant.append(r_index + n)  # 下一轮
            else:
                dire.append(d_index + n)
        
        return "Radiant" if radiant else "Dire"
```

## 相关题目

- [134. Gas Station](https://leetcode.com/problems/gas-station/)
- [376. Wiggle Subsequence](https://leetcode.com/problems/wiggle-subsequence/)
