---
title: 721.accounts-merge
date: 2026-04-09 11:30:00
categories: medium
tags: [深度优先搜索, 广度优先搜索, 并查集, 数组, 字符串]
---

## 题目描述

给定一个列表 `accounts`，每个元素 `accounts[i]` 是一个字符串列表，其中第一个元素 `accounts[i][0]` 是名称，其余元素是邮箱地址。

现在，我们想合并这些账户。如果两个账户都有一些共同的邮箱地址，则两个账户必定属于同一个人。请注意，即使两个账户具有相同的名称，它们也可能属于不同的人，因为人们可能具有相同的名称。一个人最初可以拥有任意数量的账户，但其所有账户都具有相同的名称。

合并账户后，按以下格式返回账户：每个账户的第一个元素是名称，其余元素是 **按字符串 ASCII 顺序排列** 的邮箱地址。账户本身可以以 **任意顺序** 返回。

**示例 1：**

```
输入：accounts = [["John","johnsmith@mail.com","john_newyork@mail.com"],["John","johnsmith@mail.com","john00@mail.com"],["Mary","mary@mail.com"],["John","johnnybravo@mail.com"]]
输出：[["John","john00@mail.com","john_newyork@mail.com","johnsmith@mail.com"],["Mary","mary@mail.com"],["John","johnnybravo@mail.com"]]
```

## 解题思路

**方法选择：并查集**

将邮箱作为节点，有共同邮箱的账户连接在一起。

**算法流程：**
1. 建立邮箱到账户索引的映射
2. 使用并查集合并有共同邮箱的账户
3. 按并查集的根节点分组邮箱
4. 排序并输出结果

## Python 代码实现

### 方法一：并查集（推荐）⭐

```python
from typing import List
from collections import defaultdict

class UnionFind:
    def __init__(self, n):
        self.parent = list(range(n))
    
    def find(self, x):
        if self.parent[x] != x:
            self.parent[x] = self.find(self.parent[x])
        return self.parent[x]
    
    def union(self, x, y):
        self.parent[self.find(x)] = self.find(y)

class Solution:
    def accountsMerge(self, accounts: List[List[str]]) -> List[List[str]]:
        uf = UnionFind(len(accounts))
        email_to_id = {}
        
        # 建立邮箱到账户的映射并合并
        for i, account in enumerate(accounts):
            for email in account[1:]:
                if email in email_to_id:
                    uf.union(i, email_to_id[email])
                else:
                    email_to_id[email] = i
        
        # 按根节点分组
        groups = defaultdict(set)
        for email, idx in email_to_id.items():
            root = uf.find(idx)
            groups[root].add(email)
        
        # 构建结果
        result = []
        for idx, emails in groups.items():
            name = accounts[idx][0]
            result.append([name] + sorted(emails))
        
        return result
```

### 方法二：DFS

```python
class Solution:
    def accountsMerge(self, accounts: List[List[str]]) -> List[List[str]]:
        from collections import defaultdict
        
        # 构建图：邮箱 -> 邮箱
        graph = defaultdict(set)
        email_to_name = {}
        
        for account in accounts:
            name = account[0]
            first_email = account[1]
            
            for email in account[1:]:
                graph[first_email].add(email)
                graph[email].add(first_email)
                email_to_name[email] = name
        
        # DFS 收集连通的邮箱
        visited = set()
        result = []
        
        def dfs(email, emails):
            if email in visited:
                return
            visited.add(email)
            emails.add(email)
            for neighbor in graph[email]:
                dfs(neighbor, emails)
        
        for email in graph:
            if email not in visited:
                emails = set()
                dfs(email, emails)
                result.append([email_to_name[email]] + sorted(emails))
        
        return result
```

## 复杂度分析

- **时间复杂度：** O(N × K × α(N))，N 是账户数，K 是平均邮箱数，α 是并查集的反阿克曼函数
- **空间复杂度：** O(N × K)

## 关键点总结

1. **并查集应用**：合并具有共同元素的集合
2. **邮箱映射**：邮箱到账户索引的映射
3. **排序输出**：邮箱按字典序排列

## 相关题目

- [547. Number of Provinces](https://leetcode.com/problems/number-of-provinces/)
- [684. Redundant Connection](https://leetcode.com/problems/redundant-connection/)
- [737. Sentence Similarity II](https://leetcode.com/problems/sentence-similarity-ii/)

## 学习建议

1. 掌握并查集的基本操作
2. 理解图的连通性问题
3. 注意路径压缩优化
