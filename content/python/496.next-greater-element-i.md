---
title: 496.next-greater-element-i
date: 2026-04-09 11:15:00
categories: easy
tags: [栈, 数组, 哈希表, 单调栈]
---

## 题目描述

nums1 中数字 x 的下一个更大元素是指 x 在 nums2 中对应位置右侧的第一个比 x 大的元素。

给你两个没有重复元素的数组 nums1 和 nums2，下标从 0 开始计数，其中nums1 是 nums2 的子集。

对于每个 0 <= i < nums1.length，找出满足 nums1[i] == nums2[j] 的下标 j，并且在 nums2 确定 nums2[j] 的下一个更大元素。如果不存在下一个更大元素，那么本次查询的答案是 -1。

返回一个长度为 nums1.length 的数组 ans 作为答案，满足 ans[i] 是如上所述的下一个更大元素。

**示例 1：**
```
输入：nums1 = [4,1,2], nums2 = [1,3,4,2]
输出：[-1,3,-1]
解释：nums1 中每个值的下一个更大元素如下所述：
- 4 ，用加粗斜体标识，nums2 = [1,3,4,2]。不存在下一个更大元素，所以答案是 -1。
- 1 ，用加粗斜体标识，nums2 = [1,3,4,2]。下一个更大元素是 3。
- 2 ，用加粗斜体标识，nums2 = [1,3,4,2]。不存在下一个更大元素，所以答案是 -1。
```

**示例 2：**
```
输入：nums1 = [2,4], nums2 = [1,2,3,4]
输出：[3,-1]
解释：nums1 中每个值的下一个更大元素如下所述：
- 2 ，用加粗斜体标识，nums2 = [1,2,3,4]。下一个更大元素是 3。
- 4 ，用加粗斜体标识，nums2 = [1,2,3,4]。不存在下一个更大元素，所以答案是 -1。
```

**提示：**
- 1 <= nums1.length <= nums2.length <= 1000
- 0 <= nums1[i], nums2[i] <= 10^4
- nums1和nums2中所有整数互不相同
- nums1 中的所有整数同样出现在 nums2 中

## 解题思路

这是一道经典的单调栈问题。

### 核心思想

1. **单调栈**：
   - 维护一个从栈底到栈顶递减的栈
   - 遍历 nums2 时，如果当前元素大于栈顶元素
   - 说明找到了栈顶元素的下一个更大元素

2. **哈希表映射**：
   - 使用哈希表存储每个元素的下一个更大元素
   - 最后根据 nums1 查询结果

3. **算法流程**：
   - 遍历 nums2，使用单调栈找到每个元素的下一个更大元素
   - 存入哈希表
   - 遍历 nums1，从哈希表查询结果

## Python 代码实现

### 方法一：单调栈 + 哈希表（推荐）

```python
from typing import List

class Solution:
    def nextGreaterElement(self, nums1: List[int], nums2: List[int]) -> List[int]:
        """
        单调栈 + 哈希表
        
        时间复杂度：O(m + n)
        空间复杂度：O(n)
        """
        # 哈希表存储每个元素的下一个更大元素
        next_greater = {}
        stack = []  # 单调递减栈
        
        # 遍历 nums2
        for num in nums2:
            # 当前元素比栈顶大，找到了栈顶的下一个更大元素
            while stack and stack[-1] < num:
                next_greater[stack.pop()] = num
            
            # 当前元素入栈
            stack.append(num)
        
        # 栈中剩余元素没有下一个更大元素
        while stack:
            next_greater[stack.pop()] = -1
        
        # 查询 nums1 中每个元素的答案
        return [next_greater[num] for num in nums1]
```

### 方法二：简化版（默认 -1）

```python
class Solution:
    def nextGreaterElement(self, nums1: List[int], nums2: List[int]) -> List[int]:
        """
        简化版，利用字典的 get 方法
        
        时间复杂度：O(m + n)
        空间复杂度：O(n)
        """
        next_greater = {}
        stack = []
        
        for num in nums2:
            while stack and stack[-1] < num:
                next_greater[stack.pop()] = num
            stack.append(num)
        
        # 使用 get 方法，不存在则返回 -1
        return [next_greater.get(num, -1) for num in nums1]
```

### 方法三：暴力解法（仅供对比）

```python
class Solution:
    def nextGreaterElement(self, nums1: List[int], nums2: List[int]) -> List[int]:
        """
        暴力解法
        
        时间复杂度：O(m * n)
        空间复杂度：O(1)
        """
        result = []
        
        for num in nums1:
            # 在 nums2 中找到 num 的位置
            found = False
            next_val = -1
            
            for i in range(len(nums2)):
                if found and nums2[i] > num:
                    next_val = nums2[i]
                    break
                if nums2[i] == num:
                    found = True
            
            result.append(next_val)
        
        return result
```

### 方法四：使用索引优化

```python
class Solution:
    def nextGreaterElement(self, nums1: List[int], nums2: List[int]) -> List[int]:
        """
        使用索引映射优化查找
        
        时间复杂度：O(m + n)
        空间复杂度：O(n)
        """
        # 建立值到索引的映射
        index_map = {num: i for i, num in enumerate(nums2)}
        next_greater = {}
        stack = []
        
        for num in nums2:
            while stack and stack[-1] < num:
                next_greater[stack.pop()] = num
            stack.append(num)
        
        return [next_greater.get(num, -1) for num in nums1]
```

## 复杂度分析

**方法一、二、四：单调栈**
- **时间复杂度**：O(m + n)
  - 遍历 nums2：O(n)，每个元素最多入栈出栈一次
  - 遍历 nums1：O(m)
  - 总复杂度：O(m + n)
  
- **空间复杂度**：O(n)
  - 栈的空间：O(n)
  - 哈希表：O(n)

**方法三：暴力**
- **时间复杂度**：O(m × n)
  - 对每个 nums1 的元素，遍历 nums2
  
- **空间复杂度**：O(1)
  - 不包括输出数组

## 关键点总结

1. **单调栈思想**：维护递减栈找下一个更大元素
2. **栈的性质**：栈顶是最近的未匹配元素
3. **哈希表映射**：预处理结果，快速查询
4. **出栈时机**：当前元素大于栈顶时出栈
5. **默认值处理**：栈中剩余元素没有下一个更大元素

## 易错点

1. **栈的单调性**
   - 应该是递减栈，不是递增栈
   - 栈底到栈顶：大 -> 小

2. **出栈条件**
   - while stack and stack[-1] < num
   - 不是 <=，是严格小于

3. **剩余元素处理**
   - 栈中剩余元素需要设为 -1
   - 或使用 get(num, -1) 默认值

4. **nums1 和 nums2 的关系**
   - nums1 是 nums2 的子集
   - 只需要查询 nums1 中的元素

5. **空栈检查**
   - while 循环要检查 stack 非空
   - 避免空栈 pop

## 相关题目

- [503. Next Greater Element II](https://leetcode.com/problems/next-greater-element-ii/) - 循环数组
- [556. Next Greater Element III](https://leetcode.com/problems/next-greater-element-iii/) - 下一个排列
- [739. Daily Temperatures](https://leetcode.com/problems/daily-temperatures/) - 类似问题
- [901. Online Stock Span](https://leetcode.com/problems/online-stock-span/) - 股票跨度

## 学习建议

1. **掌握单调栈模板**：
   - 递增栈 vs 递减栈
   - 找下一个更大 vs 更小
   - 栈中存值 vs 存索引

2. **单调栈的应用场景**：
   - 下一个更大/更小元素
   - 前一个更大/更小元素
   - 区间最值问题

3. **理解栈的作用**：
   - 记录待匹配的元素
   - 利用单调性快速匹配
   - O(n) 解决暴力 O(n²) 问题

4. **哈希表优化**：
   - 预处理 + 查询
   - 空间换时间
   - 适合需要多次查询的场景

5. **变种练习**：
   - 循环数组怎么处理？
   - 找前一个更大元素呢？
   - 如果要找距离呢？

6. **调试技巧**：
   - 打印栈的变化过程
   - 跟踪每个元素的入栈出栈
   - 验证单调性是否保持
