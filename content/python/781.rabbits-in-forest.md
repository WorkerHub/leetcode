---
title: 781.rabbits-in-forest
date: 2026-04-09 11:15:00
categories: medium
tags: [贪心, 数组, 哈希表, 数学]
---

## 题目描述

森林中有未知数量的兔子。提问其中若干只兔子 "还有多少只兔子与你（指被提问的兔子）颜色相同？"将答案收集到一个整数数组 `answers` 中，其中 `answers[i]` 是第 `i` 只兔子的回答。

给你数组 `answers`，返回森林中兔子的最少数量。

**示例 1:**
```
输入: answers = [1,1,2]
输出: 5
解释:
两只回答1的兔子可能颜色相同，还有一只同色兔子未被询问。
回答2的兔子必然有另外2只同色兔子。
总共至少5只兔子。
```

**示例 2:**
```
输入: answers = [10,10,10]
输出: 11
```

## 解题思路

对于回答 `x` 的兔子，一组最多有 `x+1` 只。统计每个答案的数量，计算需要多少组。

## Python 代码实现

```python
from typing import List
from collections import Counter

class Solution:
    def numRabbits(self, answers: List[int]) -> int:
        count = Counter(answers)
        result = 0
        
        for answer, freq in count.items():
            # 每组最多 answer+1 只兔子
            group_size = answer + 1
            # 需要的组数（向上取整）
            groups = (freq + group_size - 1) // group_size
            result += groups * group_size
        
        return result
```

## 复杂度分析

- **时间复杂度：** O(n)
- **空间复杂度：** O(n)

## 关键点总结

1. **分组策略**
2. **向上取整**
3. **贪心思想**

## 相关题目

- [448. Find All Numbers Disappeared in an Array](https://leetcode.com/problems/find-all-numbers-disappeared-in-an-array/)
