---
title: 1200.minimum-absolute-difference
date: 2026-04-12 15:30:00
categories: easy
tags: ['数组', '排序']
---

## 题目描述

给定一个整数数组 `arr`，找出其中所有相邻两个元素的差的绝对值的最小值，并返回所有差等于这个最小值的数对。

数对格式为 `[a, b]`，其中 `a < b`，按照升序排序返回。

**示例 1:**

```
输入: arr = [4,2,1,3]
输出: [[1,2],[2,3],[3,4]]
```

**示例 2:**

```
输入: arr = [1,3,6,10,15]
输出: [[1,3]]
```

**提示:**

- `2 <= arr.length <= 10^5`
- `-10^6 <= arr[i] <= 10^6`

## 解题思路

关键是先排序，然后找相邻元素的最小差值。

解题步骤：
1. 将数组排序
2. 遍历找到最小差值
3. 收集所有差值等于最小值的数对

## Python 代码实现

### 方法一：一次遍历（推荐）

```python
from typing import List

class Solution:
    def minimumAbsDifference(self, arr: List[int]) -> List[List[int]]:
        arr.sort()
        result = []
        min_diff = float('inf')
        
        for i in range(1, len(arr)):
            diff = arr[i] - arr[i - 1]
            
            if diff < min_diff:
                min_diff = diff
                result = [[arr[i - 1], arr[i]]]
            elif diff == min_diff:
                result.append([arr[i - 1], arr[i]])
        
        return result
```

### 方法二：两次遍历

```python
class Solution:
    def minimumAbsDifference(self, arr: List[int]) -> List[List[int]]:
        arr.sort()
        
        min_diff = min(arr[i] - arr[i - 1] for i in range(1, len(arr)))
        
        result = []
        for i in range(1, len(arr)):
            if arr[i] - arr[i - 1] == min_diff:
                result.append([arr[i - 1], arr[i]])
        
        return result
```

## 复杂度分析

- 时间复杂度：O(n log n)，排序的时间
- 空间复杂度：O(log n)，排序的空间

## 关键点总结

1. 排序后最小差值一定在相邻元素间
2. 一次遍历可同时找最小值和构建结果
3. 排序后自然满足 `a < b`

## 易错点

1. 忘记排序就计算差值
2. 使用绝对值（排序后不需要）

## 相关题目

- [2616. 最小化数对的最大差值](https://leetcode.com/problems/minimize-the-maximum-difference-of-pairs/)

## 学习建议

理解排序在差值问题中的作用，掌握一次遍历的优化技巧。
