---
title: 773.sliding-puzzle
date: 2026-04-09 10:40:00
categories: hard
tags: [广度优先搜索, 数组, 矩阵]
---

## 题目描述

在一个 `2 x 3` 的板上有 5 块砖瓦，用数字 `1~5` 来表示, 以及一块空缺用 `0` 来表示。一次移动定义为选择 `0` 与一个相邻的数字（上下左右）进行交换。

最终当板 `board` 的结果是 `[[1,2,3],[4,5,0]]` 时，谜板被解开。

给出一个谜板的初始状态，返回最少可以通过多少次移动解开谜板，如果不能解开谜板，则返回 `-1`。

**示例 1:**
```
输入：board = [[1,2,3],[4,0,5]]
输出：1
解释：交换 0 和 5，一次移动完成
```

**示例 2:**
```
输入：board = [[1,2,3],[5,4,0]]
输出：-1
解释：没有办法完成谜板
```

## 解题思路

使用 BFS 搜索所有可能的状态，直到找到目标状态。

## Python 代码实现

```python
from typing import List
from collections import deque

class Solution:
    def slidingPuzzle(self, board: List[List[int]]) -> int:
        # 将二维数组转为字符串
        start = ''.join(str(num) for row in board for num in row)
        target = "123450"
        
        # BFS
        queue = deque([(start, start.index('0'), 0)])
        visited = {start}
        
        # 每个位置可以交换的相邻位置
        neighbors = {
            0: [1, 3],
            1: [0, 2, 4],
            2: [1, 5],
            3: [0, 4],
            4: [1, 3, 5],
            5: [2, 4]
        }
        
        while queue:
            state, zero_pos, steps = queue.popleft()
            
            if state == target:
                return steps
            
            # 尝试所有可能的移动
            for next_pos in neighbors[zero_pos]:
                # 交换
                state_list = list(state)
                state_list[zero_pos], state_list[next_pos] = state_list[next_pos], state_list[zero_pos]
                new_state = ''.join(state_list)
                
                if new_state not in visited:
                    visited.add(new_state)
                    queue.append((new_state, next_pos, steps + 1))
        
        return -1
```

## 复杂度分析

- **时间复杂度：** O((m*n)! * m*n)
- **空间复杂度：** O((m*n)!)

## 关键点总结

1. **状态表示：** 用字符串表示状态
2. **BFS 搜索：** 寻找最短路径
3. **邻居关系：** 预先定义每个位置的邻居

## 相关题目

- [752. Open the Lock](https://leetcode.com/problems/open-the-lock/)
- [127. Word Ladder](https://leetcode.com/problems/word-ladder/)
