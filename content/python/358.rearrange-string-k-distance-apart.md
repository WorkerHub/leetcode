---
title: 358.rearrange-string-k-distance-apart
date: 2026-04-09 11:25:00
categories: hard
tags: [贪心, 哈希表, 字符串, 计数, 排序, 堆]
---

## 题目描述

给你一个字符串 `s` 和一个整数 `k`，请你重新排列字符串，使得重排后的字符串中相同字母的位置间隔距离至少为 `k`。

所有可能的重新排列方案需要满足前面提到的距离要求。如果不可能，则返回一个空字符串 `""`。

**示例 1：**
```
输入：s = "aabbcc", k = 3
输出："abcabc"
解释：相同的字母在新的字符串中间隔至少 3 个单位距离。
```

**示例 2：**
```
输入：s = "aaabc", k = 3
输出：""
解释：无法找到可行的重新排列方案。
```

**示例 3：**
```
输入：s = "aaadbbcc", k = 2
输出："abacabcd"
解释：相同的字母在新的字符串中间隔至少 2 个单位距离。
```

**提示：**
- `1 <= s.length <= 3 * 10^5`
- `s` 仅由小写英文字母组成
- `0 <= k <= s.length`

## 解题思路

这是一个贪心 + 堆的问题。

**核心思想：**
1. 统计每个字符的频率
2. 使用最大堆，每次选择频率最高的字符
3. 放置后暂存到等待队列，等待 k 个位置后再放回堆

## Python 代码实现

```python
from collections import Counter
import heapq

class Solution:
    def rearrangeString(self, s: str, k: int) -> str:
        if k == 0:
            return s
        
        # 统计频率
        counter = Counter(s)
        
        # 最大堆（频率，字符）
        heap = [(-freq, char) for char, freq in counter.items()]
        heapq.heapify(heap)
        
        result = []
        waiting = []  # (可用位置, 频率, 字符)
        
        while heap or waiting:
            # 将等待队列中可用的字符放回堆
            while waiting and waiting[0][0] <= len(result):
                _, freq, char = heapq.heappop(waiting)
                heapq.heappush(heap, (freq, char))
            
            # 如果堆为空，无法继续
            if not heap:
                return ""
            
            # 取出频率最高的字符
            freq, char = heapq.heappop(heap)
            result.append(char)
            
            # 如果还有剩余，放入等待队列
            if freq + 1 < 0:  # freq 是负数
                heapq.heappush(waiting, (len(result) + k, freq + 1, char))
        
        return ''.join(result)
```

**时间复杂度：** O(n log n)
**空间复杂度：** O(n)

## 关键点总结

1. **贪心策略**：优先使用频率最高的字符
2. **等待队列**：维护暂时不可用的字符
3. **可行性判断**：堆空但还有字符未放置

## 相关题目

- [621. Task Scheduler](https://leetcode.com/problems/task-scheduler/) - 任务调度器
- [767. Reorganize String](https://leetcode.com/problems/reorganize-string/) - 重构字符串

## 学习建议

**新手建议：**
1. 先理解贪心策略
2. 掌握堆的使用
3. 注意边界条件
