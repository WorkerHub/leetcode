---
title: 350.intersection-of-two-arrays-ii
date: 2026-04-09 10:45:00
categories: easy
tags: [数组, 哈希表, 双指针, 二分查找, 排序]
---

## 题目描述

给你两个整数数组 `nums1` 和 `nums2`，请你以数组形式返回两数组的交集。返回结果中每个元素出现的次数，应与元素在两个数组中都出现的次数一致（如果出现次数不一致，则考虑取较小值）。可以不考虑输出结果的顺序。

**示例 1：**
```
输入：nums1 = [1,2,2,1], nums2 = [2,2]
输出：[2,2]
```

**示例 2：**
```
输入：nums1 = [4,9,5], nums2 = [9,4,9,8,4]
输出：[4,9]
```

**提示：**
- `1 <= nums1.length, nums2.length <= 1000`
- `0 <= nums1[i], nums2[i] <= 1000`

**进阶：**
- 如果给定的数组已经排好序呢？你将如何优化你的算法？
- 如果 `nums1` 的大小比 `nums2` 小，哪种方法更优？
- 如果 `nums2` 的元素存储在磁盘上，内存是有限的，并且你不能一次加载所有的元素到内存中，你该怎么办？

## 解题思路

这道题与第 349 题的区别是：需要保留重复元素。

**核心思想：**
- 统计每个元素的出现次数
- 取两个数组中较小的出现次数

**多种解法：**
1. 哈希表计数
2. 排序 + 双指针
3. Counter（Python 特色）

## Python 代码实现

### 方法一：哈希表计数（推荐）⭐

```python
from typing import List
from collections import defaultdict

class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        # 统计 nums1 中每个元素的出现次数
        count = defaultdict(int)
        for num in nums1:
            count[num] += 1
        
        result = []
        
        # 遍历 nums2
        for num in nums2:
            if count[num] > 0:
                result.append(num)
                count[num] -= 1
        
        return result
```

**时间复杂度：** O(m + n)
**空间复杂度：** O(min(m, n))

### 方法二：Counter（最简洁）⭐

```python
from collections import Counter

class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        count1 = Counter(nums1)
        count2 = Counter(nums2)
        
        # Counter 的 & 运算取最小值
        intersection = count1 & count2
        
        # 展开为列表
        result = []
        for num, freq in intersection.items():
            result.extend([num] * freq)
        
        return result
```

**一行版本：**
```python
class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        c1, c2 = Counter(nums1), Counter(nums2)
        return list((c1 & c2).elements())
```

**时间复杂度：** O(m + n)
**空间复杂度：** O(m + n)

### 方法三：排序 + 双指针

```python
class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        # 排序
        nums1.sort()
        nums2.sort()
        
        result = []
        i, j = 0, 0
        
        while i < len(nums1) and j < len(nums2):
            if nums1[i] < nums2[j]:
                i += 1
            elif nums1[i] > nums2[j]:
                j += 1
            else:
                # 找到交集元素
                result.append(nums1[i])
                i += 1
                j += 1
        
        return result
```

**时间复杂度：** O(m log m + n log n)
**空间复杂度：** O(1)，不计结果数组

**适用场景：** 数组已排序，或空间受限

## 图解示例

```
nums1 = [1,2,2,1], nums2 = [2,2]

方法一：哈希表
count = {1: 2, 2: 2}

遍历 nums2:
num = 2, count[2] = 2 > 0 → result = [2], count[2] = 1
num = 2, count[2] = 1 > 0 → result = [2,2], count[2] = 0

方法三：双指针
排序后：
nums1 = [1,1,2,2]
nums2 = [2,2]

i=0, j=0: 1 < 2, i++
i=1, j=0: 1 < 2, i++
i=2, j=0: 2 == 2, result=[2], i++, j++
i=3, j=1: 2 == 2, result=[2,2], i++, j++
```

## 关键点总结

1. **计数**：使用哈希表或 Counter
2. **取最小值**：`count1 & count2`
3. **双指针**：需要排序
4. **空间优化**：用较小数组建哈希表

## 易错点

1. **没有考虑重复次数**
   ```python
   # 错误：只记录是否存在
   set1 = set(nums1)
   
   # 正确：记录次数
   count = Counter(nums1)
   ```

2. **忘记减少计数**
   ```python
   # 错误：
   if count[num] > 0:
       result.append(num)
   
   # 正确：
   if count[num] > 0:
       result.append(num)
       count[num] -= 1
   ```

## 进阶问题解答

**1. 数组已排序？**
使用双指针，O(m + n) 时间，O(1) 空间

**2. nums1 比 nums2 小？**
用 nums1 建哈希表，遍历 nums2

**3. nums2 在磁盘上？**
- 用 nums1 建哈希表
- 分块读取 nums2
- 每块与哈希表比对

## 相关题目

- [349. Intersection of Two Arrays](https://leetcode.com/problems/intersection-of-two-arrays/) - 不保留重复
- [1002. Find Common Characters](https://leetcode.com/problems/find-common-characters/) - 多个字符串的交集

## 学习建议

**新手建议：**
1. 理解哈希表计数的应用
2. 掌握 Counter 的用法
3. 学习双指针在有序数组中的应用

**进阶方向：**
1. 理解不同场景下的最优解
2. 掌握空间优化技巧
3. 学习外部排序和分块处理
