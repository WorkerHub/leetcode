---
title: 26.remove-duplicates-from-sorted-array
date: 2025-01-13 15:28:55
categories: easy
tags: [数组, 双指针]
---

## 题目描述

给你一个非递减顺序排列的数组 `nums`，请你原地删除重复出现的元素，使每个元素只出现一次，返回删除后数组的新长度。元素的相对顺序应该保持一致。然后返回 `nums` 中唯一元素的个数。

考虑 `nums` 的唯一元素的数量为 `k`，你需要做以下事情确保你的题解可以被通过：

- 更改数组 `nums`，使 `nums` 的前 `k` 个元素包含唯一元素，并按照它们最初在 `nums` 中出现的顺序排列。`nums` 的其余元素与 `nums` 的大小不重要。
- 返回 `k`。

**示例 1：**
```
输入：nums = [1,1,2]
输出：2, nums = [1,2,_]
解释：函数应该返回新的长度 2，并且原数组 nums 的前两个元素被修改为 1, 2。不需要考虑数组中超出新长度后面的元素。
```

**示例 2：**
```
输入：nums = [0,0,1,1,1,2,2,3,3,4]
输出：5, nums = [0,1,2,3,4,_,_,_,_,_]
解释：函数应该返回新的长度 5，并且原数组 nums 的前五个元素被修改为 0, 1, 2, 3, 4。不需要考虑数组中超出新长度后面的元素。
```

## 解题思路

### 核心思想：双指针（快慢指针）

由于数组已经排序，重复的元素一定相邻。我们可以使用双指针：
- **慢指针**（`slow`）：指向去重后数组的最后一个元素
- **快指针**（`fast`）：遍历整个数组

**算法流程：**
1. 慢指针 `slow` 从 0 开始
2. 快指针 `fast` 从 1 开始遍历
3. 如果 `nums[fast] != nums[slow]`，说明遇到新元素：
   - `slow` 前进一步
   - 将 `nums[fast]` 复制到 `nums[slow]`
4. 返回 `slow + 1`（即新数组的长度）

## Python 代码实现

### 方法一：双指针（标准解法）

```python
class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        if not nums:
            return 0
        
        slow = 0  # 慢指针
        
        # 快指针遍历数组
        for fast in range(1, len(nums)):
            # 发现新元素
            if nums[fast] != nums[slow]:
                slow += 1
                nums[slow] = nums[fast]
        
        return slow + 1
```

### 方法二：双指针（先移动后赋值）

```python
class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        if not nums:
            return 0
        
        slow = 1  # 慢指针从1开始
        
        for fast in range(1, len(nums)):
            if nums[fast] != nums[fast - 1]:
                nums[slow] = nums[fast]
                slow += 1
        
        return slow
```

### 方法三：更简洁的写法

```python
class Solution:
    def removeDuplicates(self, nums: List[int]) -> int:
        slow = 0
        for fast in range(len(nums)):
            if fast == 0 or nums[fast] != nums[fast - 1]:
                nums[slow] = nums[fast]
                slow += 1
        return slow
```

## 复杂度分析

- **时间复杂度**：O(n)，只需遍历一次数组
- **空间复杂度**：O(1)，原地修改，只使用常量额外空间

## 图解示例

以 `nums = [0,0,1,1,1,2,2,3,3,4]` 为例：

```
初始状态：
nums = [0,0,1,1,1,2,2,3,3,4]
       ↑
     slow=0

fast=1: nums[1]=0 == nums[0]=0，跳过
fast=2: nums[2]=1 != nums[0]=0
  slow++, nums[1] = nums[2]
  nums = [0,1,1,1,1,2,2,3,3,4]
           ↑
         slow=1

fast=3: nums[3]=1 == nums[1]=1，跳过
fast=4: nums[4]=1 == nums[1]=1，跳过
fast=5: nums[5]=2 != nums[1]=1
  slow++, nums[2] = nums[5]
  nums = [0,1,2,1,1,2,2,3,3,4]
             ↑
           slow=2

fast=6: nums[6]=2 == nums[2]=2，跳过
fast=7: nums[7]=3 != nums[2]=2
  slow++, nums[3] = nums[7]
  nums = [0,1,2,3,1,2,2,3,3,4]
               ↑
             slow=3

fast=8: nums[8]=3 == nums[3]=3，跳过
fast=9: nums[9]=4 != nums[3]=3
  slow++, nums[4] = nums[9]
  nums = [0,1,2,3,4,2,2,3,3,4]
                 ↑
               slow=4

返回 slow + 1 = 5
```

## 关键点总结

1. **原地修改**：不能使用额外数组，要在原数组上修改
2. **已排序**：重复元素一定相邻，这是使用双指针的前提
3. **慢指针含义**：指向去重后数组的最后一个位置
4. **快指针含义**：遍历原数组，寻找新元素
5. **返回值**：`slow + 1` 是新数组的长度

## 易错点

1. **边界条件**：
   - 空数组要特殊处理（虽然题目说了数组至少有一个元素）
   - 只有一个元素的数组直接返回 1

2. **返回值**：
   - 返回的是长度 `slow + 1`，不是索引 `slow`

3. **比较对象**：
   - 方法一：比较 `nums[fast]` 和 `nums[slow]`
   - 方法二：比较 `nums[fast]` 和 `nums[fast-1]`

## 变体题目

这道题有几个变体：
- **保留 k 个重复元素**：将比较条件改为 `nums[fast] != nums[slow - k + 1]`
- **80. 删除有序数组中的重复项 II**：每个元素最多出现两次

## 相关题目

- [27. 移除元素](https://leetcode.com/problems/remove-element/)
- [80. 删除有序数组中的重复项 II](https://leetcode.com/problems/remove-duplicates-from-sorted-array-ii/)
- [283. 移动零](https://leetcode.com/problems/move-zeroes/)

## 学习建议

这道题是双指针（快慢指针）的经典应用：
- 快指针负责遍历
- 慢指针负责维护结果
- 非常适合作为双指针入门题

建议理解后，尝试不看代码自己实现一遍。
