---
title: 102.binary-tree-level-order-traversal
date: 2026-04-09 00:00:00
categories: medium
tags: [树, 广度优先搜索, 二叉树]
---

## 题目描述

给你二叉树的根节点 `root`，返回其节点值的**层序遍历**。（即逐层地，从左到右访问所有节点）。

**示例 1：**
```
    3
   / \
  9  20
    /  \
   15   7
```
输入：root = [3,9,20,null,null,15,7]
输出：[[3],[9,20],[15,7]]

**示例 2：**
输入：root = [1]
输出：[[1]]

**示例 3：**
输入：root = []
输出：[]

**提示：**
- 树中节点数目在范围 [0, 2000] 内
- -1000 <= Node.val <= 1000

## 解题思路

层序遍历就是**广度优先搜索（BFS）**，使用队列实现。

**核心思想：**
1. 使用队列存储每一层的节点
2. 每次处理一层：记录当前层节点个数，依次出队并访问
3. 将下一层的节点（左右孩子）加入队列
4. 重复直到队列为空

**也可以使用递归（DFS）+ 层级标记实现**。

## Python 代码实现

### 方法一：BFS（队列迭代）⭐

```python
from typing import Optional, List
from collections import deque

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        """层序遍历 - BFS 队列实现"""
        if not root:
            return []
        
        result = []
        queue = deque([root])
        
        while queue:
            level_size = len(queue)  # 当前层的节点数
            level_vals = []
            
            # 处理当前层的所有节点
            for _ in range(level_size):
                node = queue.popleft()
                level_vals.append(node.val)
                
                # 将下一层节点加入队列
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
            
            result.append(level_vals)
        
        return result
```

### 方法二：DFS（递归 + 层级标记）

```python
from typing import Optional, List

class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        """层序遍历 - DFS 递归实现"""
        result = []
        
        def dfs(node: Optional[TreeNode], level: int):
            if not node:
                return
            
            # 如果当前层还没有列表，创建一个
            if len(result) == level:
                result.append([])
            
            # 将当前节点值加入对应层
            result[level].append(node.val)
            
            # 递归处理左右子树
            dfs(node.left, level + 1)
            dfs(node.right, level + 1)
        
        dfs(root, 0)
        return result
```

### 方法三：简化版 BFS（Python 特性）

```python
from typing import Optional, List

class Solution:
    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:
        """简化的 BFS 实现"""
        if not root:
            return []
        
        result = []
        current_level = [root]
        
        while current_level:
            result.append([node.val for node in current_level])
            # 生成下一层节点列表
            next_level = []
            for node in current_level:
                if node.left:
                    next_level.append(node.left)
                if node.right:
                    next_level.append(node.right)
            current_level = next_level
        
        return result
```

## 复杂度分析

**所有方法：**
- 时间复杂度：O(n)，其中 n 是树中节点总数，每个节点访问一次
- 空间复杂度：O(n)
  - BFS：队列最多存储一层的节点，最坏情况下（完全二叉树最后一层）为 O(n/2) = O(n)
  - DFS：递归调用栈深度为树的高度 O(h)，加上结果数组 O(n)，总体 O(n)

## 关键点总结

1. **BFS 核心**：队列 + 按层处理（记录当前层节点数）
2. **层分隔技巧**：通过 `len(queue)` 确定当前层的节点数
3. **DFS 实现**：用 level 参数标记深度，按深度分组
4. **Python 技巧**：列表推导式可以简化代码

## 易错点

1. **忘记判空**：root 为空时直接返回 []
2. **层分隔错误**：没有正确记录每层节点数，导致层混乱
3. **队列使用**：忘记从队头取元素（popleft），误用 pop()
4. **DFS 时机**：DFS 方法中要先检查 `len(result) == level` 再添加列表

## 相关题目

- [107. 二叉树的层序遍历 II](https://leetcode.com/problems/binary-tree-level-order-traversal-ii/) - 自底向上层序遍历
- [103. 二叉树的锯齿形层序遍历](https://leetcode.com/problems/binary-tree-zigzag-level-order-traversal/) - 之字形遍历
- [637. 二叉树的层平均值](https://leetcode.com/problems/average-of-levels-in-binary-tree/) - 计算每层平均值
- [199. 二叉树的右视图](https://leetcode.com/problems/binary-tree-right-side-view/) - 每层最右节点

## 学习建议

1. **掌握 BFS 模板**：这是树的层序遍历标准模板，需要熟练掌握
2. **理解层分隔**：通过记录当前层节点数实现层与层的分隔
3. **对比 DFS/BFS**：理解两种方法的本质区别和适用场景
4. **练习变体**：尝试实现自底向上、之字形等变体
