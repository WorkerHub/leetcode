---
title: 748.shortest-completing-word
date: 2026-04-10 10:00:00
categories: easy
tags: [数组, 哈希表, 字符串]
---

## 题目描述

给定一个字符串 licensePlate 和一个字符串数组 words，找到并返回 words 中的最短完整词。

完整词是指包含 licensePlate 中所有字母的单词。忽略 licensePlate 中的数字和空格，不区分大小写。

## Python 代码实现

```python
from typing import List
from collections import Counter

class Solution:
    def shortestCompletingWord(self, licensePlate: str, words: List[str]) -> str:
        # 统计 licensePlate 中的字母
        target = Counter(c.lower() for c in licensePlate if c.isalpha())
        
        result = None
        for word in words:
            word_count = Counter(word)
            # 检查是否包含所有字母
            if all(word_count[c] >= target[c] for c in target):
                if result is None or len(word) < len(result):
                    result = word
        
        return result
```

## 复杂度分析

- 时间复杂度：O(n * m)
- 空间复杂度：O(1)

## 关键点总结

1. 使用 Counter 统计字母频率
2. 忽略数字和空格
3. 不区分大小写

## 学习建议

1. 掌握 Counter 的使用
2. 理解字符串过滤技巧
3. 注意大小写转换
