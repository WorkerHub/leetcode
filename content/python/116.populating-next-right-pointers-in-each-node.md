---
title: 116.populating-next-right-pointers-in-each-node
date: 2026-04-09 10:05:00
categories: medium
tags: [树, 二叉树, 层序遍历, 广度优先搜索]
---

## 题目描述

给定一个完美二叉树，其所有叶子节点都在同一层，每个父节点都有两个子节点。二叉树定义如下：

```cpp
struct Node {
  int val;
  Node *left;
  Node *right;
  Node *next;
}
```

填充它的每个 `next` 指针，让这个指针指向其下一个右侧节点。如果找不到下一个右侧节点，则将 `next` 指针设置为 `NULL`。

初始状态下，所有 `next` 指针都被设置为 `NULL`。

**示例 1：**

```
输入：root = [1,2,3,4,5,6,7]
输出：[1,#,2,3,#,4,5,6,7,#]
解释：
给定二叉树如上图所示，你的函数应该填充它的每个 next 指针，以指向其下一个右侧节点。
序列化的输出按层序遍历排列，同一层节点由 next 指针连接，'#' 标志着每一层的结束。
```

**示例 2：**

```
输入：root = []
输出：[]
```

**提示：**

- 树中节点的数量在 `[0, 2^12 - 1]` 范围内
- `-1000 <= Node.val <= 1000`

**进阶：**

- 你只能使用常量级额外空间。
- 使用递归解题也符合要求，本题中递归程序占用的栈空间不算做额外的空间复杂度。

## 解题思路

这道题要求将完美二叉树的每一层节点用 `next` 指针连接起来。有多种解法：

**核心思想：**

1. **层序遍历**：使用队列，同一层的节点连接起来
2. **利用已建立的 next 指针**：从上到下，利用上一层的 next 指针来连接下一层
3. **递归**：先连接左右子节点，再连接跨父节点的节点

**完美二叉树的性质：**
- 每个节点要么有两个子节点，要么没有子节点
- 所有叶子节点都在同一层

## Python 代码实现

### 方法一：层序遍历（BFS）

```python
"""
# Definition for a Node.
class Node:
    def __init__(self, val: int = 0, left: 'Node' = None, right: 'Node' = None, next: 'Node' = None):
        self.val = val
        self.left = left
        self.right = right
        self.next = next
"""

class Solution:
    def connect(self, root: 'Optional[Node]') -> 'Optional[Node]':
        if not root:
            return None
        
        from collections import deque
        queue = deque([root])
        
        while queue:
            level_size = len(queue)
            
            # 遍历当前层的所有节点
            for i in range(level_size):
                node = queue.popleft()
                
                # 如果不是当前层的最后一个节点，连接到下一个节点
                if i < level_size - 1:
                    node.next = queue[0]
                
                # 将子节点加入队列
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
        
        return root
```

### 方法二：利用已建立的 next 指针（推荐，O(1) 空间）

```python
class Solution:
    def connect(self, root: 'Optional[Node]') -> 'Optional[Node]':
        if not root:
            return None
        
        # 从根节点开始，逐层处理
        leftmost = root
        
        while leftmost.left:  # 当前层有子节点
            # 遍历当前层的所有节点
            head = leftmost
            while head:
                # 连接同一父节点的左右子节点
                head.left.next = head.right
                
                # 连接不同父节点的子节点
                if head.next:
                    head.right.next = head.next.left
                
                # 移动到当前层的下一个节点
                head = head.next
            
            # 移动到下一层
            leftmost = leftmost.left
        
        return root
```

### 方法三：递归

```python
class Solution:
    def connect(self, root: 'Optional[Node]') -> 'Optional[Node]':
        if not root:
            return None
        
        # 连接左右子节点
        if root.left:
            root.left.next = root.right
            
            # 连接跨父节点的节点（右子节点 -> 父节点的next的左子节点）
            if root.next:
                root.right.next = root.next.left
            
            # 递归处理左右子树
            self.connect(root.left)
            self.connect(root.right)
        
        return root
```

### 方法四：简化的层序遍历

```python
class Solution:
    def connect(self, root: 'Optional[Node]') -> 'Optional[Node]':
        if not root:
            return None
        
        from collections import deque
        queue = deque([root])
        
        while queue:
            prev = None
            for _ in range(len(queue)):
                node = queue.popleft()
                
                # 连接到前一个节点
                if prev:
                    prev.next = node
                prev = node
                
                # 添加子节点
                if node.left:
                    queue.append(node.left)
                if node.right:
                    queue.append(node.right)
        
        return root
```

## 复杂度分析

**方法一：层序遍历（BFS）**
- 时间复杂度：O(n)，每个节点访问一次
- 空间复杂度：O(n)，队列最多存储一层的节点，完美二叉树最后一层有 n/2 个节点

**方法二：利用 next 指针（推荐）**
- 时间复杂度：O(n)，每个节点访问一次
- 空间复杂度：O(1)，只使用常量额外空间

**方法三：递归**
- 时间复杂度：O(n)
- 空间复杂度：O(log n)，递归栈深度为树的高度

## 关键点总结

1. **完美二叉树特性**：每个节点都有两个子节点或没有子节点，简化了问题
2. **连接规律**：
   - 同一父节点的左右子节点：`left.next = right`
   - 跨父节点的节点：`right.next = parent.next.left`
3. **空间优化**：利用已建立的 next 指针遍历当前层，无需额外空间
4. **层序思想**：可以用 BFS 实现，但不是最优解
5. **递归技巧**：先处理连接，再递归子树

## 易错点

1. **忘记连接跨父节点的节点**：只连接了左右子节点，忘记 `right.next = parent.next.left`
2. **空树处理**：没有判断 `root` 为空的情况
3. **最右侧节点**：最右侧节点的 next 应该是 `NULL`，不需要特殊处理
4. **递归顺序**：要先建立连接，再递归子树
5. **边界条件**：判断 `head.next` 是否存在，避免空指针异常

## 图解示例

```
初始状态：
       1
      / \
     2   3
    / \ / \
   4  5 6  7

第一步：连接第二层
       1 -> NULL
      / \
     2 ->3 -> NULL
    / \ / \
   4  5 6  7

第二步：连接第三层
       1 -> NULL
      / \
     2 ->3 -> NULL
    / \ / \
   4->5->6->7 -> NULL
```

## 相关题目

- [117. 填充每个节点的下一个右侧节点指针 II](https://leetcode.com/problems/populating-next-right-pointers-in-each-node-ii/)
- [102. 二叉树的层序遍历](https://leetcode.com/problems/binary-tree-level-order-traversal/)
- [199. 二叉树的右视图](https://leetcode.com/problems/binary-tree-right-side-view/)
- [429. N 叉树的层序遍历](https://leetcode.com/problems/n-ary-tree-level-order-traversal/)

## 学习建议

1. **理解完美二叉树**：与普通二叉树的区别，简化了问题
2. **掌握层序遍历**：BFS 是解决树的层次问题的常用方法
3. **空间优化思路**：利用题目给定的结构（next 指针）优化空间
4. **对比 117 题**：本题的进阶版本，处理非完美二叉树
5. **练习递归思维**：理解如何将问题分解为子问题
