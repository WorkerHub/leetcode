---
title: 1672.richest-customer-wealth
date: 2026-04-13 10:00:00
categories: easy
tags: [数组, 矩阵]
---

## 题目描述

给你一个 `m x n` 的整数网格 `accounts`，其中 `accounts[i][j]` 是第 `i` 位客户在第 `j` 家银行托管的资产数量。

返回最富有客户所拥有的**资产总量**。

客户的**财富**是他们在所有银行里托管的资产的总和，最富有的客户就是**资产总量最大**的客户。

**示例：**
```
输入：accounts = [[1,2,3],[3,2,1]]
输出：6（两个客户财富都是 6）
```

## 解题思路

简单的矩阵行求和，取最大值。

## 代码实现

### 方法一：内置函数（推荐）

```python
from typing import List

class Solution:
    def maximumWealth(self, accounts: List[List[int]]) -> int:
        return max(sum(row) for row in accounts)
```

### 方法二：双层循环

```python
from typing import List

class Solution:
    def maximumWealth(self, accounts: List[List[int]]) -> int:
        max_wealth = 0
        for customer in accounts:
            wealth = 0
            for bank in customer:
                wealth += bank
            max_wealth = max(max_wealth, wealth)
        return max_wealth
```

## 复杂度分析

| 方法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 内置函数 | O(m * n) | O(1) |
| 双层循环 | O(m * n) | O(1) |

## 关键点总结

1. 每个客户的财富 = 该行所有数字之和
2. 返回所有客户财富的最大值
3. Python 一行代码即可解决

## 易错点

- 是每行的和的最大值，不是整个矩阵的最大值

## 相关题目

- [1480. 一维数组的动态和](https://leetcode.com/problems/running-sum-of-1d-array/)
- [2500. 删除每行中的最大值](https://leetcode.com/problems/delete-greatest-value-in-each-row/)
