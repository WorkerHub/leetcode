---
title: 650.2-keys-keyboard
date: 2026-04-09 12:20:00
categories: medium
tags: [数学, 动态规划]
---

## 题目描述

最初记事本上只有一个字符 `'A'`。你每次可以对这个记事本进行两种操作：

- `Copy All`（复制全部）：复制这个记事本中的所有字符（不允许仅复制部分字符）。
- `Paste`（粘贴）：粘贴**上一次**复制的字符。

给你一个数字 `n`，你需要使用最少的操作次数，在记事本上输出**恰好** `n` 个 `'A'`。返回能够打印出 `n` 个 `'A'` 的最少操作次数。

**示例 1：**

```
输入：n = 3
输出：3
解释：
最初, 只有一个字符 'A'。
第 1 步, 使用 Copy All 操作。
第 2 步, 使用 Paste 操作来获得 'AA'。
第 3 步, 使用 Paste 操作来获得 'AAA'。
```

**示例 2：**

```
输入：n = 1
输出：0
```

**提示：**

- `1 <= n <= 1000`

## 解题思路

**核心：质因数分解**

观察：
- 要得到 n 个 A，需要通过复制和粘贴
- 最优策略：每次复制后连续粘贴若干次
- 如果 n = a * b，操作数 = a的操作数 + b

**数学方法：**
- n 的最小操作数 = n 的所有质因数之和
- 例如：n=6=2×3，操作数=2+3=5

## Python 代码实现

### 方法一：质因数分解（推荐）⭐

```python
class Solution:
    def minSteps(self, n: int) -> int:
        """
        质因数分解
        
        时间复杂度：O(√n)
        空间复杂度：O(1)
        """
        result = 0
        factor = 2
        
        while n > 1:
            while n % factor == 0:
                result += factor
                n //= factor
            factor += 1
        
        return result
```

### 方法二：动态规划

```python
class Solution:
    def minSteps(self, n: int) -> int:
        """
        DP
        
        时间复杂度：O(n√n)
        空间复杂度：O(n)
        """
        if n == 1:
            return 0
        
        dp = [float('inf')] * (n + 1)
        dp[1] = 0
        
        for i in range(2, n + 1):
            # 尝试所有因数
            for j in range(1, int(i ** 0.5) + 1):
                if i % j == 0:
                    # i = j * (i // j)
                    # 复制 j 个 A，粘贴 (i//j - 1) 次
                    dp[i] = min(dp[i], dp[j] + i // j)
                    dp[i] = min(dp[i], dp[i // j] + j)
        
        return dp[n]
```

## 关键点总结

1. **质因数和**：最小操作数 = 质因数之和
2. **DP 递推**：`dp[i] = min(dp[j] + i/j)` for all j | i
3. **贪心思想**：每次尽可能复制更多

## 相关题目

- [651. 4 Keys Keyboard](https://leetcode.com/problems/4-keys-keyboard/)
- [204. Count Primes](https://leetcode.com/problems/count-primes/)
