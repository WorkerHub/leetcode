---
title: 732.my-calendar-iii
date: 2026-04-10 10:00:00
categories: hard
tags: [设计, 线段树, 有序集合]
---

## 题目描述

实现一个 MyCalendarThree 类来存放你的日程安排，你可以一直添加新的日程安排。

每次调用 book 方法时，返回一个整数 k，表示所有事件中最大的 k 次预订。

## Python 代码实现

```python
from collections import defaultdict

class MyCalendarThree:
    def __init__(self):
        self.delta = defaultdict(int)
    
    def book(self, start: int, end: int) -> int:
        self.delta[start] += 1
        self.delta[end] -= 1
        
        active = 0
        max_k = 0
        
        for time in sorted(self.delta.keys()):
            active += self.delta[time]
            max_k = max(max_k, active)
        
        return max_k
```

## 复杂度分析

- 时间复杂度：O(n^2 log n)
- 空间复杂度：O(n)

## 关键点总结

1. 使用差分数组思想
2. 记录时间点的增减
3. 扫描所有时间点找最大值

## 学习建议

1. 理解差分数组的应用
2. 掌握事件扫描线算法
3. 学习优化查询的技巧
