---
title: 590.n-ary-tree-postorder-traversal
date: 2026-04-09 12:36:00
categories: easy
tags: [树, 深度优先搜索]
---

## 题目描述

给定一个 N 叉树的根节点 `root`，返回其节点值的后序遍历。

## Python 代码实现

### 方法一：递归

```python
class Solution:
    def postorder(self, root: 'Node') -> List[int]:
        result = []
        def dfs(node):
            if not node:
                return
            for child in node.children:
                dfs(child)
            result.append(node.val)
        dfs(root)
        return result
```

### 方法二：迭代

```python
class Solution:
    def postorder(self, root: 'Node') -> List[int]:
        if not root:
            return []
        stack, result = [root], []
        while stack:
            node = stack.pop()
            result.append(node.val)
            stack.extend(node.children)
        return result[::-1]
```

## 学习建议

理解后序遍历的特点：先访问所有子节点，最后访问根节点。
