---
title: 873.length-of-longest-fibonacci-subsequence
date: 2026-04-09 23:16:03
categories: medium
tags: [数组, 哈希表, 动态规划]
---

## 题目描述

如果序列 X_1, X_2, ..., X_n 满足下列条件，就说它是 斐波那契式 的：
- n >= 3
- 对于所有 i + 2 <= n，都有 X_i + X_{i+1} = X_{i+2}

给定一个严格递增的正整数数组形成序列 arr ，找到 arr 中最长的斐波那契式的子序列的长度。如果一个不存在，返回  0 。

**示例 1：**
```
输入：arr = [1,2,3,4,5,6,7,8]
输出：5
解释：最长的斐波那契式子序列为 [1,2,3,5,8] 。
```

**示例 2：**
```
输入：arr = [1,3,7,11,12,14,18]
输出：3
解释：最长的斐波那契式子序列有 [1,11,12]、[3,11,14] 以及 [7,11,18] 。
```

**提示：**
- 3 <= arr.length <= 1000
- 1 <= arr[i] < arr[i + 1] <= 10^9

## 解题思路

**DP 思路：**
- `dp[i][j]` 表示以 arr[i], arr[j] 结尾的最长斐波那契子序列长度
- 对于每对 (i, j)，找是否存在 k 使得 arr[k] + arr[i] = arr[j]

## Python 代码实现

### 方法一：DP + 哈希表（推荐）⭐

```python
class Solution:
    def lenLongestFibSubseq(self, arr: List[int]) -> int:
        n = len(arr)
        index = {x: i for i, x in enumerate(arr)}
        
        # dp[i][j] 表示以 arr[i], arr[j] 结尾的最长长度
        dp = [[2] * n for _ in range(n)]
        result = 0
        
        for j in range(n):
            for i in range(j):
                # 找 arr[k] 使得 arr[k] + arr[i] = arr[j]
                target = arr[j] - arr[i]
                if target < arr[i] and target in index:
                    k = index[target]
                    dp[i][j] = dp[k][i] + 1
                    result = max(result, dp[i][j])
        
        return result if result >= 3 else 0
```

### 方法二：优化空间

```python
class Solution:
    def lenLongestFibSubseq(self, arr: List[int]) -> int:
        index = {x: i for i, x in enumerate(arr)}
        dp = {}
        result = 0
        
        for j in range(len(arr)):
            for i in range(j):
                target = arr[j] - arr[i]
                if target < arr[i] and target in index:
                    k = index[target]
                    dp[i, j] = dp.get((k, i), 2) + 1
                    result = max(result, dp[i, j])
        
        return result if result >= 3 else 0
```

## 复杂度分析

- **时间复杂度**：O(n²)
- **空间复杂度**：O(n²)

## 关键点总结

1. **DP 状态**：以两个数结尾
2. **哈希优化**：快速查找前一个数
3. **斐波那契性质**：a + b = c

## 相关题目

- [300. Longest Increasing Subsequence](https://leetcode.com/problems/longest-increasing-subsequence/)
- [446. Arithmetic Slices II - Subsequence](https://leetcode.com/problems/arithmetic-slices-ii-subsequence/)
