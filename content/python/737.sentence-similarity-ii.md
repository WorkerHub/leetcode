---
title: 737.sentence-similarity-ii
date: 2026-04-10 10:00:00
categories: medium
tags: [深度优先搜索, 广度优先搜索, 并查集]
---

## 题目描述

给定两个句子和一个相似单词对列表，判断两个句子是否相似。

注意相似关系是传递的，即如果 a 和 b 相似，b 和 c 相似，那么 a 和 c 也相似。

## Python 代码实现

```python
from typing import List

class Solution:
    def areSentencesSimilarTwo(self, sentence1: List[str], sentence2: List[str], similarPairs: List[List[str]]) -> bool:
        if len(sentence1) != len(sentence2):
            return False
        
        # 构建并查集
        parent = {}
        
        def find(x):
            if x not in parent:
                parent[x] = x
            if parent[x] != x:
                parent[x] = find(parent[x])
            return parent[x]
        
        def union(x, y):
            root_x, root_y = find(x), find(y)
            if root_x != root_y:
                parent[root_x] = root_y
        
        # 建立相似关系
        for w1, w2 in similarPairs:
            union(w1, w2)
        
        # 检查每对单词
        for w1, w2 in zip(sentence1, sentence2):
            if w1 == w2:
                continue
            if find(w1) != find(w2):
                return False
        
        return True
```

## 复杂度分析

- 时间复杂度：O(n + m)
- 空间复杂度：O(m)

## 关键点总结

1. 使用并查集处理传递关系
2. 路径压缩优化查询
3. 检查是否在同一集合

## 学习建议

1. 掌握并查集的应用场景
2. 理解传递关系的处理
3. 学习路径压缩优化
