---
title: 931.minimum-falling-path-sum
date: 2026-04-10 10:00:00
categories: medium
tags: [数组, 动态规划, 矩阵]
---

## 题目描述

给定一个 n x n 的方形整数数组 matrix，返回通过矩阵的下降路径的最小和。

下降路径可以从第一行中的任何元素开始，并从每一行中选择一个元素。在下一行选择的元素和当前行所选元素最多相隔一列（即位于正下方或者沿对角线向左或者向右的第一个元素）。

**示例：**
```
输入：matrix = [[2,1,3],[6,5,4],[7,8,9]]
输出：13
```

## Python 代码实现

```python
from typing import List

class Solution:
    def minFallingPathSum(self, matrix: List[List[int]]) -> int:
        n = len(matrix)
        dp = matrix[0][:]
        
        for i in range(1, n):
            new_dp = [0] * n
            for j in range(n):
                min_prev = dp[j]
                if j > 0:
                    min_prev = min(min_prev, dp[j-1])
                if j < n - 1:
                    min_prev = min(min_prev, dp[j+1])
                new_dp[j] = matrix[i][j] + min_prev
            dp = new_dp
        
        return min(dp)
```

## 复杂度分析

- 时间复杂度：O(n^2)
- 空间复杂度：O(n)
