---
title: 2133.check-if-every-row-and-column-contains-all-numbers
date: 2026-04-13 10:00:00
categories: easy
tags: [哈希表, 矩阵]
---

## 题目描述

判断n×n矩阵的每行每列是否都包含1到n的所有整数。

## 解题思路

对每行每列转为集合，判断是否等于{1,2,...,n}。

## Java 代码实现

```java
class Solution {
    public boolean checkValid(int[][] matrix) {
        int n = matrix.length;
        
        for (int i = 0; i < n; i++) {
            boolean[] rowSeen = new boolean[n + 1];
            boolean[] colSeen = new boolean[n + 1];
            for (int j = 0; j < n; j++) {
                if (rowSeen[matrix[i][j]]) return false;
                rowSeen[matrix[i][j]] = true;
                if (colSeen[matrix[j][i]]) return false;
                colSeen[matrix[j][i]] = true;
            }
        }
        return true;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N^2)
- **空间复杂度**：O(N)

## 关键点总结

1. **布尔数组检重**：每行每列用seen数组判断是否有重复
2. **同时检查行列**：一次遍历同时检查第i行和第i列
3. **值在[1,n]**：题目保证，直接用作数组索引

## 相关题目

- [36. Valid Sudoku](https://leetcode.com/problems/valid-sudoku/)
