---
title: 49.group-anagrams
date: 2026-04-09 10:38:00
categories: medium
tags: [数组, 哈希表, 字符串, 排序]
---

## 题目描述

给你一个字符串数组，请你将字母异位词组合在一起。可以按任意顺序返回结果列表。

字母异位词是由重新排列源单词的所有字母得到的一个新单词。

**示例 1：**
```
输入：strs = ["eat","tea","tan","ate","nat","bat"]
输出：[["bat"],["nat","tan"],["ate","eat","tea"]]
```

**示例 2：**
```
输入：strs = [""]
输出：[[""]]
```

**示例 3：**
```
输入：strs = ["a"]
输出：[["a"]]
```

**提示：**
- `1 <= strs.length <= 10^4`
- `0 <= strs[i].length <= 100`
- `strs[i]` 仅包含小写字母

## 解题思路

核心思想：使用哈希表分组

**分组键的选择：**
1. 排序后的字符串
2. 字符计数（26个字母）

## Java 代码实现

### 方法一：排序（推荐）

```java
class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {
        Map<String, List<String>> anagrams = new HashMap<>();

        for (String s : strs) {
            // 排序后的字符串作为key
            char[] chars = s.toCharArray();
            Arrays.sort(chars);
            String key = new String(chars);

            // 不存在则初始化
            if (!anagrams.containsKey(key)) {
                anagrams.put(key, new ArrayList<>());
            }
            anagrams.get(key).add(s);
        }

        return new ArrayList<>(anagrams.values());
    }
}
```

### 方法二：字符计数

```java
class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {
        Map<String, List<String>> anagrams = new HashMap<>();

        for (String s : strs) {
            // 统计每个字符的出现次数
            int[] count = new int[26];
            for (char c : s.toCharArray()) {
                count[c - 'a']++;
            }

            // 将计数数组转为字符串作为key
            StringBuilder sb = new StringBuilder();
            for (int i = 0; i < 26; i++) {
                sb.append('#');
                sb.append(count[i]);
            }
            String key = sb.toString();

            if (!anagrams.containsKey(key)) {
                anagrams.put(key, new ArrayList<>());
            }
            anagrams.get(key).add(s);
        }

        return new ArrayList<>(anagrams.values());
    }
}
```

## 复杂度分析

**方法一：**
- 时间复杂度：O(n × k log k)，n 为字符串数量，k 为字符串平均长度
- 空间复杂度：O(n × k)

**方法二：**
- 时间复杂度：O(n × k)
- 空间复杂度：O(n × k)

## 关键点总结

1. **哈希分组**：相同特征的字符串分到一组
2. **排序作为key**：简单直接
3. **计数作为key**：时间复杂度更优

## 易错点

1. **key 的唯一性**：字符计数作为 key 时需要加分隔符，防止 [1,11] 和 [11,1] 冲突
2. **返回格式**：返回 values() 的列表

## 相关题目

- [242. 有效的字母异位词](https://leetcode.com/problems/valid-anagram/)
- [438. 找到字符串中所有字母异位词](https://leetcode.com/problems/find-all-anagrams-in-a-string/)

## 学习建议

这是哈希表的经典应用，理解如何选择合适的key进行分组。
