---
title: 822.card-flipping-game
date: 2026-04-09 12:25:00
categories: medium
tags: [数组, 哈希表]
---

## 题目描述

在桌子上有 `n` 张卡片，每张卡片的正面和背面都写着一个正数（正面与背面上的数有可能不一样）。

我们可以先翻转任意张卡片，然后选择其中一张卡片。

如果选中的那张卡片背面的数字 `x` 与任意一张卡片的正面的数字都不同，那么这个数字是我们想要的数字。

哪个数是这些想要的数字中最小的数（找到这些数中的最小值）呢？如果没有一个数字符合要求的，输出 `0`。

其中，`fronts[i]` 和 `backs[i]` 分别代表第 `i` 张卡片的正面和背面的数字。

**示例:**
```
输入: fronts = [1,2,4,4,7], backs = [1,3,4,1,3]
输出: 2
解释: 假设我们翻转第二张卡片，那么正面是 [1,3,4,4,7]，背面是 [1,2,4,1,3]。
我们选择背面的 2，因为 2 不在任何卡片的正面出现，所以 2 就是符合条件的最小值。
```

## Java 代码实现

```java
import java.util.*;

class Solution {
    public int flipgame(int[] fronts, int[] backs) {
        // 正反面相同的数字不能作为答案（无论如何翻转，它都会出现在正面）
        Set<Integer> same = new HashSet<>();
        for (int i = 0; i < fronts.length; i++) {
            if (fronts[i] == backs[i]) {
                same.add(fronts[i]);
            }
        }

        // 找最小的不在 same 中的数
        int result = Integer.MAX_VALUE;
        for (int num : fronts) {
            if (!same.contains(num)) {
                result = Math.min(result, num);
            }
        }
        for (int num : backs) {
            if (!same.contains(num)) {
                result = Math.min(result, num);
            }
        }

        return result == Integer.MAX_VALUE ? 0 : result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)
- **空间复杂度**：O(n)

## 关键点总结

1. **排除条件**：正反面相同的数字一定会出现在正面
2. **候选数字**：所有不在"相同"集合中的数
3. **最小值**：从候选中找最小

## 学习建议

1. **集合操作**：HashSet 的应用
2. **逻辑推理**：理解为什么相同的不能选
