---
title: 3755.shortest-cycle-detection-in-graph
date: 2026-04-13 10:00:00
categories: medium
tags: [图, BFS]
---

## 题目描述

给定有 `n` 个节点的无向图和边集 `edges`，找出图中最短环的长度（最小环的边数）。若不存在环，返回 `-1`。

## 解题思路

对每个节点作为 BFS 起点，若在 BFS 中发现已访问节点（且不是当前节点的父节点），则找到环，长度为两节点到起点距离之和加 1。取所有起点的最小值。

## Java 代码实现

```java
import java.util.ArrayList;
import java.util.ArrayDeque;
import java.util.Deque;
import java.util.List;

class Solution {
    public int findShortestCycle(int n, int[][] edges) {
        List<List<Integer>> graph = new ArrayList<>();
        for (int i = 0; i < n; i++) graph.add(new ArrayList<>());
        for (int[] e : edges) {
            graph.get(e[0]).add(e[1]);
            graph.get(e[1]).add(e[0]);
        }

        int ans = Integer.MAX_VALUE;

        for (int start = 0; start < n; start++) {
            int[] dist = new int[n];
            int[] parent = new int[n];
            java.util.Arrays.fill(dist, -1);
            java.util.Arrays.fill(parent, -1);
            dist[start] = 0;
            Deque<Integer> queue = new ArrayDeque<>();
            queue.offer(start);

            while (!queue.isEmpty()) {
                int u = queue.poll();
                for (int v : graph.get(u)) {
                    if (dist[v] == -1) {
                        dist[v] = dist[u] + 1;
                        parent[v] = u;
                        queue.offer(v);
                    } else if (parent[u] != v) {
                        // 发现已访问且不是父节点，找到环
                        ans = Math.min(ans, dist[u] + dist[v] + 1);
                    }
                }
            }
        }

        return ans == Integer.MAX_VALUE ? -1 : ans;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n * (n + m))
- 空间复杂度：O(n + m)

## 关键点总结

1. 对每个节点做 BFS，发现已访问节点（非父节点）时即找到环
2. 环长度 = 两节点到起点距离之和 + 1
3. 取所有起点的最小值

## 相关题目

- [207. Course Schedule](https://leetcode.com/problems/course-schedule/)
- [2608. Shortest Cycle in a Graph](https://leetcode.com/problems/shortest-cycle-in-a-graph/)
