---
title: 1935.maximum-number-of-words-you-can-type
date: 2026-04-13 10:00:00
categories: easy
tags: [哈希表, 字符串]
---

## 题目描述

给你字符串 `text` 和损坏键字符串 `brokenLetters`，返回 `text` 中不含任何损坏键字母的单词数量。

## 解题思路

将损坏键存入 boolean 数组，对每个单词检查是否含有损坏字母，统计可打出的单词数。

## Java 代码实现

```java
class Solution {
    public int canBeTypedWords(String text, String brokenLetters) {
        boolean[] broken = new boolean[26];
        for (char c : brokenLetters.toCharArray())
            broken[c - 'a'] = true;

        int count = 0;
        for (String word : text.split(" ")) {
            boolean ok = true;
            for (char c : word.toCharArray()) {
                if (broken[c - 'a']) { ok = false; break; }
            }
            if (ok) count++;
        }
        return count;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)
- **空间复杂度**：O(1)

## 关键点总结

1. `boolean[26]` 记录损坏键，O(1) 查找
2. 按空格分割单词，逐字符检查
3. 发现损坏键立即短路，无需遍历完整单词

## 相关题目

- [LeetCode 1704. Determine if String Halves Are Alike](https://leetcode.com/problems/determine-if-string-halves-are-alike/)
