---
title: 81.search-in-rotated-sorted-array-ii
date: 2026-04-09 11:10:00
categories: medium
tags: [数组, 二分查找]
---

## 题目描述

已知存在一个按非降序排列的整数数组 `nums`，数组中的值不必互不相同。

在传递给函数之前，`nums` 在预先未知的某个下标 `k`（`0 <= k < nums.length`）上进行了旋转，使数组变为 `[nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]]`（下标从 0 开始计数）。例如，`[0,1,2,4,4,4,5,6,6,7]` 在下标 `5` 处经旋转后可能变为 `[4,5,6,6,7,0,1,2,4,4]`。

给你旋转后的数组 `nums` 和一个整数 `target`，请你编写一个函数来判断给定的目标值是否存在于数组中。如果 `nums` 中存在这个目标值 `target`，则返回 `true`，否则返回 `false`。

你必须尽可能减少整个操作步骤。

**示例 1：**
```
输入：nums = [2,5,6,0,0,1,2], target = 0
输出：true
```

**示例 2：**
```
输入：nums = [2,5,6,0,0,1,2], target = 3
输出：false
```

**提示：**
- `1 <= nums.length <= 5000`
- `-10^4 <= nums[i] <= 10^4`
- 题目数据保证 `nums` 在预先未知的某个下标上进行了旋转
- `-10^4 <= target <= 10^4`

## 解题思路

这是第33题的进阶版本，区别在于数组中可能包含重复元素。

**核心思想：** 使用二分查找，但需要处理重复元素导致的边界不确定性。

**关键观察：**
1. 旋转数组分为两个有序部分
2. 当 `nums[left] == nums[mid] == nums[right]` 时，无法判断哪边有序，需要收缩边界
3. 其他情况同第33题，判断哪边有序，再判断 target 是否在有序区间

**算法流程：**
1. 使用二分查找框架
2. 如果遇到 `nums[left] == nums[mid]`，则 `left++` 跳过重复元素
3. 判断左半部分或右半部分是否有序
4. 根据 target 所在位置调整搜索区间

## Java 代码实现

### 方法一：二分查找（处理重复元素）

```java
class Solution {
    public boolean search(int[] nums, int target) {
        int left = 0, right = nums.length - 1;

        while (left <= right) {
            int mid = left + (right - left) / 2;

            // 找到目标值
            if (nums[mid] == target) {
                return true;
            }

            // 处理重复元素：无法判断哪边有序时，收缩左边界
            if (nums[left] == nums[mid]) {
                left++;
                continue;
            }

            // 判断左半部分是否有序
            if (nums[left] < nums[mid]) {
                // 左半部分有序，判断 target 是否在左半部分
                if (nums[left] <= target && target < nums[mid]) {
                    right = mid - 1;
                } else {
                    left = mid + 1;
                }
            } else {
                // 右半部分有序，判断 target 是否在右半部分
                if (nums[mid] < target && target <= nums[right]) {
                    left = mid + 1;
                } else {
                    right = mid - 1;
                }
            }
        }

        return false;
    }
}
```

### 方法二：先去重再二分（简化思路）

```java
class Solution {
    public boolean search(int[] nums, int target) {
        int left = 0, right = nums.length - 1;

        // 去除左边重复
        while (left < right && nums[left] == nums[left + 1]) {
            left++;
        }

        // 去除右边重复
        while (left < right && nums[right] == nums[right - 1]) {
            right--;
        }

        // 标准二分查找
        while (left <= right) {
            int mid = left + (right - left) / 2;

            if (nums[mid] == target) {
                return true;
            }

            // 左半部分有序
            if (nums[left] <= nums[mid]) {
                if (nums[left] <= target && target < nums[mid]) {
                    right = mid - 1;
                } else {
                    left = mid + 1;
                }
            } else {
                // 右半部分有序
                if (nums[mid] < target && target <= nums[right]) {
                    left = mid + 1;
                } else {
                    right = mid - 1;
                }
            }
        }

        return false;
    }
}
```

## 复杂度分析

**方法一：**
- 时间复杂度：O(n)，最坏情况下所有元素相同，需要遍历整个数组
- 空间复杂度：O(1)

**方法二：**
- 时间复杂度：O(n)，去重部分是 O(n)，二分部分是 O(log n)
- 空间复杂度：O(1)

## 关键点总结

1. **重复元素处理**：当 `nums[left] == nums[mid]` 时无法判断哪边有序，需要移动边界
2. **最坏情况**：数组全是相同元素，退化为 O(n)
3. **与第33题的区别**：需要额外处理重复元素的情况

## 易错点

1. **边界条件**：`nums[left] <= nums[mid]` 要用 `<=`，因为可能存在相等情况
2. **区间判断**：判断 target 是否在有序区间时，要注意边界的开闭
3. **continue 语句**：处理重复元素后要用 `continue` 跳过后续逻辑
4. **Java 中 mid 计算**：使用 `left + (right - left) / 2` 防止整数溢出

## 相关题目

- [33. 搜索旋转排序数组](https://leetcode.com/problems/search-in-rotated-sorted-array/)
- [153. 寻找旋转排序数组中的最小值](https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/)
- [154. 寻找旋转排序数组中的最小值 II](https://leetcode.com/problems/find-minimum-in-rotated-sorted-array-ii/)

## 学习建议

这是二分查找的进阶题，重点理解如何处理重复元素。建议先掌握第33题（无重复版本），再学习本题。理解为什么重复元素会导致无法判断哪边有序是关键。
