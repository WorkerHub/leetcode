---
title: 641.design-circular-deque
date: 2026-04-10 10:00:00
categories: medium
tags: [设计, 队列, 数组, 链表]
---

## 题目描述

设计实现双端队列，支持 insertFront、insertLast、deleteFront、deleteLast、getFront、getRear、isEmpty、isFull。

## 解题思路

循环数组 + front 指针 + size 计数器实现。

## Java 代码实现

```java
class MyCircularDeque {
    private int[] data;
    private int front;
    private int size;
    private int capacity;

    public MyCircularDeque(int k) {
        data = new int[k];
        capacity = k;
        front = 0;
        size = 0;
    }

    public boolean insertFront(int value) {
        if (isFull()) return false;
        front = (front - 1 + capacity) % capacity;
        data[front] = value;
        size++;
        return true;
    }

    public boolean insertLast(int value) {
        if (isFull()) return false;
        int rear = (front + size) % capacity;
        data[rear] = value;
        size++;
        return true;
    }

    public boolean deleteFront() {
        if (isEmpty()) return false;
        front = (front + 1) % capacity;
        size--;
        return true;
    }

    public boolean deleteLast() {
        if (isEmpty()) return false;
        size--;
        return true;
    }

    public int getFront() {
        if (isEmpty()) return -1;
        return data[front];
    }

    public int getRear() {
        if (isEmpty()) return -1;
        int rear = (front + size - 1) % capacity;
        return data[rear];
    }

    public boolean isEmpty() {
        return size == 0;
    }

    public boolean isFull() {
        return size == capacity;
    }
}
```

## 复杂度分析

- **时间复杂度**：所有操作 O(1)
- **空间复杂度**：O(k)

## 关键点总结

1. **循环数组**：取模处理索引
2. **front + size**：简洁地定位队尾
3. **insertFront**：front 前移需加 capacity 防负数

## 相关题目

- [622. Design Circular Queue](https://leetcode.com/problems/design-circular-queue/)
