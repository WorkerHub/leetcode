---
title: 893.groups-of-special-equivalent-strings
date: 2026-04-09 23:16:03
categories: medium
tags: [数组, 哈希表, 字符串, 排序]
---

## 题目描述

字符串中奇数位和偶数位的字符可以互相交换，形成等价组。给定字符串数组，返回等价组的数量。

**示例：**
```
输入：words = ["abcd","cdab","cbad","xyzz","zzxy","zzyx"]
输出：3
```

## 解题思路

特征提取：将奇数位字符排序 + 偶数位字符排序作为特征，统计不同特征数量。

## Java 代码实现

```java
class Solution {
    public int numSpecialEquivGroups(String[] words) {
        Set<String> groups = new HashSet<>();

        for (String word : words) {
            char[] even = new char[(word.length() + 1) / 2];
            char[] odd = new char[word.length() / 2];

            for (int i = 0; i < word.length(); i++) {
                if (i % 2 == 0) even[i / 2] = word.charAt(i);
                else odd[i / 2] = word.charAt(i);
            }

            Arrays.sort(even);
            Arrays.sort(odd);
            groups.add(new String(even) + new String(odd));
        }

        return groups.size();
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n × m log m)
- **空间复杂度**：O(n × m)

## 关键点总结

1. **奇偶分离**：分别对奇数位和偶数位排序
2. **特征字符串**：排序结果拼接作为唯一标识
3. **HashSet 去重**：统计不同特征数

## 相关题目

- [49. Group Anagrams](https://leetcode.com/problems/group-anagrams/)
