---
title: 867.transpose-matrix
date: 2026-04-09 23:16:03
categories: easy
tags: [数组, 矩阵]
---

## 题目描述

给你一个二维整数数组 matrix， 返回 matrix 的 转置矩阵 。

矩阵的 转置 是指将矩阵的主对角线翻转，交换矩阵的行索引与列索引。

**示例 1：**
```
输入：matrix = [[1,2,3],[4,5,6],[7,8,9]]
输出：[[1,4,7],[2,5,8],[3,6,9]]
```

**示例 2：**
```
输入：matrix = [[1,2,3],[4,5,6]]
输出：[[1,4],[2,5],[3,6]]
```

**提示：**
- m == matrix.length
- n == matrix[i].length
- 1 <= m, n <= 1000
- 1 <= m * n <= 10^5
- -10^9 <= matrix[i][j] <= 10^9

## 解题思路

转置矩阵就是将行变成列，列变成行。原矩阵的 `matrix[i][j]` 变成转置矩阵的 `result[j][i]`。

## Java 代码实现

### 方法一：直接构造（推荐）

```java
class Solution {
    public int[][] transpose(int[][] matrix) {
        int m = matrix.length, n = matrix[0].length;
        // 转置后维度为 n × m
        int[][] result = new int[n][m];

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                result[j][i] = matrix[i][j];
            }
        }

        return result;
    }
}
```

**代码说明：**
- 新矩阵维度为 n × m（行列互换）
- `result[j][i] = matrix[i][j]` 完成转置

## 复杂度分析

- **时间复杂度**：O(m × n)，遍历所有元素。
- **空间复杂度**：O(m × n)，结果矩阵。

## 关键点总结

1. **索引转换**：`matrix[i][j]` → `result[j][i]`
2. **维度变化**：m×n 矩阵转置后是 n×m
3. **新建数组**：不能在原数组上修改（非方阵时维度不同）

## 易错点

1. **维度错误**：结果数组应为 `new int[n][m]`，而不是 `new int[m][n]`
2. **索引顺序**：`result[j][i]` 而不是 `result[i][j]`

## 相关题目

- [48. Rotate Image](https://leetcode.com/problems/rotate-image/) - 原地旋转（需要转置）
- [54. Spiral Matrix](https://leetcode.com/problems/spiral-matrix/) - 矩阵遍历
