---
title: 3808.count-subarrays-with-product-less-than-k
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 滑动窗口]
---

## 题目描述

给定正整数数组 `nums` 和正整数 `k`，返回乘积严格小于 `k` 的子数组数目。

## Java 代码实现

```java
class Solution {
    public int numSubarrayProductLessThanK(int[] nums, int k) {
        if (k <= 1) return 0;
        int count = 0, product = 1, left = 0;
        for (int right = 0; right < nums.length; right++) {
            product *= nums[right];
            // 收缩左边界直到乘积 < k
            while (product >= k) product /= nums[left++];
            // 以 right 为右端点的合法子数组数
            count += right - left + 1;
        }
        return count;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)

## 关键点总结

1. 滑动窗口维护乘积严格小于 k 的最大窗口
2. 以 right 为右端点的合法子数组数为 `right - left + 1`
3. k <= 1 时无合法子数组（所有元素 >= 1）

## 相关题目

- [713. Subarray Product Less Than K](https://leetcode.com/problems/subarray-product-less-than-k/)
