---
title: 1557.minimum-number-of-vertices-to-reach-all-nodes
date: 2026-04-13 10:00:00
categories: medium
tags: [图]
---

## 题目描述

有向无环图中，找最小顶点集合使从这些顶点出发可到达所有节点。

**示例：**
```
输入：n = 6, edges = [[0,1],[0,2],[2,5],[3,4],[4,2]]
输出：[0,3]
```

## 解题思路

答案就是所有入度为 0 的节点（无法从其他节点到达）。

## Java 代码实现

```java
class Solution {
    public List<Integer> findSmallestSetOfVertices(int n, List<List<Integer>> edges) {
        boolean[] hasIncoming = new boolean[n];
        for (List<Integer> edge : edges) {
            hasIncoming[edge.get(1)] = true;
        }
        List<Integer> result = new ArrayList<>();
        for (int i = 0; i < n; i++) {
            if (!hasIncoming[i]) result.add(i);
        }
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(E + N)
- **空间复杂度**：O(N)

## 关键点总结

1. **入度为 0**：无法被其他节点到达，必须作为起点
2. **DAG 性质**：不需要 BFS/DFS，直接判断入度

## 相关题目

- [207. Course Schedule](https://leetcode.com/problems/course-schedule/)
