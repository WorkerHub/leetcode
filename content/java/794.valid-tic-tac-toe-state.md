---
title: 794.valid-tic-tac-toe-state
date: 2026-04-09 13:00:00
categories: medium
tags: [数组, 字符串]
---

## 题目描述

给定 3×3 井字游戏棋盘 board，判断是否为合法状态。X 先手，轮流下棋，有人赢则游戏结束。

**示例：**
```
输入：board = ["O  ","   ","   "]
输出：false（O 不可能先手）
```

## 解题思路

验证多个规则：1) X 数量等于 O 或多 1；2) 不能双赢；3) X 赢时 X 多一个，O 赢时数量相等。

## Java 代码实现

```java
class Solution {
    public boolean validTicTacToe(String[] board) {
        int countX = 0, countO = 0;
        for (String row : board) {
            for (char c : row.toCharArray()) {
                if (c == 'X') countX++;
                else if (c == 'O') countO++;
            }
        }

        // 数量关系
        if (countX < countO || countX > countO + 1) return false;

        boolean xWin = isWinner(board, 'X');
        boolean oWin = isWinner(board, 'O');

        // 不能双赢
        if (xWin && oWin) return false;
        // X 赢时必须 X 刚下完
        if (xWin && countX != countO + 1) return false;
        // O 赢时必须 O 刚下完
        if (oWin && countX != countO) return false;

        return true;
    }

    private boolean isWinner(String[] board, char player) {
        // 检查行
        for (int i = 0; i < 3; i++) {
            if (board[i].charAt(0) == player && board[i].charAt(1) == player && board[i].charAt(2) == player)
                return true;
        }
        // 检查列
        for (int j = 0; j < 3; j++) {
            if (board[0].charAt(j) == player && board[1].charAt(j) == player && board[2].charAt(j) == player)
                return true;
        }
        // 检查对角线
        if (board[0].charAt(0) == player && board[1].charAt(1) == player && board[2].charAt(2) == player)
            return true;
        if (board[0].charAt(2) == player && board[1].charAt(1) == player && board[2].charAt(0) == player)
            return true;
        return false;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(1)，棋盘大小固定
- **空间复杂度**：O(1)

## 关键点总结

1. **数量关系**：X 先手，countX == countO 或 countX == countO + 1
2. **双赢互斥**：不可能同时获胜
3. **获胜时机**：谁赢谁是最后下棋的人

## 相关题目

- [348. Design Tic-Tac-Toe](https://leetcode.com/problems/design-tic-tac-toe/)
- [1275. Find Winner on a Tic Tac Toe Game](https://leetcode.com/problems/find-winner-on-a-tic-tac-toe-game/)
