---
title: 3591.check-if-any-element-has-prime-frequency
date: 2026-04-13 10:00:00
categories: easy
tags: [数组, 哈希表, 数学, 数论]
---

## 题目描述

给定整数数组 `nums`，若存在某个元素出现频次为质数则返回 `true`，否则返回 `false`。

## Java 代码实现

```java
import java.util.*;

class Solution {
    public boolean checkPrimeFrequency(int[] nums) {
        Map<Integer, Integer> freq = new HashMap<>();
        for (int x : nums) freq.merge(x, 1, Integer::sum);

        for (int cnt : freq.values()) {
            if (isPrime(cnt)) return true;
        }
        return false;
    }

    private boolean isPrime(int n) {
        if (n < 2) return false;
        if (n == 2) return true;
        if (n % 2 == 0) return false;
        for (int i = 3; (long) i * i <= n; i += 2) {
            if (n % i == 0) return false;
        }
        return true;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n + sqrt(max_freq))，统计频次 O(n)，质数判断 O(sqrt(max_freq))
- 空间复杂度：O(n)

## 关键点总结

1. 用哈希表统计每个元素频次
2. 对频次逐一判断是否为质数（枚举到 sqrt(n) 即可）
3. 有任意一个质数频次即返回 true

## 相关题目

- [204. Count Primes](https://leetcode.com/problems/count-primes/)
- [1748. Sum of Unique Elements](https://leetcode.com/problems/sum-of-unique-elements/)
