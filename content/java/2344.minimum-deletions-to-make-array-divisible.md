---
title: 2344.minimum-deletions-to-make-array-divisible
date: 2026-04-13 10:00:00
categories: hard
tags: [数组, 数学, 数论, 排序, 堆]
---

## 题目描述

给你正整数数组 `nums` 和 `numsDivide`，可从 nums 中删除元素，返回使 nums 最小元素能整除 numsDivide 所有元素的最少删除次数，不可能则返回 -1。

## 解题思路

`v` 能整除 numsDivide 所有元素等价于 `v` 是 numsDivide 的 GCD 的因子。先求 numsDivide 的 GCD 为 g，对 nums 排序后，找第一个满足 `g % v == 0` 的元素，其前面的元素数即为答案。

## Java 代码实现

```java
import java.util.Arrays;

class Solution {
    public int minOperations(int[] nums, int[] numsDivide) {
        // 计算 numsDivide 的 GCD
        int g = numsDivide[0];
        for (int x : numsDivide) g = gcd(g, x);

        Arrays.sort(nums);
        // 找第一个能整除 g 的元素
        for (int i = 0; i < nums.length; i++) {
            if (g % nums[i] == 0) return i; // 前面 i 个元素需要删除
        }
        return -1;
    }

    private int gcd(int a, int b) {
        return b == 0 ? a : gcd(b, a % b);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n log n + m log(max))，n = len(nums)，m = len(numsDivide)
- **空间复杂度**：O(1)

## 关键点总结

1. 能整除 numsDivide 所有元素 等价于 能整除其 GCD（g % v == 0 即 v 是 g 的因子）。
2. 排序后找第一个满足条件的元素，其下标即删除次数。
3. 注意方向：是 `g % v == 0`（v 整除 g），而不是 `v % g == 0`。

## 相关题目

- [2343. 查询字符串最小裁剪数](https://leetcode.com/problems/query-kth-smallest-trimmed-number/)
- [1979. 找出数组的最大公约数](https://leetcode.com/problems/find-greatest-common-divisor-of-array/)
