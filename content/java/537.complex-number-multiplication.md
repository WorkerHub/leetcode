---
title: 537.complex-number-multiplication
date: 2026-04-10 10:00:00
categories: medium
tags: [数学, 字符串, 模拟]
---

## 题目描述

给定两个字符串形式的复数 `num1` 和 `num2`，返回它们相乘的结果（字符串形式）。

复数格式为 `"实部+虚部i"`。

**示例：**
```
输入: num1 = "1+1i", num2 = "1+1i"
输出: "0+2i"
```

## 解题思路

解析复数字符串提取实部虚部，套用公式：实部 = a1*a2 - b1*b2，虚部 = a1*b2 + a2*b1。

## Java 代码实现

```java
class Solution {
    public String complexNumberMultiply(String num1, String num2) {
        // 解析第一个复数
        String[] parts1 = num1.substring(0, num1.length() - 1).split("\\+");
        int a1 = Integer.parseInt(parts1[0]);
        int b1 = Integer.parseInt(parts1[1]);

        // 解析第二个复数
        String[] parts2 = num2.substring(0, num2.length() - 1).split("\\+");
        int a2 = Integer.parseInt(parts2[0]);
        int b2 = Integer.parseInt(parts2[1]);

        // 复数乘法公式
        int real = a1 * a2 - b1 * b2;
        int imag = a1 * b2 + a2 * b1;

        return real + "+" + imag + "i";
    }
}
```

## 复杂度分析

- **时间复杂度**：O(1)
- **空间复杂度**：O(1)

## 关键点总结

1. **字符串解析**：去掉 `i`，按 `+` 分割
2. **乘法公式**：(a1+b1i)(a2+b2i) = (a1a2-b1b2) + (a1b2+a2b1)i
3. **Java split**：需要转义 `\\+`
