---
title: 733.flood-fill
date: 2026-04-09 11:20:00
categories: easy
tags: [深度优先搜索, 广度优先搜索, 数组, 矩阵]
---

## 题目描述

给定一个图像 image、起点坐标 (sr, sc) 和新颜色 color，从起点开始对上下左右相邻且与起点原始颜色相同的像素进行填充。

**示例：**
```
输入：image = [[1,1,1],[1,1,0],[1,0,1]], sr = 1, sc = 1, color = 2
输出：[[2,2,2],[2,2,0],[2,0,1]]
```

## 解题思路

经典 DFS/BFS 洪水填充：从起点出发，递归访问四个方向，将颜色相同的像素染成新颜色。注意原色与新色相同时直接返回。

## Java 代码实现

```java
class Solution {
    public int[][] floodFill(int[][] image, int sr, int sc, int color) {
        int originalColor = image[sr][sc];
        if (originalColor == color) return image;
        dfs(image, sr, sc, originalColor, color);
        return image;
    }

    private void dfs(int[][] image, int r, int c, int original, int color) {
        if (r < 0 || r >= image.length || c < 0 || c >= image[0].length) return;
        if (image[r][c] != original) return;

        image[r][c] = color;
        dfs(image, r + 1, c, original, color);
        dfs(image, r - 1, c, original, color);
        dfs(image, r, c + 1, original, color);
        dfs(image, r, c - 1, original, color);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(m × n)
- **空间复杂度**：O(m × n)，递归栈空间

## 关键点总结

1. **避免死循环**：原色等于新色时直接返回
2. **四方向遍历**：上下左右递归
3. **边界检查**：防止数组越界

## 相关题目

- [200. Number of Islands](https://leetcode.com/problems/number-of-islands/)
- [695. Max Area of Island](https://leetcode.com/problems/max-area-of-island/)
