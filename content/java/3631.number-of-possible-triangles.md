---
title: 3631.number-of-possible-triangles
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 二分查找, 排序]
---

## 题目描述

给定整数数组 `nums`，返回可以构成三角形的三元组 `(i, j, k)`（`i < j < k`）的数量。

## Java 代码实现

```java
import java.util.Arrays;

class Solution {
    public int triangleNumber(int[] nums) {
        Arrays.sort(nums);
        int n = nums.length;
        int count = 0;

        // 固定最大边 nums[k]，双指针统计满足两小边之和 > 最大边的对数
        for (int k = 2; k < n; k++) {
            int left = 0, right = k - 1;
            while (left < right) {
                if (nums[left] + nums[right] > nums[k]) {
                    // [left, right-1] 都与 right 满足条件
                    count += right - left;
                    right--;
                } else {
                    left++;
                }
            }
        }
        return count;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n^2)，固定最大边后双指针扫描
- 空间复杂度：O(1)

## 关键点总结

1. 排序后只需验证两小边之和大于最大边
2. 固定最大边，双指针统计有效对数：`nums[l] + nums[r] > target` 时贡献 `r - l` 对
3. 双指针满足条件则移动右指针，否则移动左指针

## 相关题目

- [611. Valid Triangle Number](https://leetcode.com/problems/valid-triangle-number/)
- [15. 3Sum](https://leetcode.com/problems/3sum/)
