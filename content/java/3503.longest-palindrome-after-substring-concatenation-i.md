---
title: 3503.longest-palindrome-after-substring-concatenation-i
date: 2026-04-13 10:00:00
categories: medium
tags: [字符串, 动态规划, 双指针]
---

## 题目描述

给你字符串 `s` 和 `t`（长度 <= 30），从 `s` 选一个子串，从 `t` 选一个子串，拼接后求能组成的最长回文串的长度。

## 解题思路

数据规模小，枚举 `s` 和 `t` 所有子串组合，用双指针检验拼接结果是否为回文，取最大长度。同时考虑单独用 `s` 或 `t` 的子串构成回文的情况。

## Java 代码实现

```java
class Solution {
    public int longestPalindrome(String s, String t) {
        int n = s.length(), m = t.length();
        int res = 1;

        // 考虑 s 的子串本身是回文
        for (int i = 0; i < n; i++)
            for (int j = i; j < n; j++)
                if (isPalin(s, i, j)) res = Math.max(res, j - i + 1);

        // 考虑 t 的子串本身是回文
        for (int i = 0; i < m; i++)
            for (int j = i; j < m; j++)
                if (isPalin(t, i, j)) res = Math.max(res, j - i + 1);

        // 枚举 s 子串 + t 子串拼接
        for (int i = 0; i < n; i++) {
            for (int j = i; j < n; j++) {
                for (int p = 0; p < m; p++) {
                    for (int q = p; q < m; q++) {
                        String combined = s.substring(i, j + 1) + t.substring(p, q + 1);
                        if (isPalinStr(combined))
                            res = Math.max(res, combined.length());
                    }
                }
            }
        }

        return res;
    }

    private boolean isPalin(String s, int l, int r) {
        while (l < r) {
            if (s.charAt(l) != s.charAt(r)) return false;
            l++; r--;
        }
        return true;
    }

    private boolean isPalinStr(String s) {
        int l = 0, r = s.length() - 1;
        while (l < r) {
            if (s.charAt(l) != s.charAt(r)) return false;
            l++; r--;
        }
        return true;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n^2 * m^2 * (n + m))
- 空间复杂度：O(n + m)

## 关键点总结

1. 数据规模小（<= 30），枚举所有子串组合
2. 同时考虑单独子串和拼接子串两种情况
3. 结果初始值为 1（单字符必然是回文）

## 相关题目

- [5. Longest Palindromic Substring](https://leetcode.com/problems/longest-palindromic-substring/)
- [3504. Longest Palindrome After Substring Concatenation II](https://leetcode.com/problems/longest-palindrome-after-substring-concatenation-ii/)
