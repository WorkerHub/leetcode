---
title: 1743.restore-the-array-from-adjacent-pairs
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 哈希表, 图]
---

## 题目描述

给你数组中每对相邻元素 `adjacentPairs[i] = [u, v]`，还原原始数组（所有元素两两不同）。

## 解题思路

将相邻关系建成无向图，度为 1 的节点是链的端点。从端点出发迭代遍历（记录前一节点避免回头），依次还原数组。

## Java 代码实现

```java
import java.util.*;

class Solution {
    public int[] restoreArray(int[][] adjacentPairs) {
        // 建立邻接表
        Map<Integer, List<Integer>> graph = new HashMap<>();
        for (int[] pair : adjacentPairs) {
            graph.computeIfAbsent(pair[0], k -> new ArrayList<>()).add(pair[1]);
            graph.computeIfAbsent(pair[1], k -> new ArrayList<>()).add(pair[0]);
        }

        // 找端点（度为 1 的节点）
        int start = 0;
        for (Map.Entry<Integer, List<Integer>> entry : graph.entrySet()) {
            if (entry.getValue().size() == 1) {
                start = entry.getKey();
                break;
            }
        }

        int n = adjacentPairs.length + 1;
        int[] result = new int[n];
        result[0] = start;

        // 从端点迭代还原数组
        int prev = Integer.MIN_VALUE, cur = start;
        for (int i = 1; i < n; i++) {
            for (int neighbor : graph.get(cur)) {
                if (neighbor != prev) {
                    result[i] = neighbor;
                    prev = cur;
                    cur = neighbor;
                    break;
                }
            }
        }

        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)
- **空间复杂度**：O(n)

## 关键点总结

1. 相邻关系构成一条链，度为 1 的是端点
2. 从端点出发记录前一节点，迭代还原

## 相关题目

- [LeetCode 1059. 从始点到终点的所有路径](https://leetcode.com/problems/all-paths-from-source-lead-to-destination/)
