---
title: 697.degree-of-an-array
date: 2026-04-10 10:00:00
categories: easy
tags: [数组, 哈希表]
---

## 题目描述

数组的度为出现频数最大值。找到与原数组具有相同度的最短连续子数组长度。

**示例：**
```
输入：nums = [1,2,2,3,1]
输出：2（子数组 [2,2]）
```

## 解题思路

记录每个数字的出现次数、首次和末次出现位置。找到度最大的元素中，末位-首位+1 最小的。

## Java 代码实现

```java
class Solution {
    public int findShortestSubArray(int[] nums) {
        Map<Integer, Integer> count = new HashMap<>();
        Map<Integer, Integer> first = new HashMap<>();
        Map<Integer, Integer> last = new HashMap<>();

        for (int i = 0; i < nums.length; i++) {
            count.merge(nums[i], 1, Integer::sum);
            if (!first.containsKey(nums[i])) {
                first.put(nums[i], i);
            }
            last.put(nums[i], i);
        }

        int degree = Collections.max(count.values());
        int minLen = nums.length;

        for (Map.Entry<Integer, Integer> entry : count.entrySet()) {
            if (entry.getValue() == degree) {
                int len = last.get(entry.getKey()) - first.get(entry.getKey()) + 1;
                minLen = Math.min(minLen, len);
            }
        }
        return minLen;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)
- **空间复杂度**：O(n)

## 关键点总结

1. **三个 HashMap**：记录频次、首位置、末位置
2. **找度**：频次最大值
3. **最短跨度**：在所有最高频元素中取最短

## 相关题目

- [53. Maximum Subarray](https://leetcode.com/problems/maximum-subarray/)
- [169. Majority Element](https://leetcode.com/problems/majority-element/)
