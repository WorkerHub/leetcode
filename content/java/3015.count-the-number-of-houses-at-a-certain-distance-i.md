---
title: 3015.count-the-number-of-houses-at-a-certain-distance-i
date: 2026-04-13 10:00:00
categories: medium
tags: [图, BFS, 数组]
---

## 题目描述

n 所房屋连成一链（1-2-...-n），另加一条边 x-y，返回数组 result，result[k-1] 为恰好距离 k 的有序房屋对数。

## 解题思路

建图后对每个节点做 BFS，统计到所有其他节点的最短距离，累加到结果数组。

## Java 代码实现

```java
class Solution {
    public int[] countOfPairs(int n, int x, int y) {
        // 建邻接表（节点 1..n）
        List<List<Integer>> graph = new ArrayList<>();
        for (int i = 0; i <= n; i++) graph.add(new ArrayList<>());
        for (int i = 1; i < n; i++) {
            graph.get(i).add(i + 1);
            graph.get(i + 1).add(i);
        }
        if (x != y) {
            graph.get(x).add(y);
            graph.get(y).add(x);
        }
        int[] result = new int[n];
        // 对每个节点做 BFS
        for (int start = 1; start <= n; start++) {
            int[] dist = new int[n + 1];
            Arrays.fill(dist, -1);
            dist[start] = 0;
            Queue<Integer> queue = new LinkedList<>();
            queue.offer(start);
            while (!queue.isEmpty()) {
                int node = queue.poll();
                for (int nb : graph.get(node)) {
                    if (dist[nb] == -1) {
                        dist[nb] = dist[node] + 1;
                        queue.offer(nb);
                    }
                }
            }
            for (int end = 1; end <= n; end++) {
                if (end != start && dist[end] >= 1) {
                    result[dist[end] - 1]++;
                }
            }
        }
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n²)
- **空间复杂度**：O(n²)

## 关键点总结

1. 图为链 + 一条额外边，BFS 即可求最短路
2. 有序对：(i,j) 和 (j,i) 分别计数
3. n ≤ 100，O(n²) 完全够用

## 相关题目

- [3017. Count the Number of Houses at a Certain Distance II](https://leetcode.com/problems/count-the-number-of-houses-at-a-certain-distance-ii/)
