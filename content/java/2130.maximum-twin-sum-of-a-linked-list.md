---
title: 2130.maximum-twin-sum-of-a-linked-list
date: 2026-04-13 10:00:00
categories: medium
tags: [链表, 双指针, 栈]
---

## 题目描述

偶数长度链表中，第i个节点和第n-1-i个节点是孪生节点。返回最大孪生和。

## 解题思路

快慢指针找中点，反转后半部分，双指针从两端遍历配对求最大和。

## Java 代码实现

```java
class Solution {
    public int pairSum(ListNode head) {
        // 快慢指针找中点
        ListNode slow = head, fast = head;
        while (fast != null && fast.next != null) {
            slow = slow.next;
            fast = fast.next.next;
        }
        
        // 反转后半部分
        ListNode prev = null, cur = slow;
        while (cur != null) {
            ListNode next = cur.next;
            cur.next = prev;
            prev = cur;
            cur = next;
        }
        
        // 双指针配对求最大和
        int ans = 0;
        ListNode p1 = head, p2 = prev;
        while (p2 != null) {
            ans = Math.max(ans, p1.val + p2.val);
            p1 = p1.next;
            p2 = p2.next;
        }
        return ans;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(1)

## 关键点总结

1. **快慢指针找中点**：fast到底时slow在中间
2. **反转后半**：标准链表反转操作
3. **双指针配对**：前半和反转后的后半同步遍历

## 相关题目

- [876. Middle of the Linked List](https://leetcode.com/problems/middle-of-the-linked-list/)
- [206. Reverse Linked List](https://leetcode.com/problems/reverse-linked-list/)
- [234. Palindrome Linked List](https://leetcode.com/problems/palindrome-linked-list/)
