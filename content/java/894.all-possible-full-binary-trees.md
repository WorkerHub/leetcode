---
title: 894.all-possible-full-binary-trees
date: 2026-04-09 23:16:03
categories: medium
tags: [树, 递归, 记忆化搜索, 动态规划, 二叉树]
---

## 题目描述

满二叉树中每个节点有 0 或 2 个子节点。返回包含 n 个节点的所有可能满二叉树。

**示例：**
```
输入：n = 7
输出：所有包含 7 个节点的满二叉树
```

## 解题思路

递归构造：n 为偶数无解；否则枚举左子树大小 i（奇数），右子树大小 n-1-i，递归构造。

## Java 代码实现

```java
class Solution {
    private Map<Integer, List<TreeNode>> memo = new HashMap<>();

    public List<TreeNode> allPossibleFBT(int n) {
        if (n % 2 == 0) return new ArrayList<>();
        if (memo.containsKey(n)) return memo.get(n);

        List<TreeNode> result = new ArrayList<>();
        if (n == 1) {
            result.add(new TreeNode(0));
        } else {
            for (int i = 1; i < n; i += 2) {
                List<TreeNode> lefts = allPossibleFBT(i);
                List<TreeNode> rights = allPossibleFBT(n - 1 - i);
                for (TreeNode left : lefts) {
                    for (TreeNode right : rights) {
                        TreeNode root = new TreeNode(0);
                        root.left = left;
                        root.right = right;
                        result.add(root);
                    }
                }
            }
        }

        memo.put(n, result);
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(2^(n/2))，卡特兰数级别
- **空间复杂度**：O(2^(n/2))

## 关键点总结

1. **满二叉树**：节点数必须为奇数
2. **递归枚举**：左右子树大小之和 = n-1
3. **记忆化**：避免重复计算

## 相关题目

- [95. Unique Binary Search Trees II](https://leetcode.com/problems/unique-binary-search-trees-ii/)
