---
title: 3121.count-the-number-of-special-characters-ii
date: 2026-04-13 10:00:00
categories: medium
tags: [字符串, 哈希表]
---

## 题目描述

特殊字母：小写最后出现位置严格小于大写最早出现位置。统计特殊字母数。

## 解题思路

记录每个字母小写最后位置和大写最早位置，比较即可。

## Java 代码实现

```java
class Solution {
    public int numberOfSpecialChars(String word) {
        int[] lastLower = new int[26];
        int[] firstUpper = new int[26];
        Arrays.fill(lastLower, -1);
        Arrays.fill(firstUpper, -1);

        for (int i = 0; i < word.length(); i++) {
            char c = word.charAt(i);
            if (Character.isLowerCase(c)) {
                lastLower[c - 'a'] = i;
            } else {
                if (firstUpper[c - 'A'] == -1) firstUpper[c - 'A'] = i;
            }
        }

        int ans = 0;
        for (int i = 0; i < 26; i++) {
            if (lastLower[i] != -1 && firstUpper[i] != -1 && lastLower[i] < firstUpper[i]) {
                ans++;
            }
        }
        return ans;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(1)

## 关键点总结

1. **小写最后位置<大写最早位置**：满足"所有小写在大写之前"
2. **一次遍历记录位置**
3. **26个字母逐一检查**

## 相关题目

- [3120. Count the Number of Special Characters I](https://leetcode.com/problems/count-the-number-of-special-characters-i/)
