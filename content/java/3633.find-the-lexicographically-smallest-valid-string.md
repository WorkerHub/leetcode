---
title: 3633.find-the-lexicographically-smallest-valid-string
date: 2026-04-13 10:00:00
categories: medium
tags: [字符串, 贪心, 构造]
---

## 题目描述

给定字符串 `s` 和整数 `k`，每个字符可替换为字母表中排在其之前最多 `k` 位的字符，返回字典序最小的结果字符串。

## Java 代码实现

```java
class Solution {
    public String getSmallestString(String s, int k) {
        StringBuilder sb = new StringBuilder();
        for (char c : s.toCharArray()) {
            // 尽量向前移动 k 位，但不能小于 'a'
            sb.append((char) Math.max(c - k, 'a'));
        }
        return sb.toString();
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)

## 关键点总结

1. 每个字符独立贪心处理
2. 尽量减少 k，但不能低于 `'a'`（`Math.max(c - k, 'a')`）

## 相关题目

- [2697. Lexicographically Smallest Palindrome](https://leetcode.com/problems/lexicographically-smallest-palindrome/)
- [1859. Sorting the Sentence](https://leetcode.com/problems/sorting-the-sentence/)
