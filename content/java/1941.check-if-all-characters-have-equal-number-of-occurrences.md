---
title: 1941.check-if-all-characters-have-equal-number-of-occurrences
date: 2026-04-13 10:00:00
categories: easy
tags: [哈希表, 字符串, 计数]
---

## 题目描述

给你字符串 `s`，若所有字符出现次数全部相同则返回 `true`，否则返回 `false`。

## 解题思路

统计每个字符的出现次数，判断所有频次是否相同（第一个频次与其余相等）。

## Java 代码实现

```java
class Solution {
    public boolean areOccurrencesEqual(String s) {
        int[] freq = new int[26];
        for (char c : s.toCharArray()) freq[c - 'a']++;
        int target = -1;
        for (int f : freq) {
            if (f == 0) continue;
            if (target == -1) target = f;
            else if (f != target) return false;
        }
        return true;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)
- **空间复杂度**：O(1)

## 关键点总结

1. `int[26]` 统计各字符频次
2. 取第一个非零频次作为基准，后续若不同则返回 false
3. 跳过频次为 0 的字符（未出现的字符）

## 相关题目

- [LeetCode 451. Sort Characters By Frequency](https://leetcode.com/problems/sort-characters-by-frequency/)
