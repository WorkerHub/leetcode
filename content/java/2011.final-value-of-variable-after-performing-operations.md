---
title: 2011.final-value-of-variable-after-performing-operations
date: 2026-04-13 10:00:00
categories: easy
tags: [数组, 字符串, 模拟]
---

## 题目描述

给定操作字符串数组（`"++X"`、`"X++"`、`"--X"`、`"X--"`），变量 X 初始为 0，返回执行所有操作后 X 的最终值。

## 解题思路

四种操作本质只有两类：含 `+` 则 +1，含 `-` 则 -1，统计差值即可。

## Java 代码实现

### 方法一：直接统计（推荐）

```java
class Solution {
    public int finalValueAfterOperations(String[] operations) {
        int result = 0;
        for (String op : operations) {
            if (op.contains("+")) {
                result++; // ++X 或 X++ 均使X加1
            } else {
                result--; // --X 或 X-- 均使X减1
            }
        }
        return result;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)

## 关键点总结

1. 四种操作本质只有两类，只需检查是否含 `'+'`
2. 最终结果 = 加法次数 - 减法次数

## 相关题目

- [1720. Decode XORed Array](https://leetcode.com/problems/decode-xored-array/)
