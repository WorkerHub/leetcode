---
title: 2260.minimum-consecutive-cards-to-pick-up
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 哈希表, 滑动窗口]
---

## 题目描述

返回包含一对相同卡牌所需的最少连续卡牌数，不存在返回-1。

## 解题思路

哈希表记录每个值最近出现的位置，遇到重复值时计算窗口大小i-last+1，取最小值。

## Java 代码实现

```java
class Solution {
    public int minimumCardPickup(int[] cards) {
        Map<Integer, Integer> last = new HashMap<>();
        int ans = Integer.MAX_VALUE;

        for (int i = 0; i < cards.length; i++) {
            if (last.containsKey(cards[i])) {
                ans = Math.min(ans, i - last.get(cards[i]) + 1);
            }
            last.put(cards[i], i);
        }
        return ans == Integer.MAX_VALUE ? -1 : ans;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(N)

## 关键点总结

1. **记录最近位置**：每次遇到重复值计算窗口大小
2. **窗口大小=i-last+1**：包含两端
3. **更新last为当前位置**：保证窗口最短

## 相关题目

- [3. Longest Substring Without Repeating Characters](https://leetcode.com/problems/longest-substring-without-repeating-characters/)
