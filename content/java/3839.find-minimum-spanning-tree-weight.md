---
title: 3839.find-minimum-spanning-tree-weight
date: 2026-04-13 10:00:00
categories: hard
tags: [图, 最小生成树]
---

## 题目描述

给定 `n` 节点无向加权图，返回最小生成树的总权重，图不连通返回 -1。

## Java 代码实现

```java
import java.util.*;

class Solution {
    private int[] parent, rank;

    public long minimumSpanningTree(int n, int[][] edges) {
        parent = new int[n]; rank = new int[n];
        for (int i = 0; i < n; i++) parent[i] = i;

        // Kruskal：按边权排序，并查集合并
        Arrays.sort(edges, (a, b) -> a[2] - b[2]);
        long total = 0;
        int used = 0;
        for (int[] e : edges) {
            if (union(e[0], e[1])) {
                total += e[2];
                if (++used == n - 1) return total;
            }
        }
        return used == n - 1 ? total : -1;
    }

    private int find(int x) {
        if (parent[x] != x) parent[x] = find(parent[x]);
        return parent[x];
    }

    private boolean union(int x, int y) {
        int px = find(x), py = find(y);
        if (px == py) return false;
        if (rank[px] < rank[py]) { int t = px; px = py; py = t; }
        parent[py] = px;
        if (rank[px] == rank[py]) rank[px]++;
        return true;
    }
}
```

## 复杂度分析

- 时间复杂度：O(m log m)
- 空间复杂度：O(n)

## 关键点总结

1. Kruskal 算法：按边权排序，用并查集检测环
2. MST 恰好包含 n-1 条边，若不足则图不连通返回 -1
3. 并查集使用路径压缩和按秩合并保证效率

## 相关题目

- [1584. Min Cost to Connect All Points](https://leetcode.com/problems/min-cost-to-connect-all-points/)
- [1135. Connecting Cities With Minimum Cost](https://leetcode.com/problems/connecting-cities-with-minimum-cost/)
