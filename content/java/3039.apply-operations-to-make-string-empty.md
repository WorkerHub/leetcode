---
title: 3039.apply-operations-to-make-string-empty
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 哈希表, 排序, 计数]
---

## 题目描述

每轮删除字符串中所有最小字符，返回最后一轮操作前的字符串。

## 解题思路

出现次数最多的字符最后被删除。找所有最大频率字符，保留它们在原串中的出现（按原始顺序）。

## Java 代码实现

```java
class Solution {
    public String lastNonEmptyString(String s) {
        int[] freq = new int[26];
        for (char c : s.toCharArray()) freq[c - 'a']++;

        int maxFreq = 0;
        for (int f : freq) maxFreq = Math.max(maxFreq, f);

        // 找所有最大频率字符
        boolean[] isMax = new boolean[26];
        for (int i = 0; i < 26; i++) {
            if (freq[i] == maxFreq) isMax[i] = true;
        }

        // 保留这些字符的最后一次出现（倒序收集）
        StringBuilder sb = new StringBuilder();
        boolean[] seen = new boolean[26];
        for (int i = s.length() - 1; i >= 0; i--) {
            int c = s.charAt(i) - 'a';
            if (isMax[c] && !seen[c]) {
                sb.append(s.charAt(i));
                seen[c] = true;
            }
        }
        return sb.reverse().toString();
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(N)

## 关键点总结

1. **最大频率字符最后被删除**
2. **每个最大频率字符取最后一次出现**
3. **倒序收集后反转保持原始顺序**

## 相关题目

- [1647. Minimum Deletions to Make Character Frequencies Unique](https://leetcode.com/problems/minimum-deletions-to-make-character-frequencies-unique/)
