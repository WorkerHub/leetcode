---
title: 3582.generate-tag-for-video-caption
date: 2026-04-13 10:00:00
categories: easy
tags: [字符串, 模拟]
---

## 题目描述

给定视频标题字符串 `caption`，生成标签：以 `'#'` 开头，首词全小写，后续词首字母大写（驼峰），总长度超过 100 则截断。

## Java 代码实现

```java
class Solution {
    public String generateTag(String caption) {
        String[] words = caption.trim().split("\\s+");
        StringBuilder sb = new StringBuilder("#");

        for (int i = 0; i < words.length; i++) {
            String w = words[i].toLowerCase();
            if (i == 0) {
                sb.append(w);  // 首词全小写
            } else {
                // 后续词首字母大写
                sb.append(Character.toUpperCase(w.charAt(0))).append(w.substring(1));
            }
            if (sb.length() >= 100) break;  // 提前截断
        }

        // 截断到 100 字符（含 '#'）
        return sb.length() > 100 ? sb.substring(0, 100) : sb.toString();
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)，遍历字符串
- 空间复杂度：O(n)，结果字符串

## 关键点总结

1. 驼峰命名：首词全小写，后续词首字母大写其余小写
2. 截断是在加 `'#'` 之后，最终标签最多 100 个字符
3. 使用 `toLowerCase()` 统一处理大小写后再调整首字母

## 相关题目

- [1816. Truncate Sentence](https://leetcode.com/problems/truncate-sentence/)
- [2315. Count Asterisks](https://leetcode.com/problems/count-asterisks/)
