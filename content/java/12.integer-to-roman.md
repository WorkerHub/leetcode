---
title: 12.integer-to-roman
date: 2026-04-09 10:03:00
categories: medium
tags: [哈希表, 数学, 字符串]
---

## 题目描述

罗马数字包含以下七种字符：`I`，`V`，`X`，`L`，`C`，`D` 和 `M`。

```
字符          数值
I             1
V             5
X             10
L             50
C             100
D             500
M             1000
```

通常情况下，罗马数字中小的数字在大的数字的右边。但也存在特例，例如 4 不写做 `IIII`，而是 `IV`。

给你一个整数，将其转为罗马数字。

**示例 1：**
```
输入：num = 3
输出："III"
```

**示例 2：**
```
输入：num = 58
输出："LVIII"
```

**示例 3：**
```
输入：num = 1994
输出："MCMXCIV"
解释：M = 1000, CM = 900, XC = 90, IV = 4
```

**提示：**
- `1 <= num <= 3999`

## 解题思路

核心思想：从大到小枚举所有可能的罗马数字（包括特殊组合），依次减去对应的值。贪心策略：每次选择不超过 num 的最大罗马数字。

## Java 代码实现

### 方法一：贪心法（推荐）

```java
class Solution {
    public String intToRoman(int num) {
        // 按从大到小的顺序列出所有可能的值和符号
        int[] values = {1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1};
        String[] symbols = {"M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"};
        
        StringBuilder result = new StringBuilder();
        
        for (int i = 0; i < values.length; i++) {
            // 当前值能用几次
            while (num >= values[i]) {
                num -= values[i];
                result.append(symbols[i]);
            }
        }
        
        return result.toString();
    }
}
```

### 方法二：硬编码法

```java
class Solution {
    public String intToRoman(int num) {
        String[] thousands = {"", "M", "MM", "MMM"};
        String[] hundreds = {"", "C", "CC", "CCC", "CD", "D", "DC", "DCC", "DCCC", "CM"};
        String[] tens = {"", "X", "XX", "XXX", "XL", "L", "LX", "LXX", "LXXX", "XC"};
        String[] ones = {"", "I", "II", "III", "IV", "V", "VI", "VII", "VIII", "IX"};
        
        return thousands[num / 1000] +
               hundreds[(num % 1000) / 100] +
               tens[(num % 100) / 10] +
               ones[num % 10];
    }
}
```

## 复杂度分析

- 时间复杂度：O(1)，循环次数有固定上限
- 空间复杂度：O(1)

## 关键点总结

1. **包含特殊组合**：6 个减法组合（IV, IX, XL, XC, CD, CM）必须包含在映射表中
2. **贪心策略**：从大到小选择，每次选最大的可用符号
3. **顺序很重要**：必须按值从大到小排列

## 易错点

1. **遗漏特殊组合**：必须包含 4, 9, 40, 90, 400, 900
2. **顺序错误**：必须从大到小枚举

## 相关题目

- [13. 罗马数字转整数](https://leetcode.com/problems/roman-to-integer/)
