---
title: 1110.delete-nodes-and-return-forest
date: 2026-04-10 15:30:00
categories: medium
tags: [树, 深度优先搜索, 递归]
---

## 题目描述

给定二叉树和待删除节点值列表 `to_delete`，删除这些节点后返回森林（所有不相交的树的根节点列表）。

**示例：**
```
输入：root = [1,2,3,4,5,6,7], to_delete = [3,5]
输出：[[1,2,null,4],[6],[7]]
```

## 解题思路

后序 DFS：递归处理子树，若当前节点被删除则返回 null 断开连接，其子节点若不删除则成为新根加入结果。

## Java 代码实现

```java
class Solution {
    private Set<Integer> toDeleteSet;
    private List<TreeNode> forest;

    public List<TreeNode> delNodes(TreeNode root, int[] to_delete) {
        toDeleteSet = new HashSet<>();
        for (int val : to_delete) toDeleteSet.add(val);
        forest = new ArrayList<>();

        root = dfs(root);
        if (root != null) forest.add(root);

        return forest;
    }

    private TreeNode dfs(TreeNode node) {
        if (node == null) return null;

        // 后序遍历：先处理子树
        node.left = dfs(node.left);
        node.right = dfs(node.right);

        // 当前节点需要删除
        if (toDeleteSet.contains(node.val)) {
            // 子节点成为新的根
            if (node.left != null) forest.add(node.left);
            if (node.right != null) forest.add(node.right);
            return null; // 断开连接
        }

        return node;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)
- **空间复杂度**：O(n)

## 关键点总结

1. **后序遍历**：先处理子树再处理当前节点
2. **返回 null**：删除节点时返回 null 自动断开父子连接
3. **新根识别**：被删节点的非空子节点成为新树的根

## 相关题目

- [814. Binary Tree Pruning](https://leetcode.com/problems/binary-tree-pruning/)
- [669. Trim a Binary Search Tree](https://leetcode.com/problems/trim-a-binary-search-tree/)
