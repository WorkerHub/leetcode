---
title: 3195.find-the-minimum-area-to-cover-all-ones-i
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 矩阵]
---

## 题目描述

给你二进制矩阵 `grid`，返回包含所有 1 的最小轴对齐矩形的面积（保证至少有一个 1）。

## 解题思路

遍历所有格子，记录所有 1 的行范围 [minR, maxR] 和列范围 [minC, maxC]，面积 = (maxR - minR + 1) × (maxC - minC + 1)。

## Java 代码实现

```java
class Solution {
    public int minimumArea(int[][] grid) {
        int m = grid.length, n = grid[0].length;
        int minR = m, maxR = -1, minC = n, maxC = -1;

        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                if (grid[i][j] == 1) {
                    minR = Math.min(minR, i);
                    maxR = Math.max(maxR, i);
                    minC = Math.min(minC, j);
                    maxC = Math.max(maxC, j);
                }
            }
        }
        return (maxR - minR + 1) * (maxC - minC + 1);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(m × n)
- **空间复杂度**：O(1)

## 关键点总结

1. 找所有 1 的行列边界（最小/最大行列值）
2. 面积 = 行范围长 × 列范围长（均为闭区间，长度 +1）

## 相关题目

- [3197. 包含所有 1 的最小矩形面积 II](https://leetcode.com/problems/find-the-minimum-area-to-cover-all-ones-ii/)
- [1277. 统计全为 1 的正方形子矩阵](https://leetcode.com/problems/count-square-submatrices-with-all-ones/)
