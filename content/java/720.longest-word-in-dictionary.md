---
title: 720.longest-word-in-dictionary
date: 2026-04-09 11:05:00
categories: medium
tags: [字典树, 数组, 哈希表, 字符串, 排序]
---

## 题目描述

返回词典中最长的单词，该单词由词典中其他单词逐步添加一个字母组成。多个答案取字典序最小。

**示例：**
```
输入：words = ["a","banana","app","appl","ap","apply","apple"]
输出："apple"
```

## 解题思路

将所有单词加入 HashSet，遍历每个单词检查其所有前缀是否都在集合中。排序后遍历保证字典序。

## Java 代码实现

```java
class Solution {
    public String longestWord(String[] words) {
        Set<String> wordSet = new HashSet<>(Arrays.asList(words));
        Arrays.sort(words);

        String result = "";
        for (String word : words) {
            boolean valid = true;
            for (int k = 1; k < word.length(); k++) {
                if (!wordSet.contains(word.substring(0, k))) {
                    valid = false;
                    break;
                }
            }
            if (valid && word.length() > result.length()) {
                result = word;
            }
        }
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n × L²)，n 为单词数，L 为平均长度
- **空间复杂度**：O(n × L)

## 关键点总结

1. **前缀检查**：所有前缀都必须存在于词典中
2. **排序**：保证字典序最小
3. **HashSet**：O(1) 查找前缀

## 相关题目

- [208. Implement Trie (Prefix Tree)](https://leetcode.com/problems/implement-trie-prefix-tree/)
- [14. Longest Common Prefix](https://leetcode.com/problems/longest-common-prefix/)
