---
title: 1236.web-crawler
date: 2026-04-11 16:00:00
categories: medium
tags: [深度优先搜索, 广度优先搜索, 图, 哈希表]
---

## 题目描述

给定 startUrl 和 HtmlParser 接口，爬取所有与 startUrl 相同主机名的链接。不爬取重复 URL。

**示例：**
```
startUrl = "http://news.yahoo.com/news/topics/"
输出：所有 news.yahoo.com 下的 URL
```

## 解题思路

BFS/DFS 图遍历：从 startUrl 出发，提取主机名，用 HashSet 记录已访问 URL，只继续爬取同主机名的链接。

## Java 代码实现

```java
class Solution {
    public List<String> crawl(String startUrl, HtmlParser htmlParser) {
        String hostname = getHostname(startUrl);
        Set<String> visited = new HashSet<>();
        visited.add(startUrl);
        
        // BFS
        Queue<String> queue = new LinkedList<>();
        queue.offer(startUrl);
        
        while (!queue.isEmpty()) {
            String url = queue.poll();
            for (String next : htmlParser.getUrls(url)) {
                if (!visited.contains(next) && getHostname(next).equals(hostname)) {
                    visited.add(next);
                    queue.offer(next);
                }
            }
        }
        
        return new ArrayList<>(visited);
    }
    
    private String getHostname(String url) {
        // 去掉 "http://"，取第一个 "/" 之前的部分
        String withoutProtocol = url.substring(7);
        int slashIdx = withoutProtocol.indexOf('/');
        return slashIdx == -1 ? withoutProtocol : withoutProtocol.substring(0, slashIdx);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(V + E)，V 为 URL 数，E 为链接数
- **空间复杂度**：O(V)

## 关键点总结

1. **提取主机名**：去掉 "http://"，取第一个 "/" 前的字符串
2. **去重**：HashSet 记录已访问 URL
3. **同域名过滤**：只爬取与 startUrl 相同主机名的链接
4. **BFS/DFS 均可**：图遍历的标准模板

## 相关题目

- [1242. Web Crawler Multithreaded](https://leetcode.com/problems/web-crawler-multithreaded/)
