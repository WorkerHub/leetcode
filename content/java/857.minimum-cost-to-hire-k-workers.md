---
title: 857.minimum-cost-to-hire-k-workers
date: 2026-04-09 23:16:03
categories: hard
tags: [贪心, 堆, 排序, 数组]
---

## 题目描述

有 n 名工人。给定两个数组 quality 和 wage ，其中，quality[i] 表示第 i 名工人的工作质量，其最低期望工资为 wage[i] 。

现在我们想雇佣 k 名工人组成一个工资组。在雇佣一组 k 名工人时，我们必须按照下述规则向他们支付工资：

1. 对工资组中的每名工人，应当按其工作质量与同组其他工人的工作质量的比例来支付工资。
2. 工资组中的每名工人至少应当得到他们的最低期望工资。

给定整数 k ，返回组成满足上述条件的付费群体所需的最小金额。答案与实际值误差在 10^-5 以内视为正确。

**示例 1：**
```
输入：quality = [10,20,5], wage = [70,50,30], k = 2
输出：105.00000
解释：我们向 0 号工人支付 70，向 2 号工人支付 35。
```

**示例 2：**
```
输入：quality = [3,1,10,10,1], wage = [4,8,2,2,7], k = 3
输出：30.66667
解释：我们向 0 号工人支付 4，向 2 号和 3 号各支付 13.33333。
```

**提示：**
- n == quality.length == wage.length
- 1 <= k <= n <= 10^4
- 1 <= quality[i], wage[i] <= 10^4

## 解题思路

这是一道**贪心 + 堆**的经典题目。

**核心理解：**
1. 工资按质量比例分配：如果比例是 r，工人 i 的工资是 `r * quality[i]`
2. 每个工人有最低期望：`r * quality[i] >= wage[i]`，即 `r >= wage[i] / quality[i]`
3. 要满足所有人，r 必须取最大的 `wage[i] / quality[i]`

**关键观察：**
- 定义每个工人的"性价比"：`ratio[i] = wage[i] / quality[i]`
- 如果选择了一组工人，比例 r = max(ratio)（组内最大的比例）
- 总工资 = r × sum(quality)
- 要最小化总工资，应该：
  1. 固定一个工人的 ratio 作为 r
  2. 选择 ratio ≤ r 的工人中，quality 最小的 k-1 个

**算法流程：**
1. 计算每个工人的 ratio，并按 ratio 排序
2. 枚举每个工人作为 ratio 最大的那个（队长）
3. 从前面的工人中选择 quality 最小的 k-1 个
4. 使用最大堆维护前 k-1 个 quality 最小的工人
5. 计算总工资并更新最小值

## Java 代码实现

### 方法一：贪心 + 最大堆（推荐）

```java
class Solution {
    public double mincostToHireWorkers(int[] quality, int[] wage, int k) {
        int n = quality.length;
        // 计算每个工人的性价比，并按性价比排序
        double[][] workers = new double[n][2]; // [ratio, quality]
        for (int i = 0; i < n; i++) {
            workers[i][0] = (double) wage[i] / quality[i];
            workers[i][1] = quality[i];
        }
        Arrays.sort(workers, (a, b) -> Double.compare(a[0], b[0]));

        double result = Double.MAX_VALUE;
        int qualitySum = 0; // 当前选中工人的质量总和
        // 最大堆，存储 quality（以便移除最大值）
        PriorityQueue<Integer> maxHeap = new PriorityQueue<>(Collections.reverseOrder());

        for (double[] worker : workers) {
            double ratio = worker[0];
            int q = (int) worker[1];

            // 将当前工人加入堆
            maxHeap.offer(q);
            qualitySum += q;

            // 如果人数超过 k，移除 quality 最大的
            if (maxHeap.size() > k) {
                qualitySum -= maxHeap.poll();
            }

            // 如果刚好 k 个人，计算总工资
            if (maxHeap.size() == k) {
                result = Math.min(result, ratio * qualitySum);
            }
        }

        return result;
    }
}
```

**代码说明：**
- 按 ratio 排序，枚举每个工人作为"队长"（ratio 最大的）
- 用最大堆维护 quality，保留最小的 k 个
- 队长确定后，总工资 = ratio × qualitySum

## 复杂度分析

- **时间复杂度**：O(n log n)，排序 O(n log n) + 堆操作 O(n log k)。
- **空间复杂度**：O(n)，存储工人信息和堆。

## 图解示例

**示例 1**：`quality = [10,20,5], wage = [70,50,30], k = 2`

```
工人信息：
工人 0: quality=10, wage=70, ratio=7.0
工人 1: quality=20, wage=50, ratio=2.5
工人 2: quality=5,  wage=30, ratio=6.0

按 ratio 排序：
工人 1: ratio=2.5, quality=20
工人 2: ratio=6.0, quality=5
工人 0: ratio=7.0, quality=10

枚举过程：
1. 工人 1: ratio=2.5, quality=20
   堆=[20], qualitySum=20, 人数=1 < k

2. 工人 2: ratio=6.0, quality=5
   堆=[20, 5], qualitySum=25, 人数=2 = k
   成本 = 6.0 × 25 = 150

3. 工人 0: ratio=7.0, quality=10
   堆=[20, 10, 5] → 移除20 → [10, 5]
   qualitySum=15, 人数=2 = k
   成本 = 7.0 × 15 = 105 ← 最小

答案：105.0
```

## 关键点总结

1. **性价比排序**：ratio = wage / quality，按从小到大排序
2. **贪心策略**：固定一个工人作为 ratio 最大的，选择前面 quality 最小的
3. **最大堆维护**：保留 quality 最小的 k 个工人
4. **工资计算**：总工资 = ratio × qualitySum
5. **枚举所有可能**：每个工人都可能是"队长"

## 易错点

1. **ratio 的理解**：不是越小越好，而是要枚举所有可能

2. **Java 最大堆**：使用 `PriorityQueue<>(Collections.reverseOrder())`

3. **整数溢出**：quality 相加可能较大，注意 int 范围

4. **浮点数精度**：double 足够，不需要特殊处理

5. **边界条件**：只有凑够 k 个人才计算成本

## 相关题目

- [502. IPO](https://leetcode.com/problems/ipo/) - 贪心 + 堆选择
- [1834. Single-Threaded CPU](https://leetcode.com/problems/single-threaded-cpu/) - 堆模拟

## 学习建议

1. **理解问题约束**：工资按比例分配且满足最低期望
2. **贪心思想**：为什么按 ratio 排序？为什么选择 quality 最小的？
3. **堆的应用**：动态维护 top k，使用最大堆剔除最大元素
4. **推荐做题顺序**：先掌握堆的基本操作，再做本题
