---
title: 2933.high-access-employees
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 字符串, 哈希表, 排序]
---

## 题目描述

给定员工访问时间记录，找出任意60分钟内访问>=3次的高访问员工。

## 解题思路

按员工分组，时间转分钟数后排序，检查相邻三个时间首尾差<60。

## Java 代码实现

```java
class Solution {
    public List<String> findHighAccessEmployees(List<List<String>> access_times) {
        Map<String, List<Integer>> groups = new HashMap<>();
        for (List<String> entry : access_times) {
            String name = entry.get(0);
            String time = entry.get(1);
            int minutes = Integer.parseInt(time.substring(0, 2)) * 60
                        + Integer.parseInt(time.substring(2));
            groups.computeIfAbsent(name, k -> new ArrayList<>()).add(minutes);
        }

        List<String> result = new ArrayList<>();
        for (Map.Entry<String, List<Integer>> e : groups.entrySet()) {
            List<Integer> times = e.getValue();
            Collections.sort(times);
            for (int i = 0; i + 2 < times.size(); i++) {
                if (times.get(i + 2) - times.get(i) < 60) {
                    result.add(e.getKey());
                    break;
                }
            }
        }
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N logN)
- **空间复杂度**：O(N)

## 关键点总结

1. **时间转分钟数**：方便比较差值
2. **排序后检查连续3个**：首尾差<60即满足
3. **严格小于60**：一小时内不含端点

## 相关题目

- [1604. Alert Using Same Key-Card Three or More Times in a One Hour Period](https://leetcode.com/problems/alert-using-same-key-card-three-or-more-times-in-a-one-hour-period/)
