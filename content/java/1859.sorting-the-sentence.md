---
title: 1859.sorting-the-sentence
date: 2026-04-13 10:00:00
categories: easy
tags: [字符串, 排序]
---

## 题目描述

每个单词末尾有位置数字，将打乱的句子恢复原序（去掉数字）。

## 解题思路

分割字符串，根据末尾数字放到对应位置，拼接去掉数字的单词。

## Java 代码实现

```java
class Solution {
    public String sortSentence(String s) {
        String[] words = s.split(" ");
        String[] result = new String[words.length];
        for (String word : words) {
            int idx = word.charAt(word.length() - 1) - '1'; // 0-indexed
            result[idx] = word.substring(0, word.length() - 1);
        }
        return String.join(" ", result);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(N)

## 关键点总结

1. **末尾数字**：word.charAt(len-1) - '1' 得到0-indexed位置
2. **直接放置**：无需排序，O(N)填充

## 相关题目

- [1370. Increasing Decreasing String](https://leetcode.com/problems/increasing-decreasing-string/)
