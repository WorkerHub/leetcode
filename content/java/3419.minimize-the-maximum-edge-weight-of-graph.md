---
title: 3419.minimize-the-maximum-edge-weight-of-graph
date: 2026-04-13 10:00:00
categories: medium
tags: [图, BFS, 二分查找]
---

## 题目描述

给定有向图（n 个节点，边数组 `[u,v,w]`），找最小阈值 T，使得只保留权重 <= T 的边时，所有节点都能到达节点 0，不可能则返回 -1。

## Java 代码实现

```java
import java.util.*;

class Solution {
    public int minMaxWeight(int n, int[][] edges, int threshold) {
        // 收集所有权重并排序去重
        int maxW = 0;
        for (int[] e : edges) maxW = Math.max(maxW, e[2]);

        // 检查最大权重是否可行
        if (!canReachAll(n, edges, maxW)) return -1;

        // 二分最小阈值
        int lo = 1, hi = maxW;
        while (lo < hi) {
            int mid = (lo + hi) / 2;
            if (canReachAll(n, edges, mid)) hi = mid;
            else lo = mid + 1;
        }
        return lo;
    }

    private boolean canReachAll(int n, int[][] edges, int T) {
        // 构建反向图，从节点 0 出发 BFS
        List<List<Integer>> revGraph = new ArrayList<>();
        for (int i = 0; i < n; i++) revGraph.add(new ArrayList<>());

        for (int[] e : edges) {
            if (e[2] <= T) {
                // 原边 u->v，反向边 v->u
                revGraph.get(e[1]).add(e[0]);
            }
        }

        boolean[] visited = new boolean[n];
        Deque<Integer> queue = new ArrayDeque<>();
        queue.offer(0);
        visited[0] = true;
        int cnt = 1;

        while (!queue.isEmpty()) {
            int node = queue.poll();
            for (int nb : revGraph.get(node)) {
                if (!visited[nb]) {
                    visited[nb] = true;
                    cnt++;
                    queue.offer(nb);
                }
            }
        }
        return cnt == n;
    }
}
```

## 复杂度分析

- 时间复杂度：O(E log W)，E 为边数，W 为最大权重
- 空间复杂度：O(E + N)

## 关键点总结

1. 二分权重阈值，检查是否所有节点能到达 0。
2. 检查时用反向图从节点 0 出发 BFS（将"谁能到达 0"转化为"从 0 出发能到谁"）。
3. 若最大权重时仍不可行，直接返回 -1。

## 相关题目

- [1631. Path With Minimum Effort](https://leetcode.com/problems/path-with-minimum-effort/)
