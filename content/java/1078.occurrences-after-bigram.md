---
title: 1078.occurrences-after-bigram
date: 2026-04-10 15:28:55
categories: easy
tags: [字符串]
---

## 题目描述

给定字符串 text 和两个字符串 first、second，找到所有 "first second third" 模式中的 third。

**示例：**
```
输入：text = "alice is a good girl she is a good student", first = "a", second = "good"
输出：["girl","student"]
```

## 解题思路

分割字符串为单词数组，遍历检查连续三元组是否匹配 first、second 模式。

## Java 代码实现

```java
class Solution {
    public String[] findOcurrences(String text, String first, String second) {
        String[] words = text.split(" ");
        List<String> result = new ArrayList<>();

        for (int i = 0; i + 2 < words.length; i++) {
            if (words[i].equals(first) && words[i + 1].equals(second)) {
                result.add(words[i + 2]);
            }
        }

        return result.toArray(new String[0]);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)，n 为单词数
- **空间复杂度**：O(n)

## 关键点总结

1. **split 分割**：按空格切分单词
2. **遍历边界**：i + 2 < length 保证不越界
3. **equals 比较**：Java 字符串比较用 equals 而非 ==

## 相关题目

- [1065. Index Pairs of a String](https://leetcode.com/problems/index-pairs-of-a-string/)
