---
title: 2053.kth-distinct-string-in-an-array
date: 2026-04-13 10:00:00
categories: easy
tags: [数组, 哈希表, 字符串, 计数]
---

## 题目描述

给定字符串数组 `arr` 和整数 `k`，按原顺序返回数组中第 k 个只出现一次的字符串，不存在则返回空串。

## 解题思路

先统计每个字符串的出现频次，再按原顺序遍历，对频次为 1 的字符串计数，第 k 个即为答案。

## Java 代码实现

### 方法一：哈希表计数（推荐）

```java
import java.util.*;

class Solution {
    public String kthDistinct(String[] arr, int k) {
        Map<String, Integer> freq = new HashMap<>();
        for (String s : arr) freq.merge(s, 1, Integer::sum);
        for (String s : arr) {
            if (freq.get(s) == 1 && --k == 0) return s;
        }
        return "";
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)

## 关键点总结

1. 先统计频次，再按原顺序遍历保持顺序
2. 不足 k 个时返回空串

## 相关题目

- [1002. Find Common Characters](https://leetcode.com/problems/find-common-characters/)
