---
title: 1382.balance-a-binary-search-tree
date: 2026-04-13 10:00:00
categories: medium
tags: [分治, 贪心, 树, 深度优先搜索, 二叉搜索树]
---

## 题目描述

给你一棵二叉搜索树，返回一棵平衡后的二叉搜索树（节点值相同，左右子树高度差不超过 1），多种答案均可接受。

## 解题思路

两步：1) 中序遍历 BST 得到有序数组（BST 中序遍历 = 有序序列）；2) 分治建平衡 BST：每次取数组中间元素为根，左半建左子树，右半建右子树，递归保证左右高度差不超过 1。

## Java 代码实现

```java
import java.util.*;

class Solution {
    private List<Integer> nodes = new ArrayList<>();

    public TreeNode balanceBST(TreeNode root) {
        // 中序遍历得到有序序列
        inorder(root);
        // 分治构建平衡 BST
        return build(0, nodes.size() - 1);
    }

    private void inorder(TreeNode node) {
        if (node == null) return;
        inorder(node.left);
        nodes.add(node.val);
        inorder(node.right);
    }

    private TreeNode build(int left, int right) {
        if (left > right) return null;
        int mid = (left + right) / 2;
        TreeNode node = new TreeNode(nodes.get(mid));
        node.left = build(left, mid - 1);
        node.right = build(mid + 1, right);
        return node;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)，中序遍历 O(n) + 建树 O(n)。
- 空间复杂度：O(n)，存储中序序列，递归栈 O(log n)（建树时已平衡）。

## 关键点总结

1. BST 中序遍历得到有序序列，这是本题的核心性质。
2. 有序数组取中点为根，递归左右两半，保证每层高度差不超过 1。
3. 建树用下标范围 `[left, right]`，`left > right` 时返回 null。

## 相关题目

- [LeetCode 108 - Convert Sorted Array to Binary Search Tree](https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/)
- [LeetCode 109 - Convert Sorted List to Binary Search Tree](https://leetcode.com/problems/convert-sorted-list-to-binary-search-tree/)
