---
title: 2737.find-the-closest-marked-node
date: 2026-04-13 10:00:00
categories: medium
tags: [图, 最短路, 堆]
---

## 题目描述

给你有向带权图，找从起点 `s` 到任意标记节点的最短路径长度，不可达返回 -1。

## Java 代码实现

```java
import java.util.*;

class Solution {
    public int minimumDistance(int n, List<List<Integer>> edges, int s, int[] marked) {
        // 构建有向图邻接表
        List<int[]>[] graph = new List[n];
        for (int i = 0; i < n; i++) graph[i] = new ArrayList<>();
        for (List<Integer> e : edges) {
            graph[e.get(0)].add(new int[]{e.get(1), e.get(2)});
        }
        // Dijkstra 从 s 出发
        int[] dist = new int[n];
        Arrays.fill(dist, Integer.MAX_VALUE);
        dist[s] = 0;
        PriorityQueue<int[]> pq = new PriorityQueue<>(Comparator.comparingInt(a -> a[0]));
        pq.offer(new int[]{0, s});
        while (!pq.isEmpty()) {
            int[] cur = pq.poll();
            int d = cur[0], u = cur[1];
            if (d > dist[u]) continue;
            for (int[] next : graph[u]) {
                int v = next[0], w = next[1];
                if (dist[u] + w < dist[v]) {
                    dist[v] = dist[u] + w;
                    pq.offer(new int[]{dist[v], v});
                }
            }
        }
        // 找标记节点中的最短距离
        int ans = Integer.MAX_VALUE;
        for (int node : marked) ans = Math.min(ans, dist[node]);
        return ans == Integer.MAX_VALUE ? -1 : ans;
    }
}
```

## 复杂度分析

- 时间复杂度：O((n + E) log n)，E 为边数
- 空间复杂度：O(n + E)

## 关键点总结

1. 标准 Dijkstra 求单源最短路
2. 遍历标记节点取最小距离

## 相关题目

- [743. 网络延迟时间](https://leetcode.com/problems/network-delay-time/)
- [1514. 概率最大的路径](https://leetcode.com/problems/path-with-maximum-probability/)
