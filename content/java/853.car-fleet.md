---
title: 853.car-fleet
date: 2026-04-09 23:16:03
categories: medium
tags: [数组, 栈, 排序, 单调栈]
---

## 题目描述

在一条单行道上，有 n 辆车开往同一目的地，目的地是几英里以外的 target 。

给定两个整数数组 position 和 speed ，长度都是 n ，其中 position[i] 是第 i 辆车的位置， speed[i] 是第 i 辆车的速度（单位：英里/小时）。

一辆车永远不会超过前面的另一辆车，但它可以追上去，并以较慢车的速度在另一辆车旁边行驶。

车队 是指并排行驶的一辆或几辆汽车。车队的速度是车队中 最慢 的车的速度。

即便一辆车在 target 才赶上了一个车队，它们仍然会被视为是同一个车队。

返回到达目的地的 车队数量 。

**示例 1：**
```
输入：target = 12, position = [10,8,0,5,3], speed = [2,4,1,1,3]
输出：3
解释：
- 从 10 和 8 开始的车会组成一个车队，它们在 12 处相遇。车队在 target 形成。
- 在 0 处的车不会追上其他车，所以它自己是一个车队。
- 从 5 和 3 开始的车会组成一个车队，它们在 6 处相遇。注意，在到达 target 之前，车队可能发生变化（比如在 6 处，车队从 [5,3] 变为只有一辆车）。
```

**示例 2：**
```
输入：target = 10, position = [3], speed = [3]
输出：1
解释：只有一辆车，因此只有一个车队。
```

**示例 3：**
```
输入：target = 100, position = [0,2,4], speed = [4,2,1]
输出：1
解释：
- 从 0，2 和 4 开始的车向右行驶。
- 速度分别为 4，2，1 的车在位置 4，4 和 4 相遇，形成一个车队。
- 车队在 target 到达。
```

**提示：**
- n == position.length == speed.length
- 1 <= n <= 10^5
- 0 < target <= 10^6
- 0 <= position[i] < target
- position 中每个值都不同
- 0 < speed[i] <= 10^6

## 解题思路

这道题的关键是理解**车队的形成规则**：
1. 后面的车如果比前面的车快，会追上并减速到前车速度
2. 后面的车如果比前面的车慢，永远追不上

**核心观察：**
1. **按位置从后往前考虑**：离目标近的车先考虑
2. **计算到达时间**：`time = (target - position) / speed`
3. **合并规则**：如果后面的车到达时间 ≤ 前面的车，它们会合并成一个车队
4. **单调性**：从后往前，如果当前车到达时间 > 前一个车队的到达时间，说明它无法追上，形成新车队

**算法流程：**
1. 将车按位置从大到小排序（从离目标近到远）
2. 计算每辆车到达目标的时间
3. 从前往后遍历，使用栈维护车队
4. 如果当前车的到达时间 > 栈顶车队时间，说明无法追上，入栈形成新车队
5. 否则，当前车会追上栈顶车队，不入栈

## Java 代码实现

### 方法一：排序 + 单调栈（推荐）

```java
class Solution {
    public int carFleet(int target, int[] position, int[] speed) {
        int n = position.length;
        // 将位置和速度打包排序，按位置从大到小（离目标近的在前）
        Integer[] indices = new Integer[n];
        for (int i = 0; i < n; i++) indices[i] = i;
        Arrays.sort(indices, (a, b) -> position[b] - position[a]);

        // 栈存储每个车队到达目标的时间
        Deque<Double> stack = new ArrayDeque<>();

        for (int idx : indices) {
            // 计算当前车到达目标的时间
            double time = (double)(target - position[idx]) / speed[idx];

            // 如果栈为空，或当前车到达时间大于栈顶（无法追上前面的车队）
            if (stack.isEmpty() || time > stack.peek()) {
                stack.push(time);
            }
            // 否则，当前车会追上栈顶车队，合并（不入栈）
        }

        return stack.size();
    }
}
```

**代码说明：**
- 按位置排序，从离目标近的车开始处理
- 栈存储每个车队的到达时间
- 如果当前车到达时间 > 栈顶，说明它追不上，形成新车队

### 方法二：排序 + 计数（不使用栈）

```java
class Solution {
    public int carFleet(int target, int[] position, int[] speed) {
        int n = position.length;
        // 创建索引数组并按位置从大到小排序
        Integer[] indices = new Integer[n];
        for (int i = 0; i < n; i++) indices[i] = i;
        Arrays.sort(indices, (a, b) -> position[b] - position[a]);

        int fleets = 0;
        double prevTime = 0; // 前一个车队的到达时间

        for (int idx : indices) {
            // 计算当前车到达时间
            double time = (double)(target - position[idx]) / speed[idx];

            // 如果当前车到达时间 > 前一个车队时间，形成新车队
            if (time > prevTime) {
                fleets++;
                prevTime = time;
            }
        }

        return fleets;
    }
}
```

**代码说明：**
- 不使用栈，只记录前一个车队的到达时间
- 更简洁，空间复杂度更低

## 复杂度分析

- **时间复杂度**：O(n log n)，排序的时间复杂度。
- **空间复杂度**：O(n)，排序和栈的空间。

## 图解示例

**示例 1**：`target = 12, position = [10,8,0,5,3], speed = [2,4,1,1,3]`

```
位置排序（从大到小）：
位置: 10,  8,  5,  3,  0
速度:  2,  4,  1,  3,  1
时间:  1, 1, 7,  3, 12

处理过程：
1. 位置 10，时间 1  → stack = [1]，车队数 = 1
2. 位置 8， 时间 1  → 1 <= 1（追上了），不入栈，stack = [1]
3. 位置 5， 时间 7  → 7 > 1（追不上），入栈，stack = [1, 7]，车队数 = 2
4. 位置 3， 时间 3  → 3 <= 7（追上了），不入栈，stack = [1, 7]
5. 位置 0， 时间 12 → 12 > 7（追不上），入栈，stack = [1, 7, 12]，车队数 = 3

最终车队数：3
```

## 关键点总结

1. **排序策略**：按位置从大到小排序，从离目标近的车开始处理
2. **到达时间**：`time = (target - position) / speed`
3. **合并判断**：后车到达时间 ≤ 前车时间，说明会追上并合并
4. **单调栈应用**：栈中存储的是每个车队的到达时间，保持单调递增
5. **贪心思想**：每辆车尽可能追上前面的车队

## 易错点

1. **排序方向错误**：应该从位置大的（离目标近的）开始处理

2. **到达时间计算错误**：忘记减去当前位置
   - 正确：`time = (target - pos) / spd`

3. **合并条件判断错误**：
   - 应该是 `time > prevTime` 才形成新车队
   - 如果 `time <= prevTime`，说明会追上，不应该计入新车队

4. **整数除法问题**：Java 中除法要注意强转为浮点数

## 相关题目

- [735. Asteroid Collision](https://leetcode.com/problems/asteroid-collision/) - 类似的栈模拟问题
- [1776. Car Fleet II](https://leetcode.com/problems/car-fleet-ii/) - 本题的进阶版
- [739. Daily Temperatures](https://leetcode.com/problems/daily-temperatures/) - 单调栈经典题
- [402. Remove K Digits](https://leetcode.com/problems/remove-k-digits/) - 单调栈应用

## 学习建议

1. **理解物理过程**：先在纸上画图，理解车队如何形成

2. **掌握排序技巧**：
   - 为什么从后往前？因为后面的车状态不受前面的车影响
   - 如何排序？使用索引数组配合比较器

3. **单调栈模式**：
   - 栈存储什么？每个车队的到达时间
   - 何时入栈？当前车追不上前面的车队
   - 何时不入栈？当前车会追上并合并

4. **推荐做题顺序**：
   - 单调栈基础（739）→ 本题 → 进阶版（1776）
