---
title: 588.design-in-memory-file-system
date: 2026-04-09 12:32:00
categories: hard
tags: [设计, 字典树, 哈希表, 字符串]
---

## 题目描述

设计一个内存文件系统，支持 ls、mkdir、addContentToFile、readContentFromFile 操作。

## 解题思路

使用 TreeMap 模拟目录树结构，区分文件和目录。

## Java 代码实现

```java
class FileSystem {
    private TrieNode root;

    public FileSystem() {
        root = new TrieNode();
    }

    public List<String> ls(String path) {
        TrieNode node = navigate(path);
        if (node.isFile) {
            return Arrays.asList(path.substring(path.lastIndexOf('/') + 1));
        }
        return new ArrayList<>(node.children.keySet());
    }

    public void mkdir(String path) {
        navigate(path);
    }

    public void addContentToFile(String filePath, String content) {
        TrieNode node = navigate(filePath);
        node.isFile = true;
        node.content.append(content);
    }

    public String readContentFromFile(String filePath) {
        return navigate(filePath).content.toString();
    }

    private TrieNode navigate(String path) {
        TrieNode node = root;
        if (path.equals("/")) return node;
        String[] parts = path.split("/");
        for (int i = 1; i < parts.length; i++) {
            node.children.putIfAbsent(parts[i], new TrieNode());
            node = node.children.get(parts[i]);
        }
        return node;
    }

    private static class TrieNode {
        TreeMap<String, TrieNode> children = new TreeMap<>();
        StringBuilder content = new StringBuilder();
        boolean isFile = false;
    }
}
```

## 复杂度分析

- **ls/mkdir/add/read**：O(L)，L 为路径长度
- **空间复杂度**：O(总文件和目录数)

## 关键点总结

1. **TreeMap**：自动按字典序排序
2. **文件标记**：isFile 区分文件和目录
3. **路径分割**：split("/") 导航

## 相关题目

- [208. Implement Trie](https://leetcode.com/problems/implement-trie-prefix-tree/)
