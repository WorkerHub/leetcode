---
title: 3008.find-beautiful-indices-in-the-given-array-ii
date: 2026-04-13 10:00:00
categories: hard
tags: [字符串, 双指针, 哈希表, 二分查找, KMP, 滚动哈希]
---

## 题目描述

与 3006 相同：给字符串 s、a、b 和整数 k，返回所有"美丽下标"，数据规模更大（s.length 达 5×10^5）。

## 解题思路

KMP 找所有匹配位置后用双指针（而非二分）配对，两个有序列表均只向右移动，整体 O(n)。

## Java 代码实现

```java
class Solution {
    public List<Integer> beautifulIndices(String s, String a, String b, int k) {
        List<Integer> posA = kmpSearch(s, a);
        List<Integer> posB = kmpSearch(s, b);
        List<Integer> result = new ArrayList<>();
        int ptr = 0;
        for (int i : posA) {
            // 双指针：推进直到 posB[ptr] >= i-k
            while (ptr < posB.size() && posB.get(ptr) < i - k) ptr++;
            if (ptr < posB.size() && posB.get(ptr) <= i + k) {
                result.add(i);
            }
        }
        return result;
    }

    private List<Integer> kmpSearch(String text, String pattern) {
        int n = text.length(), m = pattern.length();
        int[] fail = new int[m];
        for (int i = 1, j = 0; i < m; i++) {
            while (j > 0 && pattern.charAt(i) != pattern.charAt(j)) j = fail[j - 1];
            if (pattern.charAt(i) == pattern.charAt(j)) j++;
            fail[i] = j;
        }
        List<Integer> positions = new ArrayList<>();
        for (int i = 0, j = 0; i < n; i++) {
            while (j > 0 && text.charAt(i) != pattern.charAt(j)) j = fail[j - 1];
            if (text.charAt(i) == pattern.charAt(j)) j++;
            if (j == m) {
                positions.add(i - m + 1);
                j = fail[j - 1];
            }
        }
        return positions;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n + m1 + m2)
- **空间复杂度**：O(n)

## 关键点总结

1. KMP 保证线性时间搜索
2. 双指针利用两个有序列表的单调性，ptr 不回退
3. 失配回退 j = fail[j-1] 是 KMP 核心

## 相关题目

- [3006. Find Beautiful Indices in the Given Array I](https://leetcode.com/problems/find-beautiful-indices-in-the-given-array-i/)
- [28. Find the Index of the First Occurrence in a String](https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/)
