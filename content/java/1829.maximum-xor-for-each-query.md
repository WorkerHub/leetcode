---
title: 1829.maximum-xor-for-each-query
date: 2026-04-13 10:00:00
categories: medium
tags: [位运算, 数组, 前缀和]
---

## 题目描述

有序数组nums，每次查询从当前数组找k<2^maximumBit使异或和最大，然后删除末尾元素。返回所有k。

## 解题思路

前缀异或+取反：最优k = xor_all XOR mask（mask=2^maximumBit-1）。每次删末尾元素只需再异或一次。

## Java 代码实现

```java
class Solution {
    public int[] getMaximumXor(int[] nums, int maximumBit) {
        int n = nums.length;
        int mask = (1 << maximumBit) - 1;
        int xorAll = 0;
        for (int x : nums) xorAll ^= x;
        
        int[] result = new int[n];
        for (int i = 0; i < n; i++) {
            result[i] = xorAll ^ mask;
            xorAll ^= nums[n - 1 - i]; // 删除末尾元素
        }
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(1)

## 关键点总结

1. **最优k = xor_all ^ mask**：低maximumBit位取反
2. **异或自反性**：删除元素只需再异或一次
3. **mask = 2^maximumBit - 1**

## 相关题目

- [421. Maximum XOR of Two Numbers in an Array](https://leetcode.com/problems/maximum-xor-of-two-numbers-in-an-array/)
