---
title: 1282.group-the-people-given-the-group-size-they-belong-to
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 哈希表, 贪心]
---

## 题目描述

给定 groupSizes 数组，groupSizes[i] 表示第 i 人所在组的大小。返回一种合法分组方案。

**示例：**
```
输入：groupSizes = [3,3,3,3,3,1,3]
输出：[[5],[0,1,2],[3,4,6]]
```

## 解题思路

贪心：按组大小分桶，凑够指定人数就输出一组。

## Java 代码实现

```java
class Solution {
    public List<List<Integer>> groupThePeople(int[] groupSizes) {
        Map<Integer, List<Integer>> map = new HashMap<>();
        List<List<Integer>> result = new ArrayList<>();
        
        for (int i = 0; i < groupSizes.length; i++) {
            int size = groupSizes[i];
            map.computeIfAbsent(size, k -> new ArrayList<>()).add(i);
            
            // 凑够了就输出一组
            if (map.get(size).size() == size) {
                result.add(map.get(size));
                map.put(size, new ArrayList<>());
            }
        }
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n)
- **空间复杂度**：O(n)

## 关键点总结

1. **按大小分桶**：相同组大小的人放一起
2. **满即输出**：达到组大小就加入结果
3. **重置桶**：输出后清空重新凑

## 相关题目

- [49. Group Anagrams](https://leetcode.com/problems/group-anagrams/)
