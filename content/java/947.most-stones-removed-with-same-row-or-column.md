---
title: 947.most-stones-removed-with-same-row-or-column
date: 2026-04-10 10:00:00
categories: medium
tags: [深度优先搜索, 并查集, 图]
---

## 题目描述

石头在二维平面上，同行或同列有其他石头时可移除。返回可移除的最大石头数。

## 解题思路

并查集：同行或同列的石头属于同一连通分量。答案 = 总石头数 - 连通分量数（每个分量至少保留一块）。

## Java 代码实现

```java
class Solution {
    private Map<Integer, Integer> parent = new HashMap<>();

    public int removeStones(int[][] stones) {
        for (int[] stone : stones) {
            // 用 ~y 区分行和列（行用正数，列用取反）
            union(stone[0], ~stone[1]);
        }

        // 统计连通分量数
        Set<Integer> roots = new HashSet<>();
        for (int[] stone : stones) {
            roots.add(find(stone[0]));
        }

        return stones.length - roots.size();
    }

    private int find(int x) {
        parent.putIfAbsent(x, x);
        if (parent.get(x) != x) {
            parent.put(x, find(parent.get(x)));
        }
        return parent.get(x);
    }

    private void union(int x, int y) {
        parent.put(find(x), find(y));
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n × α(n)) ≈ O(n)
- **空间复杂度**：O(n)

## 关键点总结

1. **行列合并**：同行/同列的石头归入同一集合
2. **~y 技巧**：用取反区分行号和列号
3. **结果公式**：石头数 - 连通分量数

## 相关题目

- [200. Number of Islands](https://leetcode.com/problems/number-of-islands/)
