---
title: 2785.sort-vowels-in-a-string
date: 2026-04-13 10:00:00
categories: medium
tags: [字符串, 排序]
---

## 题目描述

给你字符串 `s`，将所有元音字母按非递减顺序排序，辅音字母保持原位，返回结果字符串。

## Java 代码实现

```java
import java.util.*;

class Solution {
    public String sortVowels(String s) {
        String vowels = "aeiouAEIOU";
        char[] chars = s.toCharArray();
        List<Integer> positions = new ArrayList<>();
        List<Character> values = new ArrayList<>();

        for (int i = 0; i < chars.length; i++) {
            if (vowels.indexOf(chars[i]) >= 0) {
                positions.add(i);
                values.add(chars[i]);
            }
        }
        // 按 ASCII 升序排序元音
        Collections.sort(values);
        // 填回排序后的元音
        for (int i = 0; i < positions.size(); i++) {
            chars[positions.get(i)] = values.get(i);
        }
        return new String(chars);
    }
}
```

## 复杂度分析

- 时间复杂度：O(n log n)，排序元音部分
- 空间复杂度：O(n)

## 关键点总结

1. 收集所有元音的下标和字符值，排序后按原位置填回
2. 按 ASCII 排序（大写字母 < 小写字母）
3. 辅音字母位置和值不变

## 相关题目

- [345. 反转字符串中的元音字母](https://leetcode.com/problems/reverse-vowels-of-a-string/)
- [1859. 将句子排序](https://leetcode.com/problems/sorting-the-sentence/)
