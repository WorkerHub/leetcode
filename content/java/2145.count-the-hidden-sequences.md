---
title: 2145.count-the-hidden-sequences
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 前缀和]
---

## 题目描述

给定differences数组（differences[i]=hidden[i+1]-hidden[i]）和范围[lower,upper]，求满足条件的隐藏数组数量。

## 解题思路

设hidden[0]=x，则hidden[i]=x+prefix[i]。约束转为对x的上下界：x>=lower-min_prefix，x<=upper-max_prefix。答案=max(0, hi-lo+1)。

## Java 代码实现

```java
class Solution {
    public int numberOfArrays(int[] differences, int lower, int upper) {
        long prefix = 0;
        long minPrefix = 0, maxPrefix = 0;
        
        for (int d : differences) {
            prefix += d;
            minPrefix = Math.min(minPrefix, prefix);
            maxPrefix = Math.max(maxPrefix, prefix);
        }
        
        // x的范围：[lower - minPrefix, upper - maxPrefix]
        long xLo = lower - minPrefix;
        long xHi = upper - maxPrefix;
        return (int) Math.max(0, xHi - xLo + 1);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(1)

## 关键点总结

1. **设首元素为x**：所有元素用x+prefix表示
2. **约束转为x的范围**：只需记录min和max前缀和
3. **用long**：前缀和可能溢出int

## 相关题目

- [1893. Check if All the Integers in a Range Are Covered](https://leetcode.com/problems/check-if-all-the-integers-in-a-range-are-covered/)
