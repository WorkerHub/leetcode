---
title: 2299.strong-password-checker-ii
date: 2026-04-13 10:00:00
categories: easy
tags: [字符串]
---

## 题目描述

判断密码是否满足：长度>=8、含大小写字母、含数字、含特殊字符、无相邻重复字符。

## 解题思路

一次遍历检查所有条件：用布尔标记记录是否出现各类字符，同时检查相邻字符是否相同。

## Java 代码实现

```java
class Solution {
    public boolean strongPasswordCheckerII(String password) {
        if (password.length() < 8) return false;
        boolean hasLower = false, hasUpper = false, hasDigit = false, hasSpecial = false;
        String special = "!@#$%^&*()-+";

        for (int i = 0; i < password.length(); i++) {
            char c = password.charAt(i);
            if (i > 0 && c == password.charAt(i - 1)) return false;
            if (Character.isLowerCase(c)) hasLower = true;
            else if (Character.isUpperCase(c)) hasUpper = true;
            else if (Character.isDigit(c)) hasDigit = true;
            else if (special.indexOf(c) >= 0) hasSpecial = true;
        }
        return hasLower && hasUpper && hasDigit && hasSpecial;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(1)

## 关键点总结

1. **一次遍历多条件检查**：布尔标记
2. **相邻重复立即返回false**
3. **特殊字符集合判断**：indexOf或contains

## 相关题目

- [420. Strong Password Checker](https://leetcode.com/problems/strong-password-checker/)
