---
title: 3795.count-paths-with-bounded-product
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 动态规划]
---

## 题目描述

给定整数数组 `nums` 和整数 `k`，统计乘积不超过 `k` 的连续子数组的数量。

## Java 代码实现

```java
class Solution {
    public int numSubarrayProductLessThanK(int[] nums, int k) {
        if (k <= 1) return 0;
        int product = 1, ans = 0, left = 0;

        for (int right = 0; right < nums.length; right++) {
            product *= nums[right];
            // 收缩左边界直到乘积 <= k-1（即 < k）
            while (product >= k && left <= right) {
                product /= nums[left++];
            }
            // 以 right 为右端点的合法子数组数：right - left + 1
            ans += right - left + 1;
        }

        return ans;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)，双指针单次遍历
- 空间复杂度：O(1)

## 关键点总结

1. 所有元素为正整数，乘积具有单调性，适合滑动窗口
2. 以 `right` 为右端点的合法子数组数为 `right - left + 1`
3. `k <= 1` 时直接返回 0

## 相关题目

- [713. Subarray Product Less Than K](https://leetcode.com/problems/subarray-product-less-than-k/)
