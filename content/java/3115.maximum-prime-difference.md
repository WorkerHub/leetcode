---
title: 3115.maximum-prime-difference
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 数学, 数论]
---

## 题目描述

返回数组中两个质数元素的最大差值（数值差）。

## 解题思路

nums[i]<=100，预筛质数表，遍历数组找出所有质数的最大值和最小值，相减即可。

## Java 代码实现

```java
class Solution {
    public int maximumPrimeDifference(int[] nums) {
        boolean[] isPrime = new boolean[101];
        Arrays.fill(isPrime, true);
        isPrime[0] = isPrime[1] = false;
        for (int i = 2; i * i <= 100; i++) {
            if (isPrime[i]) {
                for (int j = i * i; j <= 100; j += i) isPrime[j] = false;
            }
        }

        int first = -1, last = -1;
        for (int i = 0; i < nums.length; i++) {
            if (isPrime[nums[i]]) {
                if (first == -1) first = i;
                last = i;
            }
        }
        return last - first;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(N)
- **空间复杂度**：O(1)

## 关键点总结

1. **预筛100以内质数**：埃氏筛O(1)
2. **题目实际求下标差**：第一个和最后一个质数下标之差
3. **1不是质数**

## 相关题目

- [204. Count Primes](https://leetcode.com/problems/count-primes/)
