---
title: 599.minimum-index-sum-of-two-lists
date: 2026-04-09 12:54:00
categories: easy
tags: [数组, 哈希表, 字符串]
---

## 题目描述

找出两个列表中共同喜爱的餐厅，要求索引和最小。

**示例：**
```
输入：list1 = ["Shogun","Tapioca Express","Burger King","KFC"],
     list2 = ["KFC","Shogun","Burger King"]
输出：["Shogun"]
```

## Java 代码实现

```java
class Solution {
    public String[] findRestaurant(String[] list1, String[] list2) {
        Map<String, Integer> map = new HashMap<>();
        for (int i = 0; i < list1.length; i++) {
            map.put(list1[i], i);
        }

        List<String> result = new ArrayList<>();
        int minSum = Integer.MAX_VALUE;

        for (int j = 0; j < list2.length; j++) {
            if (map.containsKey(list2[j])) {
                int sum = map.get(list2[j]) + j;
                if (sum < minSum) {
                    minSum = sum;
                    result.clear();
                    result.add(list2[j]);
                } else if (sum == minSum) {
                    result.add(list2[j]);
                }
            }
        }
        return result.toArray(new String[0]);
    }
}
```

## 复杂度分析

- **时间复杂度**：O(m + n)
- **空间复杂度**：O(m)

## 关键点总结

1. **HashMap**：记录 list1 的索引
2. **维护最小索引和**：遍历 list2 时更新结果
