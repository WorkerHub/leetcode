---
title: 3606.coupon-code-validator
date: 2026-04-13 10:00:00
categories: easy
tags: [字符串, 模拟]
---

## 题目描述

验证优惠券码是否有效：长度在 `[6, 20]`，只含字母和数字，且至少包含一个大写字母、一个小写字母和一个数字。

## Java 代码实现

```java
class Solution {
    public boolean isValid(String code) {
        int n = code.length();
        if (n < 6 || n > 20) return false;

        boolean hasUpper = false, hasLower = false, hasDigit = false;
        for (char c : code.toCharArray()) {
            if (Character.isUpperCase(c)) hasUpper = true;
            else if (Character.isLowerCase(c)) hasLower = true;
            else if (Character.isDigit(c)) hasDigit = true;
            else return false;  // 含非字母数字字符
        }
        return hasUpper && hasLower && hasDigit;
    }
}
```

## 复杂度分析

- 时间复杂度：O(n)，遍历字符串
- 空间复杂度：O(1)

## 关键点总结

1. 条件逐一拆开验证：长度、字符类型、三类字符均存在
2. `Character.isUpperCase/isLowerCase/isDigit` 逐字符分类
3. 遇到非字母数字字符直接返回 false

## 相关题目

- [468. Validate IP Address](https://leetcode.com/problems/validate-ip-address/)
- [65. Valid Number](https://leetcode.com/problems/valid-number/)
