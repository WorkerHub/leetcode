---
title: 74.search-a-2d-matrix
date: 2026-04-09 11:03:00
categories: medium
tags: [数组, 二分查找, 矩阵]
---

## 题目描述

给你一个满足下述两条属性的 `m x n` 整数矩阵：
- 每行中的整数从左到右按非递减顺序排列
- 每行的第一个整数大于前一行的最后一个整数

给你一个整数 `target`，如果 `target` 在矩阵中，返回 `true`；否则，返回 `false`。

**示例 1：**
```
输入：matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 3
输出：true
```

**示例 2：**
```
输入：matrix = [[1,3,5,7],[10,11,16,20],[23,30,34,60]], target = 13
输出：false
```

**提示：**
- `m == matrix.length`
- `n == matrix[i].length`
- `1 <= m, n <= 100`
- `-10^4 <= matrix[i][j], target <= 10^4`

## 解题思路

核心思想：将二维矩阵看作一维有序数组，使用二分查找

关键观察：
- 矩阵可以看作一维有序数组
- 通过 mid / n 和 mid % n 转换为二维坐标

## Java 代码实现

```java
class Solution {
    public boolean searchMatrix(int[][] matrix, int target) {
        int m = matrix.length, n = matrix[0].length;
        int left = 0, right = m * n - 1;

        while (left <= right) {
            int mid = left + (right - left) / 2;
            // 将一维索引转换为二维坐标
            int row = mid / n;
            int col = mid % n;
            int value = matrix[row][col];

            if (value == target) {
                return true;
            } else if (value < target) {
                left = mid + 1;
            } else {
                right = mid - 1;
            }
        }

        return false;
    }
}
```

## 复杂度分析

- 时间复杂度：O(log(m × n))
- 空间复杂度：O(1)

## 关键点总结

1. **一维化**：将二维坐标转换为一维索引
2. **坐标转换**：row = mid / n, col = mid % n
3. **二分查找**：标准模板

## 易错点

1. **坐标转换**：注意行列的计算方式
2. **边界**：right初始化为 m*n-1
3. **防止溢出**：使用 `left + (right - left) / 2` 而非 `(left + right) / 2`

## 相关题目

- [240. 搜索二维矩阵 II](https://leetcode.com/problems/search-a-2d-matrix-ii/)

## 学习建议

这道题结合了二分查找和矩阵操作，理解坐标转换。
