---
title: 2120.execution-of-all-suffix-instructions-staying-in-a-grid
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 字符串, 模拟]
---

## 题目描述

n×n网格，机器人从startPos出发，对每个后缀指令s[i:]，计算能执行多少条指令直到越界。

## 解题思路

直接模拟：对每个起点i，从startPos开始执行s[i:]直到越界。O(m^2)，m<=500可接受。

## Java 代码实现

```java
class Solution {
    public int[] executeInstructions(int n, int[] startPos, String s) {
        int m = s.length();
        int[] result = new int[m];
        
        for (int i = 0; i < m; i++) {
            int r = startPos[0], c = startPos[1];
            int count = 0;
            for (int j = i; j < m; j++) {
                char ch = s.charAt(j);
                if (ch == 'L') c--;
                else if (ch == 'R') c++;
                else if (ch == 'U') r--;
                else r++;
                
                if (r < 0 || r >= n || c < 0 || c >= n) break;
                count++;
            }
            result[i] = count;
        }
        return result;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(M^2)
- **空间复杂度**：O(M)

## 关键点总结

1. **暴力模拟**：每个起点独立执行，m<=500时O(m^2)可接受
2. **越界即停**：一旦越界break
3. **每次重置起点**：startPos不变

## 相关题目

- [54. Spiral Matrix](https://leetcode.com/problems/spiral-matrix/)
