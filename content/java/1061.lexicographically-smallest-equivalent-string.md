---
title: 1061.lexicographically-smallest-equivalent-string
date: 2026-04-10 15:28:55
categories: medium
tags: [并查集, 字符串]
---

## 题目描述

给定 s1、s2 建立字符等价关系（传递性），返回 baseStr 的字典序最小等价字符串。

**示例：**
```
输入：s1 = "parker", s2 = "morris", baseStr = "parser"
输出："makkek"
```

## 解题思路

并查集维护等价关系，合并时让字典序小的字符成为根。对 baseStr 每个字符找其等价类最小代表。

## Java 代码实现

```java
class Solution {
    private int[] parent = new int[26];

    public String smallestEquivalentString(String s1, String s2, String baseStr) {
        for (int i = 0; i < 26; i++) parent[i] = i;

        // 建立等价关系
        for (int i = 0; i < s1.length(); i++) {
            union(s1.charAt(i) - 'a', s2.charAt(i) - 'a');
        }

        // 构建结果
        StringBuilder sb = new StringBuilder();
        for (char c : baseStr.toCharArray()) {
            sb.append((char) (find(c - 'a') + 'a'));
        }

        return sb.toString();
    }

    private int find(int x) {
        if (parent[x] != x) parent[x] = find(parent[x]);
        return parent[x];
    }

    private void union(int x, int y) {
        int px = find(x), py = find(y);
        if (px != py) {
            // 字典序小的作为根
            if (px < py) parent[py] = px;
            else parent[px] = py;
        }
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n + m)，n 为 s1 长度，m 为 baseStr 长度
- **空间复杂度**：O(1)，只需 26 个字母的并查集

## 关键点总结

1. **并查集维护等价关系**：合并时让小字符作根
2. **路径压缩**：优化查找效率
3. **传递性**：并查集自然处理传递性

## 相关题目

- [990. Satisfiability of Equality Equations](https://leetcode.com/problems/satisfiability-of-equality-equations/)
- [721. Accounts Merge](https://leetcode.com/problems/accounts-merge/)
