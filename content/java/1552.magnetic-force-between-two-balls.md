---
title: 1552.magnetic-force-between-two-balls
date: 2026-04-13 10:00:00
categories: medium
tags: [数组, 二分查找, 排序]
---

## 题目描述

将 m 个球放在 position 数组的 m 个位置，使任意两球间最小磁力最大化。返回最大化的最小磁力。

**示例：**
```
输入：position = [1,2,3,4,7], m = 3
输出：3
```

## 解题思路

二分答案：二分最小磁力 d，贪心验证能否放下 m 个球（排序后从左往右尽早放）。

## Java 代码实现

```java
class Solution {
    public int maxDistance(int[] position, int m) {
        Arrays.sort(position);
        int lo = 1, hi = position[position.length - 1] - position[0];
        int ans = 1;
        
        while (lo <= hi) {
            int mid = (lo + hi) / 2;
            if (canPlace(position, m, mid)) {
                ans = mid;
                lo = mid + 1;
            } else {
                hi = mid - 1;
            }
        }
        return ans;
    }
    
    private boolean canPlace(int[] position, int m, int minDist) {
        int count = 1;
        int last = position[0];
        for (int i = 1; i < position.length; i++) {
            if (position[i] - last >= minDist) {
                count++;
                last = position[i];
                if (count == m) return true;
            }
        }
        return false;
    }
}
```

## 复杂度分析

- **时间复杂度**：O(n log n + n log D)，D = max - min
- **空间复杂度**：O(1)

## 关键点总结

1. **二分答案**：二分最小磁力值
2. **贪心验证**：从左到右尽早放球
3. **求最大值**：lo = mid + 1 模板

## 相关题目

- [410. Split Array Largest Sum](https://leetcode.com/problems/split-array-largest-sum/)
- [1011. Capacity to Ship Packages Within D Days](https://leetcode.com/problems/capacity-to-ship-packages-within-d-days/)
