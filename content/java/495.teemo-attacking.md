---
title: 495.teemo-attacking
date: 2026-04-09 11:10:00
categories: easy
tags: [数组, 模拟]
---

## 题目描述

在《英雄联盟》的世界中，有一个叫 "提莫" 的英雄。他的攻击可以让敌方英雄艾希进入中毒状态。

当提莫攻击艾希，艾希的中毒状态正好持续 duration 秒。

正式地讲，提莫在 t 发起发起攻击意味着艾希在时间区间 [t, t + duration - 1]（含 t 和 t + duration - 1）处于中毒状态。如果提莫在中毒影响结束前再次攻击，中毒状态计时器将会重置，在新的攻击之后，中毒影响将会在 duration 秒后结束。

给你一个非递减的整数数组 timeSeries，其中 timeSeries[i] 表示提莫在 timeSeries[i] 秒时对艾希发起攻击，以及一个表示中毒持续时间的整数 duration。

返回艾希处于中毒状态的总秒数。

**示例 1：**
```
输入：timeSeries = [1,4], duration = 2
输出：4
解释：提莫攻击对艾希的影响如下：
- 第 1 秒，提莫攻击艾希并使其立即中毒。中毒状态会维持 2 秒，即第 1 秒和第 2 秒。
- 第 4 秒，提莫再次攻击艾希，艾希中毒状态又持续 2 秒，即第 4 秒和第 5 秒。
艾希在第 1、2、4、5 秒处于中毒状态，所以总中毒秒数是 4。
```

**示例 2：**
```
输入：timeSeries = [1,2], duration = 2
输出：3
解释：提莫攻击对艾希的影响如下：
- 第 1 秒，提莫攻击艾希并使其立即中毒。中毒状态会维持 2 秒，即第 1 秒和第 2 秒。
- 第 2 秒，提莫再次攻击艾希，并重置中毒计时器，艾希中毒状态需要持续 2 秒，即第 2 秒和第 3 秒。
艾希在第 1、2、3 秒处于中毒状态，所以总中毒秒数是 3。
```

**提示：**
- 1 <= timeSeries.length <= 10^4
- 0 <= timeSeries[i], duration <= 10^7
- timeSeries 按非递减顺序排列

## 解题思路

这是一道简单的模拟题，核心是理解中毒时间的叠加规则。

### 核心思想

1. **区间合并思想**：
   - 每次攻击产生一个中毒区间 [t, t + duration - 1]
   - 如果新攻击在旧中毒结束前，则重置计时器
   - 计算所有中毒时间的并集

2. **两种情况**：
   - **不重叠**：`timeSeries[i] - timeSeries[i-1] >= duration`
     - 前一次中毒完全结束，贡献 duration 秒
   - **重叠**：`timeSeries[i] - timeSeries[i-1] < duration`
     - 贡献 `timeSeries[i] - timeSeries[i-1]` 秒

3. **算法流程**：
   - 遍历攻击时间
   - 计算每次攻击的实际贡献时间
   - 最后一次攻击一定贡献完整的 duration

## Java 代码实现

### 方法一：一次遍历（推荐）

```java
class Solution {
    public int findPoisonedDuration(int[] timeSeries, int duration) {
        if (timeSeries.length == 0) return 0;

        int total = 0;
        int n = timeSeries.length;

        // 遍历每次攻击
        for (int i = 0; i < n - 1; i++) {
            // 计算两次攻击的间隔，取间隔和持续时间的最小值
            total += Math.min(timeSeries[i + 1] - timeSeries[i], duration);
        }

        // 最后一次攻击贡献完整的 duration
        total += duration;

        return total;
    }
}
```

### 方法二：区间思维

```java
class Solution {
    public int findPoisonedDuration(int[] timeSeries, int duration) {
        if (timeSeries.length == 0) return 0;

        int total = 0;

        for (int i = 0; i < timeSeries.length; i++) {
            if (i == timeSeries.length - 1) {
                // 最后一次攻击
                total += duration;
            } else {
                // 下一次攻击时间
                int nextAttack = timeSeries[i + 1];
                // 当前攻击的结束时间
                int endTime = timeSeries[i] + duration;

                // 如果下次攻击在当前中毒结束前
                if (nextAttack < endTime) {
                    total += nextAttack - timeSeries[i];
                } else {
                    total += duration;
                }
            }
        }

        return total;
    }
}
```

### 方法三：维护中毒结束时间

```java
class Solution {
    public int findPoisonedDuration(int[] timeSeries, int duration) {
        if (timeSeries.length == 0) return 0;

        int total = 0;
        int endTime = 0; // 当前中毒结束时间

        for (int attackTime : timeSeries) {
            // 如果当前还在中毒状态
            if (attackTime < endTime) {
                // 只增加额外的中毒时间
                total += attackTime + duration - endTime;
            } else {
                // 完全不重叠，增加完整的 duration
                total += duration;
            }

            // 更新结束时间
            endTime = attackTime + duration;
        }

        return total;
    }
}
```

### 方法四：简洁版

```java
class Solution {
    public int findPoisonedDuration(int[] timeSeries, int duration) {
        int total = duration; // 最后一次攻击的贡献

        for (int i = 0; i < timeSeries.length - 1; i++) {
            total += Math.min(timeSeries[i + 1] - timeSeries[i], duration);
        }

        return total;
    }
}
```

## 复杂度分析

所有方法：
- **时间复杂度**：O(n)
  - 遍历数组一次

- **空间复杂度**：O(1)
  - 只使用常数额外空间

## 关键点总结

1. **区间处理**：每次攻击产生一个时间区间
2. **重叠判断**：比较攻击间隔和持续时间
3. **边界处理**：最后一次攻击特殊处理
4. **取最小值**：`Math.min(interval, duration)` 是关键
5. **非递减性**：数组已排序，简化处理

## 易错点

1. **最后一次攻击**
   - 最后一次攻击一定贡献完整的 duration
   - 不要忘记处理

2. **区间理解**
   - 中毒时间是 [t, t + duration - 1]
   - 共 duration 秒，不是 duration + 1

3. **重叠计算**
   - 重叠时贡献的是间隔，不是 duration
   - 不是简单的相加

4. **空数组**
   - timeSeries 为空时返回 0
   - 需要特殊判断（虽然题目保证长度 >= 1）

5. **整数溢出**
   - duration 和 timeSeries[i] 可能较大（最大 10^7）
   - Java 中 int 范围约 2.1 × 10^9，本题不会溢出
   - 但要注意 attackTime + duration 的最大值为 2 × 10^7

## 相关题目

- [56. Merge Intervals](https://leetcode.com/problems/merge-intervals/) - 区间合并
- [57. Insert Interval](https://leetcode.com/problems/insert-interval/) - 插入区间
- [228. Summary Ranges](https://leetcode.com/problems/summary-ranges/) - 区间总结
- [352. Data Stream as Disjoint Intervals](https://leetcode.com/problems/data-stream-as-disjoint-intervals/)

## 学习建议

1. **理解题意**：
   - 仔细阅读中毒规则
   - 理解重置的含义
   - 画图帮助理解

2. **区间处理技巧**：
   - 合并、重叠、包含等关系
   - 使用起止时间标记
   - 排序简化处理

3. **边界情况**：
   - 空数组
   - 单个元素
   - 完全不重叠
   - 完全重叠

4. **代码简洁性**：
   - 方法四最简洁
   - 但方法一最易理解
   - 平衡可读性和简洁性

5. **模拟题技巧**：
   - 严格按照题目规则
   - 不要过度思考
   - 测试各种情况

6. **优化思路**：
   - 一次遍历 vs 多次遍历
   - 利用数据特性（已排序）
   - 避免不必要的计算
