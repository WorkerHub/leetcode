---
title: 3051.find-candidates-for-data-scientist-position
date: 2026-04-13 10:00:00
categories: easy
tags: [数据库]
---

## 题目描述

表：`Candidates`

```
+-------------+---------+
| Column Name | Type    |
+-------------+---------+
| candidate_id| int     |
| skill       | varchar |
+-------------+---------+
```

`(candidate_id, skill)` 是主键。

找出拥有所有以下技能的候选人 ID：Python、Tableau、PostgreSQL。

结果按 `candidate_id` 升序返回。

## SQL 代码实现

### 方法一：GROUP BY + HAVING（推荐）

```sql
SELECT candidate_id
FROM Candidates
WHERE skill IN ('Python', 'Tableau', 'PostgreSQL')
GROUP BY candidate_id
HAVING COUNT(DISTINCT skill) = 3
ORDER BY candidate_id ASC;
```

### 方法二：多次 JOIN

```sql
SELECT DISTINCT c1.candidate_id
FROM Candidates c1
JOIN Candidates c2 ON c1.candidate_id = c2.candidate_id AND c2.skill = 'Tableau'
JOIN Candidates c3 ON c1.candidate_id = c3.candidate_id AND c3.skill = 'PostgreSQL'
WHERE c1.skill = 'Python'
ORDER BY c1.candidate_id ASC;
```

## 复杂度分析

- 时间复杂度：O(n)，n 为记录数
- 空间复杂度：O(k)，k 为候选人数

## 关键点总结

1. 先过滤三种技能，再按候选人分组
2. `HAVING COUNT(DISTINCT skill) = 3` 确保三种技能都有
3. `DISTINCT` 防止重复计数

## 易错点

1. 需要精确匹配 3 种技能（不多不少）
2. 若候选人同一技能出现多次，用 `DISTINCT` 去重

## 相关题目

- [184. 部门工资最高的员工](https://leetcode.com/problems/department-highest-salary/)
- [1978. 上级经理已离职的公司员工](https://leetcode.com/problems/employees-whose-manager-left-the-company/)
