---
title: 1990.count-the-number-of-experiments
date: 2026-04-13 10:00:00
categories: medium
tags: [数据库]
---

## 题目描述

表 `Experiments`：
```
+-----------------+--------+
| Column Name     | Type   |
+-----------------+--------+
| experiment_id   | int    |
| platform        | enum   |
| experiment_name | varchar|
+-----------------+--------+
experiment_id 是主键。
platform 是枚举类型，可能值为 'Android', 'IOS', 'Web'。
experiment_name 是枚举类型，可能值为 'Reading', 'Sports', 'Programming'。
```

统计每个 `(platform, experiment_name)` 组合出现的次数。对于从未出现的组合，其计数为 0。

结果按 `platform` 和 `experiment_name` 任意顺序排列。

---

## 解题思路

### 核心思想：笛卡尔积 + LEFT JOIN

先用 CROSS JOIN 生成所有 3 × 3 = 9 种组合（平台 × 实验名），再 LEFT JOIN 原表统计每种组合的数量。

---

## 代码实现

### 方法一：CROSS JOIN + LEFT JOIN（推荐）

```sql
-- 生成所有平台和实验名的组合
WITH platforms AS (
    SELECT 'Android' AS platform
    UNION ALL SELECT 'IOS'
    UNION ALL SELECT 'Web'
),
experiments AS (
    SELECT 'Reading' AS experiment_name
    UNION ALL SELECT 'Sports'
    UNION ALL SELECT 'Programming'
),
all_combinations AS (
    SELECT p.platform, e.experiment_name
    FROM platforms p CROSS JOIN experiments e
)
SELECT
    ac.platform,
    ac.experiment_name,
    COUNT(ex.experiment_id) AS num_experiments
FROM all_combinations ac
LEFT JOIN Experiments ex
    ON ac.platform = ex.platform
    AND ac.experiment_name = ex.experiment_name
GROUP BY ac.platform, ac.experiment_name;
```

### 方法二：直接 CROSS JOIN 行内

```sql
SELECT
    p.platform,
    e.experiment_name,
    COUNT(ex.experiment_id) AS num_experiments
FROM
    (SELECT 'Android' AS platform UNION ALL SELECT 'IOS' UNION ALL SELECT 'Web') p
CROSS JOIN
    (SELECT 'Reading' AS experiment_name UNION ALL SELECT 'Sports' UNION ALL SELECT 'Programming') e
LEFT JOIN Experiments ex
    ON p.platform = ex.platform
    AND e.experiment_name = ex.experiment_name
GROUP BY p.platform, e.experiment_name;
```

---

## 复杂度分析

| 操作 | 说明 |
|------|------|
| CROSS JOIN | 生成 9 行 |
| LEFT JOIN | O(n)，n = Experiments 行数 |

---

## 关键点总结

1. 用 CROSS JOIN 生成所有可能的平台和实验名组合
2. LEFT JOIN 保证未出现的组合也在结果中
3. `COUNT(ex.experiment_id)` 对 NULL 计为 0（LEFT JOIN 后未匹配行为 NULL）

---

## 易错点

- 必须用 `COUNT(ex.experiment_id)` 而非 `COUNT(*)`，后者对 NULL 行也计 1
- CROSS JOIN 生成全部 9 种组合，不要漏掉平台或实验名

---

## 相关题目

- [1280. 学生们参加各科测试的次数](https://leetcode.com/problems/students-and-examinations/)
- [1757. 可回收且低脂的产品](https://leetcode.com/problems/recyclable-and-low-fat-products/)
