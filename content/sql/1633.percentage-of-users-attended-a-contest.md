---
title: 1633.percentage-of-users-attended-a-contest
date: 2026-04-13 10:00:00
categories: easy
tags: [数据库]
---

## 题目描述

表：`Users`
```
+-------------+---------+
| Column Name | Type    |
+-------------+---------+
| user_id     | int     |
| user_name   | varchar |
+-------------+---------+
user_id 是主键
```

表：`Register`
```
+-------------+---------+
| Column Name | Type    |
+-------------+---------+
| contest_id  | int     |
| user_id     | int     |
+-------------+---------+
(contest_id, user_id) 是主键
```

查询出每个竞赛的用户注册百分比，保留两位小数。

结果按百分比**降序**排列，若百分比相同，按 `contest_id` **升序**排列。

百分比 = 注册该竞赛的用户数 / 总用户数 * 100

## 解题思路

1. 统计每个竞赛的注册用户数
2. 除以总用户数，乘以 100，保留两位小数
3. 排序

## 代码实现

### 方法一：GROUP BY + ROUND（推荐）

```sql
SELECT
    contest_id,
    ROUND(COUNT(user_id) * 100 / (SELECT COUNT(*) FROM Users), 2) AS percentage
FROM Register
GROUP BY contest_id
ORDER BY percentage DESC, contest_id ASC;
```

### 方法二：子查询单独计算总用户数

```sql
SELECT
    r.contest_id,
    ROUND(COUNT(r.user_id) * 100.0 / u.total, 2) AS percentage
FROM Register r
CROSS JOIN (SELECT COUNT(*) AS total FROM Users) u
GROUP BY r.contest_id, u.total
ORDER BY percentage DESC, contest_id ASC;
```

## 复杂度分析

| 方法 | 说明 |
|------|------|
| GROUP BY | 标准分组聚合，效率高 |
| CROSS JOIN | 将总数作为常量与每行连接 |

## 关键点总结

1. 百分比 = `COUNT(user_id) * 100 / (SELECT COUNT(*) FROM Users)`
2. `ROUND(..., 2)` 保留两位小数
3. 排序：先按百分比降序，再按 `contest_id` 升序

## 易错点

- 除法可能产生整数除法截断（MySQL 中整数/整数=整数），用 `* 100.0` 或将分子乘以 100 后再除
- 排序条件是百分比降序 + contest_id 升序（双重排序）
- `ROUND` 的第二个参数是小数位数

## 相关题目

- [1084. 销售分析 III](https://leetcode.com/problems/sales-analysis-iii/)
- [1211. 查询结果的质量和占比](https://leetcode.com/problems/queries-quality-and-percentage/)
